# あつまれ、あれこれ — Claude Code 作業開始ガイド

**新しいセッションを始める前に、必ずこのファイル全体を読んでください。** ここに書かれていることが分からないまま実装を始めると、既存の設計と矛盾するコードを書いてしまいます。

---

## 1. このアプリについて

スマホでみんなで遊ぶパーティゲームアプリ。**「一つの点からすべてを喜ばせる」——スマホを人を分断する道具ではなく、集める道具として使う**という思想が土台にあります。

## 2. 大切なこと・決めたこと（絶対に守ること・変更禁止・追加のみ可）

1. **プレイヤーファースト**：実装する前に「実際にその場に座って複数人で遊んでいる状況」を具体的に想像すること。物理的な動作（スマホを渡す、画面をみんなに見せる）が伴う場面では、見えてはいけない情報が映っていないか必ず確認する
2. **1つずつ完璧にしてから次へ**：新しいゲーム・カセットは、器だけ先に用意し、中身は1つずつ完璧にしてから次に着手する
3. **収益化しない**：ガチャ・課金・広告は一切なし
4. **スマホを「人を分断する道具」ではなく「人を集める道具」として使う**：技術は主役ではなく、審判役・進行役・記録係に徹する。オンライン対戦（見知らぬ人同士のマッチング）は採用しない
5. **心理的な配慮は「同意」ではなく「設計」で解決する**：同意画面は身体的リスクに限定する（多用すると本当に大事な警告が埋もれる）。心理面の配慮は、称号（勝ち負けと関係なく褒めるだけ）のような設計で解決する
6. **ルールの伝え方は機能の一部**：セットアップ画面を必ず挟む。「準備OK」を押すまで始まらない
7. **UI・アニメーションの品質基準**：分かりやすさを一番に、遊んでいてアドレナリンが出る楽しさを。ただし「1画面につき、やることは1つだけ」「アニメーションは意味を持つ時だけ、ただし意味がある時は出し惜しみしない」「褒める時は全力で、責める時は静かに」「すべての演出はスキップできる」が原則。桜井政博氏のスマブラのようなゲームUIとiOSのような美しさを目指すが、見た目をそのまま真似ない
8. **デプロイ・運用のルール**：`git push`はレビュー完了後、明示的な指示を受けてから（commitまでは自由）。`docs/`は常に最新に保つ。実装前に、実際のコードの行番号・関数名を確認してから着手する
9. **借りたものは、その世界観ごと運ばれる**：あるゲームから別のゲームへロジックを流用すると、①言葉づかいがその世界観のまま漏れる、②片方には実装して満足し、もう片方に入れ忘れる、という事故が起きやすい。並走する仕組みに変更を入れる時は、必ず両方に反映されているか確認する
10. **一つの点からすべてを喜ばせる**：あらゆる状況を想定し尽くし、そこに来た誰もが守られ、楽しめる状態を作る

## 3. 今日までに何度も踏んだ落とし穴（再発防止リスト）

1. **並走する実装の片方だけ直して、もう片方に反映し忘れる**（手渡し版と1人1台版、通常版と競争版など）
2. **借りたロジック・用語が、その世界観のまま漏れる**（別ゲームの言葉がそのまま残る）
3. **専用テーマ・スタイルの適用範囲が、実際にプレイ中の画面だけに限定され、設定・ウィザード・大画面が漏れる**
4. **手書きの一覧・条件分岐に、新しい項目を登録し忘れる**。可能な限り一覧化・自動判定（データから導く）にする
5. **IDやモード名を変えると、記録・履歴に古いIDが直書きされている箇所が取り残される**。`grep`で参照箇所を全部洗い出し、テストで固定する
6. **匿名性・秘密情報が、新しい画面や機能で漏れる**
7. **タイマー・カウントダウンが、新しいフェーズをまたぐと重複したり止まったりする**。既存の共通処理を使う
8. **上限・下限のチェックが片方向にしか効いていない**
9. **1つの修正が、新しい見落としの発生源になることがある**
10. **テストは、実際に赤くなることを確認してから直す**
11. **複数のテストプロセスを同時に走らせると、メモリを食い潰して信頼できない結果になる**。最終確認は何も並走させないクリーンな1回の実行で行う
12. **「動くはず」で終わらせず、実際に手を動かして確認してから報告する**。特に見た目・演出・タイミングに関するものは、テストが緑でも「確認した」とは言えない。実サーバーを立てて実際に操作する
13. **「直った」と報告する前に、本当に直っているか目で見て確認する**。前回報告と実態がズレた実例が複数ある
14. **画面遷移とサーバー確認は、操作の性質で使い分ける（どちらか一方を全部に当てはめない）**。
    （a）**本人の意思が確定している操作**（退室・とりやめ等）：先に画面を動かし、サーバーへの頼みは本人特定情報（部屋コード＋memberId）を添えて裏で送る。返事待ちにすると、通信が詰まった時に最大タイムアウト分ボタンが無反応に見える。
    （b）**存在が不確かな対象への遷移**（「部屋」ボタンで今の部屋に飛ぶ等）：逆に、遷移する前にサーバーへ確認する。古い記憶のまま消えた部屋に飛ぶと、新しい不整合バグを生む。
    どちらの場合も**状態の権威はサーバー**。socketのつなぎ直しをまたぐと socket.data の「このsocketは誰か」の印は消えるので、サーバーは空の頼みを**「ok」と返しながら黙って無視してはいけない**（第35弾Aの退室バグ。できていないのに ok を返すのが一番悪い）
15. **報告されたバグが手元で再現しない時は、デプロイ先で動いているコミットと手元の差を先に確認する**。未pushのコミットに入っている機能は本番に無い（開始3-2-1で実際に起きた。手元では全ゲーム動くのに、本番にはそもそも実装が無かった）

## 4. 技術構成

- Node.js + Express + SQLite（`better-sqlite3`）、フロントは基本単一HTML（`public/index.html`）
- テスト可能・再利用可能なロジックは`public/js/`に切り出す（例：`wolf-logic.js`, `bomb-logic.js`）
- リアルタイム同期の土台は`realtime.js`。ゲームごとの進行役は`GAME_DRIVERS`に登録するだけで繋がる形（`wolf-room.js`, `bomb-room.js`など）
- 演出の共通部品は`public/js/fx.js`（`hold`, `flash`, `banner`, `flip`, `countUp`, `stagger`, `fly`, `alive`, `notice`など）。**新しい演出は、ここに追加する形で作る。ゲームごとに個別実装しない**
- 称号システムは`public/js/titles.js`（`STAT_SHAPE`にカウンタ、`CATALOG`にパーツを追加する形）
- 絵文字はTwemoji（SVG）に統一済み。新しい絵文字を使う時は `node tools/gen-emoji-list.js --fetch` を実行する
- `npm test`：全テストを1コマンドで実行（`tests/`配下、`package.json`の`scripts.test`参照）
- `npm run playthrough`：通しプレイ検証

## 5. 作業の進め方

- **push は、明示的な指示があるまでしない**（commitは自由）
- **意味のある単位でコミットを分け、区切りごとに報告する**。一度に全部やろうとしない
- **判断に迷ったら、上記の原則に沿って自分で判断し、理由を残す**（無人実行時は特に）
- **実装中に気づいた、プレイヤーのために追加した方がいいことがあれば、指示になくても提案する**

## 6. 関連ドキュメント（`docs/`フォルダ）

着手するカセット・機能に応じて、該当するものを読んでください。

- `あつまれあれこれ_棚全体の構想.md` — 全カセットの構想一覧
- `あれそれどれこれ_プロジェクト概要.md`
- `爆弾解除設計.md`
- `クイズ王設計.md`
- `オークション設計.md`
- `脱出ミッション設計.md`
- `演出の共通部品.md`
- `絵文字とアイコン.md`
- `レビュー_お題と問題の追加候補.md`
- `レビュー_オークション品物プール.md`

**このファイル（`CLAUDE.md`）と`docs/`の内容が食い違っている場合は、実際のコードを正としてください。** ドキュメントは更新が追いついていない可能性があります。
