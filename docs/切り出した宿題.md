# 切り出した宿題

指示の途中で見つかったが、**その指示の中で解くべきではない**と判断して外したもの。

「あとでやる」を口約束にすると消えるので、ここに書く。
**何を・なぜ外したか・再開する時に何を確かめるか**まで書く。

---

## 1. 手渡しと部屋の並走を安全にする（第41弾から切り出し・2026-09-07）

### 何を外したか

指示41 2-4 の当初案：

> 【1台専用・部屋あり】
> このカセットは、1台のスマホをまわして遊びます。
> **部屋はそのまま残ります。ほかの人のスマホは待ちます。**
> 　　　[ はじめる ]　　やめる
>
> 部屋を持ったまま1台専用を選んだら：部屋は残り、他の人の画面は
> 「いま、1台のスマホで遊んでいます」で待つ。終わって戻ると待合に復帰

### なぜ外したか

**第33弾B-3が塞いだ穴を、開け直すものだった。**

`public/index.html` の該当箇所にこう書いてある：

> 部屋を持ったまま「1台のスマホで遊ぶ」へ入らない。
> 部屋に紐づいたまま手渡しへ進むと、「もう一度」や「ゲームを終了」が
> **部屋の方に流れて、状態が混ざる（実機で起きた）**。

本人の裁定（2026-09-07）：

1. 第33弾の安全策は**実際に実機で起きた事故の記録**。
   指示41 2-4 を書いた時にこの事故を確認しておらず、
   「部屋を残す」は机上で考えた理想の流れだった
2. 状態の混ざりを直したうえで部屋を残すのは、確認画面1枚の話ではなく
   **独立した実装作業**。指示41に混ぜると「1指示1つの使える状態」から外れる
3. **安全性が実装の完成度より優先される。**
   分離できる保証が無い状態で、部屋を残す方を選ぶ理由がない

### 第41弾ではどうしたか

確認画面の文面を、閉じてから進む形に確定した：

```
このカセットは、1台のスマホをまわして遊びます。
部屋を閉じて、1台のスマホで遊びますか？
      [ 部屋を閉じて はじめる ]    やめる
```

実体は `public/js/ui-text.js` の `WAY.leaveRoom`。
進行役なら「閉じて」、参加者なら「抜けて」。

### 再開する時に確かめること

**「部屋を残す」を実装する前に、まず混ざりを直す。** 順番を逆にしない。

1. 手渡し中の「もう一度」「ゲームを終了」が、**部屋ではなく手渡し側に流れる**こと
   （第33弾B-3で実機で壊れたのはここ）
2. 待っている人の画面に「いま、1台のスマホで遊んでいます」が出ること（新規）
3. 終わって戻った時に待合へ復帰すること
4. 上の3つが揃ってから、`WAY.leaveRoom` を「残す」形に書き換える

**この宿題の完了条件は、確認画面の文面ではなく、上の1〜3が動くこと。**

### 未確認のまま残っていること

第33弾B-3のコメントにある「実機で起きた」は、
**コード内のコメントとしてのみ**受け取っている。
当時の実機確認の記録そのものは、本人もこちらも見ていない。
再開時、もし混ざりが再現しないなら、この前提から確かめ直す。

---

## 2. 検査に複製された「カセットを押す」4行（第41弾で気づいた・2026-09-07）

### 何が起きているか

```js
cart.click();
if (activeScreen(doc) === 'scr-shelf') cart.click();  // 中央でなければ2回目で選択
await sleep(win, 100);
```

この並びが **tests/ の8ファイルに53箇所** 複製されている。

| ファイル | 箇所 |
|---|---|
| `smoke.js` | 37 |
| `rt-screens.js` | 7 |
| `wolf-vote.js` | 4 |
| `harness.js` | 1 |
| `shelf.js` | 1 |
| `secrecy-gates.js` | 1 |
| `fixes36.js` | 1 |
| `playthrough.js` | 1 |
| **合計** | **53** |

数え方：`.cart[…]` から取った要素を押し、その直後に「まだ棚なら押し直す」を
置いている箇所を1つと数える。押し直しの2つの書き方
（素のものと `&& !doc.querySelector('.cassette-warp')` 付き）を両方数える。

> **訂正（2026-09-08）。** 最初この節には「6ファイルに47箇所」と書いていた。
> 両方とも誤り。
>
> - 書いた内訳（34+5+4+1+1+1）の**合計は46**で、見出しの47と合っていなかった
> - その内訳は、変数名が `cart` で、行が `cart.click();` で終わるものだけを
>   数える grep から出ていた。**`.cassette-warp` 付きの書き方と、
>   受け手が `cart2`／`bomb`／`wolf` の箇所が丸ごと落ちていた**
>
> 数え直しは2通りの方法で別々に行い、どちらも同じ 53／8 に着いた。
> **書いた数字を、確かめずに宿題として残していた**——
> 宿題の側にも「確かめていないこと」があった、という記録として残す。

### なぜ宿題になるか

第41弾でカセットの後ろに「遊び方の確認」が挟まった時、
**後続が `waitScreen` の site は自動で通り抜け、`sleep` の後に分岐する site だけが落ちた。**
落ちた順に1つずつ直したので、いまは緑だが——
棚の流れに次に1段挟まった時、また同じことが起きる。
しかも「どこが落ちるか」は後続の書き方次第なので、事前に読み切れない。

手で複製した並びに、あとから前提が変わる形（落とし穴1・4）。

### やること

`tests/harness.js` に `openCassette(win, doc, id)` を1つ置き、
各ファイルのローカルな `toModeScreen` 等がそれを呼ぶ形に寄せる。
**53箇所すべてではなく、まずローカルの補助関数を寄せれば大半が片づく。**

寄せる先の候補（呼ばれている回数）：
`pickCart`（shelf 15回）・`toModeScreen`（playthrough/secrecy-gates/smoke 22回）・
`setupPlayers`（4ファイル 20回）・`openCassette`（play-way 5回）・
`gotoModeScreen`（smoke 4回）。
**この5本を寄せれば、53箇所の大半がその下に入る。**

### 完了条件

棚とカセットの間に画面を1つ足す変更を入れた時、
**直す場所が1箇所で済むこと。**

---

## 3. お気に入り（第41弾 2-6 を削った・2026-09-08）

### 何を外したか

指示41 2-6「お気に入り（最小）」——
タイルの隅に☆、押すと★になって0.4秒かけて先頭側へ滑る、というもの。

2-6 自身に**「指示が大きくなりすぎたら、ここは削る候補の筆頭。
削る場合は報告する」**と書かれている。その条項を使った。

### なぜ外したか

**1つは、指示41が実際に大きくなったこと。**
入口の作り直し・棚の1列化と輪・中央の帯・人数チップ・遊び方の確認・
役割の切り替えまでで、1つの指示に入る量を超えている。

**2つめは、2-2 と静かにぶつかること。**
2-2 は「固定順（棚の設計順）。**遊ぶたびに並びを変えない**（体の記憶を壊さない）」
と決めている。お気に入りは、まさに並びを変える機能——
2-6 は「お気に入りだけ先頭側へ」と書いているので矛盾ではないが、
**★を1つ付け外しするたびに棚の並びが変わる**のは事実で、
体の記憶という観点では同じ性質の揺れになる。

いまカセットは6枚で、**輪にしたので端が無く、どこからでも3回以内に届く**。
並べ替えて短縮できる距離が、そもそも小さい。

### 再開する時に確かめること

1. カセットが**10枚を超えているか**（6枚では効き目が薄い）
2. ★の付け外しで並びが変わることを、遊ぶ人がどう感じるか——
   2-2 の「体の記憶」と実際にぶつからないか、実機で確かめてから入れる
3. 入れるなら、置き場所は端末（未ログイン）とサーバー（ログイン）の両方。
   2-10 の秘密情報の表に「お気に入りは本人だけ」と既に書いてある

### 完了条件

**★を付けても、次に開いた時に「いつもの場所」が壊れていないこと。**
機能が動くことではなく、体の記憶が保たれることの方が条件。

---

## 4. 二つ名の「の」が重なる（第42弾で見つけた・2026-09-09）

### 何が起きているか

未ログインでも既定で付く二つ名が **「なつまつり**のの**一歩」** になる。

- 頭のパーツ `first-natsu` の札が **「なつまつりの」**（すでに「の」で終わる）
- つなぎのパーツ `joiner-no` が **「の」**

`TitleLogic.titleText()` は3つを素直につなぐだけなので、「の」が2つ並ぶ。
**下部バーに常に出ている文字**なので、目に入る回数が多い。

### なぜ外したか

指示42の6節が **`public/js/titles.js` の `CATALOG` を触ることを禁じている**。
札を「なつまつり」に直すのも、つなぎを増やすのも、どちらも `CATALOG` の変更になる。
42は「画面の作り直し」の指示で、パーツの中身は範囲外。

### やること（どちらか）

1. **札から末尾の「の」を落とす**（`first-natsu` を「なつまつり」に）。
   いちばん小さいが、`gotAt` に記録が残っている人の見え方が変わる
2. **つなぎ側で吸収する**——`titleText()` が
   「頭が『の』で終わっていたら、つなぎの『の』を出さない」。
   `CATALOG` を触らずに済むが、**言葉の規則をロジックに埋める**ことになる。
   同じ形（「〜と」＋「と」など）が増えた時に、そこも足す必要が出る

**1を推す。**2は落とし穴4（手書きの条件分岐に、新しい項目を登録し忘れる）に近い。

### 完了条件

`TitleLogic.titleText()` が返す文字列に、**同じ助詞が2つ続く組み合わせが1つも無い**こと。
1組を目で見るのではなく、**`CATALOG` の全組み合わせを回して機械で確かめる**
（落とし穴4・正本ループ）。新しいパーツを足した日に、同じ事故が赤で出るように。

---

## 5. 未ログインでも姿を選べるようにする（第42弾 門E8 で見つけた・2026-09-09）

### 何が起きているか

指示42 2-2 は「保存：ログイン時はアカウント、**未ログイン時は端末**」と書いている。
端末保存（`acac-look`）は第42弾で新設し、経路としては生きている。

**ところが、未ログインでは選べるものが各スロットにちょうど1つしかない。**

| スロット | 無印（最初から持っている）のパーツ |
|---|---|
| アイコン | `icon-default`（いつもの顔）だけ |
| はじめの言葉 | `first-hajime` だけ |
| つなぎ | `joiner-no` だけ |
| おわりの言葉 | `last-ippo` だけ |

つまり**端末に保存される値は、いつでも初期状態**。
シートは開くし、押せば保存も走るが、**姿は変わりようがない。**
遊ぶ人から見ると「変えられそうなのに、変えても何も起きない」——
落とし穴21（設定は出るのに、何も起きない）と同じ形になっている。

### なぜ外したか

増やすには `titles.js` の `CATALOG` に `free: true` のパーツを足すことになり、
**指示42の6節が `CATALOG` を触ることを禁じている。**
42は「画面の作り直し」の指示で、パーツの中身は範囲外。

### やること

1. **無印のパーツを、各スロットに2〜3個ずつ足す。**
   ログインしていない人が「自分の顔」を選べる状態にする
   （アプリの入口で名乗りを作れることには、集まりの中で意味がある）
2. 足したら、`tests/shelf.js`「未ログインでは、姿を変える選択肢が各スロット1つしかない」は
   **役目を終える**ので、「未ログインでも選び直せて、端末に残る」に書き換える
3. 無印を増やすと、**ログイン済みの人の「15 / 102」の分母も動く**。
   分母は `CATALOG` から導出しているので自動で追随するが、
   実ブラウザで数字が変わることを一度確かめる

### 完了条件

**未ログインで姿を選び直し、アプリを開き直しても、その姿で始まること。**
いまは「選び直せない」ので、この条件が試せない。

---

## 6. 爆弾解除の PREP（AIの説明文を作る段階）が、丸ごと死にコードになっている（第47弾の着手前に見つけた・2026-09-13）

### 何が起きているか

第32弾-A-3-6 で問題バンクに切り替わり、`bomb-room.js:158-164` は
**PREP を素通りする**ようになった。

```
  // 第32弾-A-3-6：問題は最初から揃っているので、待つ段階を通らずにそのまま始める。
  // PREP そのものは残してある（画面・記録が段階名を見ているため）が、
  // ここを通り過ぎるので、遊ぶ人からは見えない
  w.ready = w.wires.length;
  finishPrep(room, c);
```

そのため次の3つが取り残されている：

| 何 | どこ | 状態 |
|---|---|---|
| `prepare()`（AIに説明文を作らせる並列ワーカー） | `bomb-room.js:173-211` | **どこからも呼ばれない** |
| `PREP_RETRY` / `PREP_CONCURRENCY` / `w.aiError` | `bomb-room.js:37` ほか | prepare 専用なので一緒に死んでいる |
| 「🤖 AIが説明文を用意しています。」の文言 | `public/index.html:14869` | 段階に入らないので**遊ぶ人には見えない** |

**落とし穴33（表示している約束は、実装より長生きする）と同じ型。**
ただし33と違って、この文言は画面に出ないので実害は無い。

### なぜ外したか

指示47 は「大画面と演出」で、爆弾解除のサーバー側の掃除は範囲外。
**範囲を広げすぎない**という本人の裁定（2026-09-13）に従った。

### やること

1. `prepare()` と、それだけが使う定数・`aiError` を消す
2. `finishPrep()` の「AIの説明文を1本も作れなかったので、始められませんでした」も
   到達しなくなるので、あわせて見直す（`kept.length === 0` は
   問題バンクが空の時にしか起きない。文言をその意味に直す）
3. `public/index.html:14869` の prep 分岐の文言を、
   段階を残すなら実態に合わせて直す。段階ごと消すなら分岐も消す
4. 消す前に **`grep "AI"` を1回**かける（落とし穴33の恒久対策）

### 完了条件

`ai-describe.js` を参照しているのが**あれそれどれこれ側だけ**になり、
爆弾解除から「AI」という言葉が1つも出てこないこと。
`tests/mode-rules.js` の両方向照合が緑のままであること。

---

## 7. `scr-cassette` が、どこからも到達できない死んだ画面（第47弾の着手前に見つけた・2026-09-13）

### 何が起きているか

`public/index.html:3538` に `<div class="screen" id="scr-cassette">` があるが、
**`goTo('scr-cassette')` も `goBack('scr-cassette')` も1か所も無い。**

それでいて、この画面idは3つの手書きの一覧に載り続けている：

| 一覧 | 行 |
|---|---|
| `THEME_FREE_SCREENS`（テーマを当てない画面） | `public/index.html:9450` |
| `noGear`（浮いている⚙を出さない画面） | `public/index.html:9528` |
| （棚卸しの正本 `tests/inventory.js` にも載っている可能性・要確認） | — |

**落とし穴30の裏向き**：画面を消したのに規則が残るのではなく、
**画面が残っているのに誰も行かない。**
一覧に載っているので、`dead-style` にも `room-paths` にも捕まらない。

### なぜ外したか

指示47 の範囲外。消すと3つの一覧を同時に触ることになり、
**指示47-2 の「大画面の全画面に出口がある」の機械照合**と衝突しうる
（照合の対象一覧が動く）。47 が終わってからのほうが安全。

### やること

1. `scr-cassette` の中身が、いまどこかに引っ越し済みかを確かめる
   （`scr-shelf` の中央帯・`scr-game` と役目が重なっていないか）
2. 消す。あわせて `THEME_FREE_SCREENS`・`noGear`・`tests/inventory.js` から落とす
3. **消したら、その名前で `grep tests/` する**（落とし穴32）
4. 恒久対策：`tests/room-paths.js` に
   「実在する画面idは、すべてどこかから `goTo`／`goBack` されている」の**逆向き**を足す
   （いまは「検査が名指しする画面idが実在する」の片方向だけ・落とし穴20）

### 完了条件

死んだ画面が1枚も無いこと（逆向きの照合が緑）。

---

## 8. 大画面しか残っていない部屋の、自動の畳み方（第47弾 47-2 から切り出し・2026-09-13）

### 何が起きるか

47-2 で「**自動移譲の候補から大画面を除外する**」を入れた。
プレイヤーが1人も接続していない部屋では、`pickNextHost` が `null` を返し、
**進行役が一時的に不在になる。**

不在の間、その部屋は誰も閉じられない（`room:close` は進行役だけが呼べる）。
次に誰かが入れば `ensureHost(room, 'join')`（`realtime.js:1003`）が
その人を進行役にするので、詰みはしない。

### なぜ不在にする方を選んだか（本人の裁定 2026-09-13）

- 47-2 の禁止事項（**大画面はホストになれない**）を、例外なく守れる
- 大画面しか接続していない部屋は、そもそも**遊ぶ人が0人**の状態。実害が無い
- 例外を1つ作ると「プレイヤーがいない時だけ大画面がホスト」という
  2つ目の規則ができ、そこから漏れる（落とし穴4）

### やること（今回は実装しない）

**大画面だけの状態が一定時間（例：5分）続いたら、自動で部屋を閉じる安全弁。**

1. 既存の見回り（`realtime.js` の部屋ごとの巡回）に相乗りする。
   新しいタイマーを足さない（落とし穴7）
2. 判定は「接続中のプレイヤーが0人」の状態が続いた時間。
   `room.emptySince`（`realtime.js:797`）が近い概念だが、
   あれは**接続中メンバーが0人**なので、大画面が繋がっていると立たない。
   **別の印が要る**（例：`room.noPlayerSince`）
3. 閉じる時は、残っている大画面にも `room:closed` を届ける（黙って消さない）
4. 時間は運用メモ（`docs/運用メモ.md`）の再起動の扱いと足並みをそろえる

### 完了条件

大画面だけを繋いだまま5分放置すると部屋が消え、
大画面の画面が「部屋は終わりました」に変わること。
**プレイヤーが1人でも繋がっている間は、何時間でも消えないこと**（こちらが大事）。

---

## 9. すごろくの大画面で、駒がゴールに飛び込む（第47弾 47-6b から切り出し・2026-09-14）

### 何を外したか

指示47-6 の表の1行：

> すごろく：あがり｜駒がゴールに飛び込む→名前と順位→紙吹雪

このうち**「駒がゴールに飛び込む」だけ**を外した。
名前と順位（既存の決着表示）と紙吹雪は入っている。

### なぜ外したか（構造上の制約）

1. **駒に「誰のものか」の印が無い。**
   `sugoFillBoard`（`public/index.html:17322`〜）が描く `<b class="sugo-piece">` には
   id も `data-*` も無い。区別できるのは記号（●▲■…）と、
   マスの中の並び順が `v.players` / `v.groups` の添字順という**暗黙の規約**だけ
2. **盤は毎更新で作り直される。**
   `sugoFillBoard` は `host.innerHTML` の総入れ替えで、差分描画も「同じなら描かない」の門も無い。
   `room:update` は進行と無関係な出来事（誰かの切断・準備OK・アイコン変更）でも飛ぶので、
   掴んだ駒は数ms後に消えることがある
3. **駒はインライン要素**（`<b>`）なので `transform` がそのままでは効かず、
   親の `.sugo-pieces` に `overflow:hidden` が掛かっている（`public/index.html:2543`〜）。
   マスの外へ飛ばすには、駒そのものではなく**層（`#uiLayerRoot`）に飛ばす印**が要る

つまり「駒が飛ぶ」は、いまの作りの上では**駒を飛ばすのではなく、
駒に似せた印を層で飛ばす**ことになる。それ自体は `FxKit.fly` で書けるが、
**どの駒がどこから飛ぶか**を決めるのに、駒に印を足す作業が先に要る。

### やること

1. `sugoFillBoard` の駒に `data-pi="<添字>"` を足す（マスは既に `data-i` を持っている）
2. ゴールのマスは `#bigSugoBoard .sugo-cell[data-i="<v.cells>"]` で名指しできる
3. あがりの瞬間、**その人の直前の位置**が要る。
   いまの公開ビューには「前の位置」が無いので、**飛ばす元をどう決めるか**を先に決める
   （案：盤の外の名前の札から飛ばす／案：ゴールの1つ手前から飛ばす）
4. 「どこにいる？」（`sugohide`）は大画面に駒が1つも無い。
   **あがり＝決着**なので決着後は実位置が公開されるが、
   演出の作り方はゲームごとに分ける必要があるか確かめる（落とし穴22）

### 完了条件

5ゲームすべてで、あがりの瞬間に**ゴールのマスへ印が飛び込む**こと。
盤が作り直されても、飛んでいる印が消えないこと（層に出ているか）。
**本人の判断（2026-09-14）**：いまの紙吹雪とできごとの札で見せ場としては足りているので、
**優先度は低い**。

---

## 10. あれそれどれこれを大画面に対応させる（第47弾 47-7 から切り出し・2026-09-14）

### 何を外したか

指示47-7 まるごと。大画面に「残り時間（大きく）／今の出題者の名前／正解数／
『説明中』『相談中』などの場面／正解の瞬間のコールアウト」を出す話。

### なぜ外したか——**前提の形に、いまは到達できない**

指示はこう書いている：

> 手渡しのゲームなので、部屋の待合から「大画面をつなぐ」だけで成立する形（**1台の手渡し＋大画面**）

ところが、あれそれは**手渡し専用**（`RT_GAME_SCREENS` にも `GAME_DRIVERS` にも無い）。
そのため `playWayPlan`（`public/index.html:6023`）はこう返す：

```js
// 1台でしか遊べない。部屋があるなら閉じるか聞く（B）、無いならそのまま（A）
return room ? { kind:'leaveRoom', isHost: ... } : { kind:'through', way:'handoff' };
```

**部屋にいる人があれそれを始めようとすると「部屋を閉じて はじめる」になる。**
部屋が閉じれば、つないだ大画面も消える。ホストでもゲストでも同じ（「抜けて」に変わるだけ）。

つまり 47-7 は、**宿題1番（手渡しと部屋の並走を安全にする）に依存している。**
そして1番は、第33弾B-3 が実機の事故で塞いだ穴を開け直す作業なので、
本人の裁定（2026-09-07）でこう決まっている：

> **安全性が実装の完成度より優先される。**
> 「部屋を残す」を実装する前に、まず混ざりを直す。**順番を逆にしない。**

大画面側だけ先に作ると、**到達できない機能**になる（落とし穴21：設定は出るのに何も起きない）。

### やる順番（本人の裁定 2026-09-14）

1. **宿題1番の1〜3**（混ざりを直す／待っている人に「いま1台で遊んでいます」／戻ったら待合へ）
2. `WAY.leaveRoom` を「部屋を残す」形に書き換える
3. **それから** 47-7（あれそれの大画面）

### 先に入れてあるもの

**秘密の逆向きの見張り**だけは、第47弾のうちに入れた
（`tests/secrecy-gates.js`「47-7：あれそれのお題・封印語は、部屋の知らせに1文字も載らない」）。

- 部屋の画面（`RT_GAME_SCREENS`）と進行役（`GAME_DRIVERS`）が、**そろって在るかそろって無いか**
- 部屋の知らせを作る側が、お題・封印語の語を**1つも触っていない**

いま漏れていないことを見るだけでは、部屋対応にした日に静かに漏れる——
**照合には向きがある**（落とし穴20）。変異（`realtime.js` に `topicPool` を混ぜる）で
赤くなることも確かめてある。

### 完了条件

1台の手渡しであれそれを遊んでいる間、つないだ大画面に
残り時間・出題者・正解数・場面が出て、**お題と封印語が1文字も映らない**こと。

---

## 11. 「全員そろうまで始められない」を、サーバーの門にする（第48弾から切り出し・2026-09-14）

### 何を外したか

指示48 48-1 は「準備OKの門を通らずに開始できる経路が無いことを**機械照合**」と書いている。
照合そのものは範囲内だが、調べてみると**門がサーバーに無かった**。

`wolf:start`（`realtime.js:1157`付近）が見ているのは
「部屋にいるか」「ホストか」「もう始まっていないか」の3つだけで、
**`readyTally(room).all` を一度も見ていない。**
止めているのは端末の `rtStartBtn.disabled`（`public/index.html:11725`付近）だけ。

実測：誰も準備OKを押していない部屋でも `wolf:start` は `ok` を返す。

### なぜ外したか

1. **指示48が求めているものと違う。** 48-1 が言う「迂回する経路が無いこと」は
   端末側の話で、サーバーに門を足すのは別の設計判断（落とし穴14そのもの）
2. **`wolf:start` を直に呼ぶ検査が 118 か所ある。**
   門を足すと、そのすべてが「先に全員で準備OKを押す」形に書き換わる。
   指示48の実装に混ぜると、何が原因で赤くなったのか分からなくなる
3. 大切なこと2「1つずつ完璧にしてから次へ」

### 再開する時に確かめること

1. `wolf:start` に `readyTally(room).all` の門を足し、`not_ready` を返す
2. **切れている人は数に入らない**（`readyTally` はすでにそうなっている。
   `tests/room-ready.js`「切れている人は門番の数に入らない」が今も緑であること）
3. 118 か所を書き換える前に、`tests/room-edge.js` に
   `readyAll(rm)` のようなヘルパを1つ置く（同じ3行を118回書かない）
4. **大画面は押す相手に数えない**（これも `readyTally` がすでにそう）

### いま何が起きるかは、検査に書いてある

`tests/room-ready.js` の「切れている人は門番の数に入らない」の直前に、
**いま門が無いこと**を長いコメントで残してある。
「そろってから始まる」ことしか試していなかった既存の検査（落とし穴10-c）も、
そこで名指ししてある。

---

## 12. 48-2 の調査で出た、実装の穴 13件（第48弾から切り出し・2026-09-14）

帯（「いま何をする」）を書こうとして初めて分かったもの。
**本人の裁定（2026-09-14）で「遊べなくなる3件だけ今すぐ直す」**ことにしたので、
残りをここに置く。3件（すごろくの退室ボタン・人狼の夜の持ち時間・
ミニゲームの押した速さ）は第48弾で直した。

一覧と file:line は `docs/監査_指示48の門.md` の
「途中で見つかった、実装の穴」2つの表にある。要点だけ：

| # | 何が起きているか |
|---|---|
| 1 | せり上げ式の `highest` に id が無く、同名の人がいると「自分が最高額か」を判定できない（落札者側は 47-6b で `winnerId` を足した前例がある） |
| 2 | 早押しの `matchResult` も名前だけ。`bracket` は端末に1つも送っていないので、観戦者に「誰と誰が戦うか」を出せない |
| 3 | とくとく・早押しの `lastNote` が次の問題／対戦で消えず、前の結果が新しい画面に残ったまま配られる |
| 4 | とくとくクイズで、外した人の画面に「いま押せば◯点」が出たまま（同じ画面の上には「もう答えられません」と出ている） |
| 5 | 大画面のすごろく（こまはひとつ）に、クイズの問題文が出ない（送っているのに読んでいない） |
| 6 | 早押しの大画面に `break` の分岐が1つも無く、決勝の直後の6秒間、終わったペアが出たまま |
| 7 | ふたりでひとつ（部屋）に、自分の組を光らせる印が無い（手渡しには専用の組発表画面がある・落とし穴1） |
| 8 | どこにいる？（部屋）の `result` に何が起きたかが1つも出ず、`ended` でも明かされた実位置が盤にも一覧にも出ない |
| 9 | オークションの半額チケット／相場操作に、使う窓口が画面に1つも無い（落とし穴21） |
| 10 | オークションで途中参加した人は描画が早期returnするのに、「アイテムをえらんでください」と出る（押しても弾かれる） |
| 11 | 早押しの読み上げが、対戦していない端末でも鳴る（全員の端末が同時にしゃべる）※実機未確認 |
| 12 | つうこうりょうで払った通行料が誰にも渡らない（減るだけ）。**ルールとしてこれで正しいかの判断が要る** |
| 13 | 手渡しのクイズ解除・早押し・すごろくの連打は、設定を開いても止まらない（⏸の対象が2つしかない） |

### 再開する時に確かめること

- **1・2・3 は帯の前提**。48-2 の帯で「あなたが最高額です」「勝ったのはあなた」
  「次は◯◯と◯◯」を書くには、先にサーバーが id と bracket を送る必要がある
- **12 はルールの話**なので、実装の前に本人の判断を仰ぐ
