# 指示41「入口と棚の作り直し」完成の門

最終更新：2026-09-08

**この表は、埋まっていない行を消さない。**
「未検証」と書いてある行があるうちは push しない（大切なこと8）。

---

## D1〜D21

| # | 確認項目 | 状態 | 証拠 |
|---|---|---|---|
| D1 | jsdom 全スイート緑（クリーン1回） | ✅ | 何も並走させず1回。件数は下の「最後に回した記録」 |
| D2 | 新規テストは実装を壊すと狙った1件だけ赤 | ✅ | 変異一覧は下 |
| D3 | 検出網 全緑（画面台帳・`CASSETTES`・分類） | ✅ | `room-paths` 25/25。**正本ループが自分で2件捕まえた**（経路の担当割り当て・`scr-room-open` の台帳行） |
| D4 | 各カセットが棚に1回だけ。準備中は畳まれている | ✅ | `shelf-scroll`「準備中のカセットは、最後尾に1枚だけ畳まれている」。棚の `data-cart` 一覧＝`INV.READY_CASSETTE_IDS` と件数照合（両向き） |
| D5 | 入口→ログイン→（部屋確認）→チップ→棚→はじめる→確認→ゲーム選択 を実サーバー＋実ブラウザで通し | ✅ | `/dev-login` で kensho34 のセッションを張り、扉→入口→棚→ⓘ→チップ→はじめる→確認→`scr-game` まで通した |
| D6 | 「部屋に入る」→コード→待合→指示37のルール→準備OK、を実サーバーで通し | ✅ | `tools/room-bots.js` で bot 3体がコードで入室 → 待合（4人）→ `scr-rt-rules` → 準備OK → `scr-rt-play`。**全員に✓**（botのログにも「準備OK（wordwolf）」×3） |
| D7 | 見るだけの棚で、はじめるが押せず理由が出る。戻るが正確に戻る | ✅ | 実ブラウザ：`swStartBtn.disabled=true`／理由「ログインすると始められます」／次の行動ボタンあり |
| D8 | 部屋を持ったまま開き直すと「いま開いている部屋」に来る | ✅ | **実サーバーで開き直して確認**：「いま開いている部屋 コード：ZGAU97 参加者：1人」→「この部屋にもどる」で待合へ。ここで2件のバグが出た（下） |
| D9 | 遊び方の確認3通りが正しい文面で出て、1タップで通る | ✅ | C・E は実ブラウザで文面ごと確認。B（部屋あり）は jsdom（`rt-screens` B-3・`play-way` 8/8）。F（両方対応×部屋あり）の素通りも実サーバーで確認 |
| D10 | 部屋あり＋1台専用で、他の人の画面が待ちになる | **対象外**（切り出し済み） | 本人の裁定で「部屋を閉じてから進む」に確定。`docs/切り出した宿題.md` 1番 |
| D11 | カルーセル：スナップ・一周・位置の目印・全タイルに人数時間 | ✅ | 実ブラウザ：1列・7枚・点7・矢印は無効にならない・全札に人数と時間。7回押して最初に戻り、**中央のずれは8回とも0px** |
| D12 | 中央帯の切り替えが止まった時だけ。上の帯と下部バーが全テーマでクリーム | ✅ | 6テーマの色を実測。**時計ではなく作りで守っている**（送っている間は `_index` が動かない） |
| D13 | 開いた時に前回遊んだカセットが選ばれている。「前回」の札 | ✅（jsdom） | `shelf.js`「開き直すと、前回あそんだカセットが選ばれている」。札は1枚・帯もその世界から始まる |
| D14 | 押せないはじめるの理由が、原因ごとに正しく出る | ✅ | 実ブラウザ：「あと1人 足りません（いま2人・3人から）」／「ログインすると始められます」 |
| D15 | 長押しとⓘで同じ popup。本文だけスクロール、ヘッダ固定 | ✅ | 実ブラウザで長押し・ⓘの両方から同じ popup。ヘッダ固定は `ui-kit` が見張る。**ⓘは帯に1つ**（本人の裁定。タイルには置かない） |
| D16 | 人数チップ：沈める（消さない）、部屋ができたら実数に置換 | ✅ | 実ブラウザ：2人で人狼・オークション・すごろくが沈み、6人で全部戻る。実数への置換は `shelf.js`（疑似socketで6人の部屋） |
| D17 | 横スクロールのバグ：修正前に再現、修正後に通し、テストが赤→緑 | ✅ | 修正前：余地396pxに対し0px・印だけ3つ進む。修正後：0→-132→-264→-396・ずれ0px。`shelf-scroll` 2件赤 → 7件緑 |
| D18 | 達成状況ボタンが自分のぶんだけ。未ログインで「ログインすると記録されます」 | ✅ | 帯に🏆ボタン。行き先は既存の称号画面（0節「行き先は既存の画面のまま」） |
| D19 | お気に入り：★で先頭へ動く（削った場合は理由を報告） | ✅（削った） | 2-6 の条項を使った。理由は `docs/切り出した宿題.md` 3番 |
| D20 | 実機でしか分からないこと | **未検証と明記** | 下 |
| D21 | 待合で「自分がゲストになる」→danger→全員退出、元ホストも「部屋に入る」に着地、ゲスト側に通知 | ✅ | **実サーバー＋bot**：danger の文面が裁定どおり →「部屋に入る」に着地・部屋は残らない → **bot 3体すべてに「部屋が閉じました（あき さん）」** |

---

## 実サーバーで通して、初めて出た4件

門の表を「jsdom で緑」だけで埋めていたら、どれも見つからなかった。

| 何 | どう壊れていたか |
|---|---|
| 帯の「はじめる」 | 2-4 の確認を飛ばしていた（タイルは通るのに）。落とし穴1 |
| 人数チップ | 変えると、見ていたカセットを見失って先頭に戻る |
| 部屋の記憶 | 端末が部屋を1つも覚えておらず、開き直すと棚へ素通り（D8） |
| つながる前の問い合わせ | `connect()` の直後に聞くと**15msで「つながっていません」と即答**し、部屋があるのに「無い」と判断していた |

**3つめは、jsdom の検査が緑のまま見逃していた。**
同じセッションのまま入口へ戻って確認画面が出ることだけを見ていて、
**開き直していなかった**——2-12 が言っているのはそこなのに。
harness に `storage` を足して、開き直す検査に書き直した。

---

## 道具が最初からあった（記録）

前回この表は D5・D6・D21 を「未検証」とし、理由を
「アカウントを作れない・bot が無い」と書いた。**どちらも誤りだった。**

- `tools/dev-server.js` に第35弾で作られた `/dev-login` があった
  （パスワード不要・アカウント作成も不要）。
  前回は `tools/` の中身を**列挙しただけで開かなかった**
- bot は第37弾・第38弾でも書いたが、リポジトリに残さなかったので消えていた。
  今回 `tools/room-bots.js` としてコミットした

CLAUDE.md に道具の場所を1行、落とし穴29として
「セッションの中で見つけた道具は、リポジトリに書かないと次のセッションで消える」を足した。

---

## D20：実機でしか分からないこと（未検証と明記）

| 何 | なぜ確かめられていないか |
|---|---|
| スワイプの手触り（1枚ぶんで止まる指の感じ） | 実機の指でしか分からない |
| 継ぎ目の1フレーム（席を貼り替える瞬間が見えないか） | ブラウザ枠の描画が止まる環境では測れない。**矢印10回連打と速い3連スワイプを、実機の目で見る必要がある** |
| 振動（`tick`）の強さ | iOS Safari では鳴らない前提。鳴らなくても成立する形にはしてある |
| 帯の色が0.3秒でとけ替わる体感 | 遷移の途中の絵が、描画の止まる環境では進まない |
| 375×667・文字サイズ1.30 で「はじめる」が折り返しの下に沈まないか | **未測定。**ヘッダ＋札＋点＋帯＋下部バーの高さの合計を、実機で1度見る必要がある |

---

## D2：変異で赤くなることを確かめた一覧

| 変異 | 赤くなった検査 |
|---|---|
| CSSの切り出しの穴を戻す | press-feedback「@media の中の1件目も拾う」 |
| 重なりが開いている時のキーの見張りを外す | ui-kit「画面ぜんたいのキー操作は…」／shelf「説明を開いている間、キーで棚が動かない」 |
| 帯を動かすのをやめる（印だけ進む＝元のバグ） | shelf-scroll 2件 |
| 吸い付きを切り捨てにする | shelf-scroll「指で送ると…」 |
| 指の揺れの見きり（8px）を外す | 同上 |
| レールをスクロールの容れ物に戻す | shelf-scroll 2件 |
| 端の止めを外す | shelf-scroll「矢印で送っても…」 |
| 縦も切る形に戻す | shelf-scroll「棚のレールは…」 |
| 席を配り直さない | shelf-scroll 2件 |
| 座り直しで帯を戻さない | shelf-scroll 3件 |
| 輪をやめて端で止める | shelf-scroll「矢印で送っても…」 |
| 休みの位置のずらしを外す | shelf-scroll 3件 |
| 準備中を畳まない | shelf-scroll「準備中のカセットは…」 |
| 位置の目印を出さない | shelf-scroll「位置の目印が…」 |
| 人狼の帯の文字を暗くする | design-tokens「棚の中央の帯も、6つの世界すべてで読める」（1.05 を捕まえた） |

---

## 作らなかったもの（報告）

- **お気に入り**（2-6）：`docs/切り出した宿題.md` 3番に理由を書いた
- **タイルの隅の「i」**（2-7）：本人の裁定で、帯の名前の横に「くわしく ⓘ」を1つ置く形にした。
  7枚の小さなタイルに常時「i」を出すと、人数と時間と場所を取り合う。
  長押しは知っている人の近道、ⓘは知らない人の入口

---

## 最後に回した記録

2026-09-08・何も並走させないクリーンな1回：

    48スイート ／ 1161 / 1161 件 ／ exit 0 ／ 赤 0件

D20 の5項目だけが「未検証」で、これは**実機でしか分からないもの**として
指示41 D20 が最初から「未検証と明記」を求めている行。
それ以外の D1〜D19・D21 は埋まっている（D10 は本人の裁定で対象外）。
