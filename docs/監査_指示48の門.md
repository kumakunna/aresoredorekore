# 監査：指示48「進行と分かりやすさ」の門

指示48 4節の K1〜K14。**証拠の無い行は「未検証」と書く**（大切なこと8）。

**進み具合**：着手前調査のうち **48-1（原因特定）と 48-6（クラス表）は完了**。
48-2（全場面の表）は 3カセット中 1.5カセット分。48-3〜48-8 は下調べの途中。
実装はまだ1行も入れていない。

---

## 着手前に分かった、指示の前提と違ったこと

指示48は5節の表で前提を9つ挙げている。**そのうち1つが、現物と違った。**

| 指示の前提 | 現物 | どこ |
|---|---|---|
| 「**47-6で、部屋・大画面にも `bomb-danger` が付くようになった**（`v.phase==='ended'` で外れる）」 | **半分ちがう。付いたのは大画面だけ。** `14778` は `rtBigFx()` の中にあり、`rtBigFx` を呼ぶのは `renderRtBig`（`14937`）ただ1か所。つまり**部屋のスマホ（`scr-rt-bomb`）には、赤い縁がそもそも付かない**。実測でも部屋の端末は最後まで `bomb-danger` なし（下の表）。**手渡しにはあって部屋には無い＝落とし穴1** | `public/index.html:14726`・`14778`・`14937` |
| さらに「`v.phase==='ended'` で外れる」も**成り立たない** | `renderRtBig` は待合（`!v.phase \|\| v.phase==='lobby'`）で **`rtBigFx` の手前で return する**（`14907`〜`14929`）。強制終了は `clearGameState` で `data={}` にするので、`phase` は `ended` を**通らずに**消える。**外す処理に一度も到達しない** | `public/index.html:14907` |

残る7つ（`rtNeedsRules` の4条件・`rtRulesSeen` がメモリ・`state.seenRules` が永続・
`pickUnused` の黙った再抽選・手渡しの `bomb-danger` が `stopAllPlayTimers` で外れない・
`SET_QUICK_ITEMS`・47で入った芯・大画面はホストになれない・`/api/auth/name` が
`users.username` を書き換える）は、**現物どおり**だった。

---

## 48-1：ルール・準備OKの画面が出ない時がある（原因の特定）

### 結論：原因は1つではなく、**独立した3つ**。どれも「ランダム」に見える

指示は「(a) ready が残る／(b) 放送の順序／(c) 2回目／(d) 手渡しの永続化」を候補に
挙げていたが、**実際に効いていたのは (d) と、候補になかった2つ**だった。

#### 原因①（本命）：「読んだか」を数える鍵が、**経路によって変わる**

鍵は `rtRulesKey()` ＝ `モードid ?? ゲームid`（`public/index.html:11304`）。
そのモードidは部屋の `state.data.modeId` に載っている。

ところが `clearGameState()` は **`room.state.data = {}`** にする（`realtime.js:76`）。
`pickGame` を呼ぶ5か所のうち、**`data:{modeId}` を渡しているのは
`backToRoomWithGame`（`11266`）ただ1つ**。再戦（`21710`）・強制終了（`22417`）・
えらび直し（`21716`）は渡していない。

**本物のサーバーで実測**（`tests/room-edge.js` の `startTestServer` で `realtime.js` を起動）：

| 遊ぶ人の操作 | 部屋に載る `data.modeId` | 「読んだか」の鍵 |
|---|---|---|
| 棚 → 協力版をえらぶ | `bomb-coop` | **`bomb-coop`** |
| 二人とも準備OK → はじまる | （消える） | `bomb` |
| **「もう一度」（再戦）のあと** | **無し** | **`bomb`** |
| **「みんなを待合にもどす」のあと** | **無し** | **`bomb`** |
| 競争版にえらび直した | `bomb-race` | `bomb-race` |
| また協力版をえらんだ | `bomb-coop` | **`bomb-coop`** |

同じ「クイズ解除・協力版」を遊んでいるのに、**記憶が `bomb-coop` と `bomb` の
2つに割れている。** どちらが使われるかは「どの道でそのゲームに来たか」で決まり、
遊ぶ人の画面には何の違いも出ない。

- **出すぎる向き**：再戦すると鍵が `bomb` に変わるので、
  2周目なのに全員がルール画面へ引き戻される（設計は「2周目には出さない」）
- **出ない向き**：いちど `bomb` で準備OKを押すと、以後その部屋で
  `bomb` を鍵にする経路（再戦・強制終了あと）ではルールが二度と出ない

#### 原因②：部屋の画面の外にいる人は、**引っぱられない**

`rtSync` は `RT_SCREENS.indexOf(cur) === -1` で早く帰る（`13024`）。
戻ってきても `enterRtRoom()` は描き直すだけで、いるべき画面を計算し直さない（`11478`）。

**jsdom で実測**（対照実験つき。落とし穴10-b）：

| | 待合にいたまま選ばれた | ⚙→アプリの設定→アイコン・二つ名（`scr-titles`）に出ている間に選ばれた |
|---|---|---|
| ホストが選んだ直後 | `scr-rt-rules`（出た） | `scr-titles`（動かない） |
| 自分で待合にもどったあと | — | **`scr-rt-room`（ルールが出ないまま）** |
| 待合の「準備OK」 | — | **押せる（＝読まずに押せてしまう）** |

`scr-titles` は待合から⚙で普通に行ける画面。**遊ぶ人には「自分の二つ名を見ていたら
ルールが出なかった」としか見えない。**

#### 原因③：進行役の初回ルールは、**端末に永久保存**されている

`finishWizard()` が見ているのは `state.seenRules[modeId]`（`10809`）。これは
`localStorage['aresoredorekore-prefs']` に入って**永久に残る**
（`8164` 初期化・`8440` 読み込み・`8451` 保存・`17014` 立てる）。

つまり **その端末で一度でも遊んだモードは、別の日・別の部屋でも二度と説明が出ない。**
ゲスト側は `rtRulesSeen`（メモリ・部屋を出たら忘れる、`11292`）なので、
**同じ部屋の中でホストとゲストで寿命が違う**（落とし穴1）。
45-3 でルール文を書き換えた時、既読の人が新しい文を一度も見ないまま遊んだのはこれ。

#### 原因④（設計上の穴）：「キャンセル（あとで押す）」が既読の印を立てる

`11605`：進行役でない人が「キャンセル」を押すと `rtRulesSeen[key] = true`。
一度でも後回しにすると、その部屋ではもう説明画面に引き戻されない。

### 「ランダム」の正体

**4つとも、遊ぶ人の画面には手がかりが出ない。**
①は「どの道で来たか」、②は「その瞬間どこを見ていたか」、③は「その端末の過去」、
④は「前に一度押したかどうか」で決まる。どれも本人には見えないので、
「出る時と出ない時がある」としか感じられない。

---

## 48-6：ゲーム固有クラスの表（`#app` / `:root`）

掃引は2方向（落とし穴20）——JS の `classList` 操作から、と CSS の `.app.◯` / `:root.◯` 規則から。

| クラス | 付く先 | 付ける | 外す | 強制終了 | 退室 | 解散 | 開き直し | 判定 |
|---|---|---|---|---|---|---|---|---|
| `theme-wolf` / `-bomb` / `-quiz` / `-auction` / `-sugoroku` | `#app` | `applyGameTheme` `9638` | 同じ関数（**当てるもの以外は必ず外す**） | ○ | ○ | ○ | ○ | 安全（中央集約＋除外一覧） |
| `phase-night` / `phase-day` | `#app` | `setWolfPhaseTone` `9645`-`9646` | `applyGameTheme` `9640` | ○ | ○ | ○ | ○ | 安全 |
| `edge-village` / `edge-wolf` / `edge-third` | `#app` | `flashRoleEdge` `19115` | `19113` ＋ `setTimeout` `19116` | ○ | ○ | ○ | ○ | 安全（自己回収） |
| **`bomb-danger`** | `#app` | 手渡し `20040` ／ **大画面 `14778`** | 手渡し `20135`・`20152` **のみ** | **×** | **×** | **×** | ○ | **漏れる（下記）** |
| `has-float-bottom` | `#app` | `syncFloatBottom` `16763` | 同じ toggle | ○ | ○ | ○ | ○ | 安全 |
| `season-◯`（`TitleLogic.SEASONS`） | `#app` | `applySeason` `23001` | `22995`（全部外してから付ける） | ○ | ○ | ○ | ○ | 安全（日付で決まる・ゲーム固有ではない） |
| `big-screen` | `:root` | `applyGameTheme` `9631` | 同じ toggle | ○ | ○ | ○ | ○ | 安全 |
| `fs-85`〜`fs-130` / `fs-small` / `fs-large` | `:root` | `applyAppPrefs` `8769`-`8772` | `8768` が正規表現で全消し | — | — | — | ○ | 設定（ゲーム固有ではない）。ただし `catalogSet` `14020`-`14021` が別経路で触る |
| `fx-skip` / `no-flash` / `no-shake` | `:root` | `applyAppPrefs` `8781`・`8787`・`8788` | 同じ toggle | — | — | — | ○ | 設定 |
| `--font-scale` / `--screen-bright` / `--fx-scale` / `data-timerView` | style・dataset | `applyAppPrefs` `8759`・`8775`・`8780`・`8791` | 同じ | — | — | — | ○ | 設定（クラスではない状態。落とし穴30の型で別に掃いた） |

**ゲーム固有で外し漏れがあるのは `bomb-danger` ただ1つ。** ただし**3面それぞれで壊れ方が違う**。

### 実測（本物の進行役 `bomb-room.js` を動かして、本物の `publicView` を画面へ流した）

**大画面**

| 場面 | 画面 | `bomb-danger` |
|---|---|---|
| はじめ | `scr-rt-big` | なし |
| ミス1（ライフ2） | `scr-rt-big` | なし |
| ミス2（ライフ1） | `scr-rt-big` | **★あり**（正しい） |
| 強制終了 → 待合 | `scr-rt-big` | **★あり（残る）** |
| さらに人狼にえらび直した | `scr-rt-big` | **★あり（残る）** |

最後の `#app` の class は `app bomb-danger theme-wolf`。
**テレビが、人狼をえらんでいる待合の画面で赤く脈打ち続ける。**

**手渡し**

| 場面 | 画面 | ライフ | `bomb-danger` |
|---|---|---|---|
| はじめ | `scr-bomb-play` | ❤️❤️❤️ | なし |
| まちがえ1 | `scr-bomb-play` | ❤️❤️🖤 | なし |
| まちがえ2 | `scr-bomb-play` | ❤️🖤🖤 | **★あり**（正しい） |
| ⚙→「ゲームを終了する」 | **`scr-shelf`** | ❤️🖤🖤 | **★あり（残る）** |

最後の `#app` の class は `app bomb-danger`。**棚が赤く脈打つ。**

**部屋（スマホ）**

| 場面 | 画面 | `bomb-danger` |
|---|---|---|
| ミス2（ライフ1） | `scr-rt-bomb` | **なし（そもそも付かない）** |

### 「終わる」経路と、いま何を片付けているか

| 経路 | 入口 | いま通る後片付け |
|---|---|---|
| 手渡し・強制終了 | ⚙→「ゲームを終了する」`actEndGame` `22429`〜 | `goTo('scr-shelf')` → `stopAllPlayTimers()` `9787` |
| 手渡し・決着 | `defuseComplete` `20129` / `explode` `20145` | それぞれが `bomb-danger` を外す（**ここだけ正しい**） |
| 部屋・強制終了 | ⚙→「みんなを待合にもどす」`actEndGame` `22417` | `rt.pickGame(gameId,{reset:true})` → サーバー `clearGameState` |
| 部屋・退室 | `rtLeaveRoom` `11867` | `rtForgetRulesSeen` → `rt.leave()` → `goBack('scr-shelf')` |
| 部屋・解散 | `room:close` | （端末側は `room:closed` を受けて棚へ） |
| 部屋・再戦 | `doNextChoiceInRoom('again')` `21710` | `rt.pickGame(gameId,{reset:true})` |
| 開き直し | ページ再読み込み | クラスは消える（`localStorage` からは復元されない） |

`stopAllPlayTimers()`（`8900`〜`8915`）が止めているのは**時計と音と演出の順番待ちだけ**で、
**見た目のクラスは1つも外していない。** ここが 48-6 の「1か所に集める」置き場として自然。

---

## 48-2：3カセットの全場面（進行中）

| カセット | 状態 |
|---|---|
| クイズラッシュ・つぎつぎクイズ | 28場面ぶん取得済み |
| てふだ＋すごろく共通 | 62場面ぶん取得済み |
| 相場オークション | **未** |
| つうこうりょう・こまはひとつ | **未** |
| ふたりでひとつ・どこにいる？ | **未** |
| とくとくクイズ・早押しトーナメント | **未** |

すでに見つかっている「1行で書けない／書くと嘘になる」場面：

| ゲーム | 場面 | 何が問題か |
|---|---|---|
| てふだ（大画面） | `offer` | 大画面の主文は `v.turn` から「◯◯さんの番」と書くが、`offer` で動くのは**手番の人以外**。帯を足すなら主語を変える必要がある（`12649`-`12652`） |
| てふだ（大画面） | `ended` | 大画面の一覧は「あと◯／🪙／札◯」だけで**順位の列が無い**。「順位を見ます」と書くと画面に無いものを指す（`12684`-`12691`） |

---

## K1〜K14 の状態

| # | 確認項目 | 状態 |
|---|---|---|
| K1 | 全スイート緑（ランナーの3点） | **着手前の基準値のみ**：54/54 スイート・1287/1287 件・終了コード0（1052秒）。実装後は未検証 |
| K2 | 変異で狙った1件だけ赤 | 未検証 |
| K3 | 検出網 全緑 | 未検証 |
| K4 | 48-1：原因の特定結果／4経路の表 | **原因特定は完了**（上記①〜④・実サーバーと jsdom の実測つき）。4経路の表は実装後に取り直す |
| K5 | 48-1：準備OKを通らない開始経路が0 | 未検証 |
| K6 | 48-2：3カセットの全場面に一行 | 未検証（材料が 1.5/3 カセット） |
| K7 | 48-3：端子の3状態・初回の吹き出し | 未検証 |
| K8 | 48-4：脱落者が結果に着地 | 未検証 |
| K9 | 48-5：ポーズ | 未検証 |
| K10 | 48-6：終了後にゲーム固有クラスが残らない（3面） | **いまは残る**ことを実測で確認（上記）。直したあとは未検証 |
| K11 | 48-7：一周の通知／50回で50件 | 未検証（実データが50件ちょうどであることだけ確認：easy 18 / normal 14 / hard 8 / nanisore 5 / muri 5） |
| K12 | 48-8：表示名とログインID | 未検証 |
| K13 | 表示している約束は本当か | 未検証 |
| K14 | 実機 | **未検証**と明記 |
