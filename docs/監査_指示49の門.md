# 監査：指示49の門（見た目と日本語・2026-09-16）

指示49（2026-09-16 更新版）の「完成の門 L1〜L12」に対する証拠。
**「未検証」は未検証と書く。**証拠の無い行に丸を付けない。

---

## 着手前に分かった、指示の前提とのズレ

指示を実装に当てる前に現物を測ったところ、**4か所で前提が違っていた**。
どれも「指示が間違い」ではなく、**測っていなかったから分からなかった**もの。

| # | 指示の前提 | 実測 |
|---|---|---|
| 49-1 | 中央帯の**説明文**（人数・時間の下の1〜2行）が見にくい | `.sw-desc` は**元から 12.21〜16.12 で AAA**。読みにくかったのは `--warp-soft` を使う**小さい行**（人数・時間 11.5px／下の案内 10.5px／タイルの人数 9.5px）で、共通5.67・すごろく6.43・人狼6.87 |
| 49-3 | 7色の総当たり**21組** | いまは**15組**。あれそれと共通が**同じ値**（ΔE 0.0）なので、色は6つしかなかった。21組になるのは 49-4 の後 |
| 49-4 | あれそれに**世界の色**を持たせる | 世界の色だけでは足りない。幕が広がる色＝そのカセットの地、という約束があるので、**配色（.app.theme-aresore）も要る** |
| 49-6 | タイル名の**文字幅・サイズ・字送り**を調整 | 原因は幅でもサイズでもなく、`title:'クイズ<br>王'` と**データに改行が書いてあった**。使える幅96pxに対し最長72pxで、**全部1行に入る** |

---

## L1 全スイート緑（ランナーの3点）

- **証拠**：本文末尾の「クリーンな1回」の節に貼る
- 途中の1回（配色を入れた直後）は **赤5件**で、内訳は
  **本物の退行1件＋値を名指ししていた検査4件**。両方ともこの指示の中で直した（下の L1-b）

### L1-b 途中で出た赤5件の正体

| 赤 | 正体 |
|---|---|
| rt-screens：部屋コードで参加した人にも、人狼のテーマが当たる | **本物の退行**（下の「49-4が掘り当てた穴」） |
| rt-screens：47-5 夜と朝の配色が切り替わる | 同上（同じ根） |
| rt-screens：47-5 朝夜を残さない | 同上（同じ根） |
| smoke：競り市が追加専用のスコープで書かれている | `--paper:#1A1012` と**値を名指し**（落とし穴10-d） |
| smoke：スタジオの見た目が追加専用のスコープで書かれている | `--paper:#10182E` と**値を名指し**（同上） |

---

## L4 49-1：説明が7色で 7:1 以上

**実サーバー（`node tools/dev-server.js`）で、遷移を切ってから測った。**
遷移を切らずに測ると、枠が裏に回っている時に**途中の色**が返る（落とし穴47）。
宣言値（`--warp-color`）と computed の背景色が一致することを、7色すべてで先に確かめてある。

| 世界 | 地 | 人数・時間 11.5px | 下の案内 10.5px | タイルの人数 9.5px | 説明の行 13px |
|---|---|---|---|---|---|
| 共通 | `#EFE8D3` | **7.87** | 7.87 | 7.87 | 12.21 |
| あれそれ | `#FBD9BC` | **7.23** | 7.23 | 7.23 | 10.63 |
| 人狼 | `#261A55` | **8.24** | 8.24 | 8.24 | 13.06 |
| 爆弾 | `#17181B` | **8.02** | 8.02 | 8.02 | 14.73 |
| クイズ | `#04365F` | **7.42** | 7.42 | 7.42 | 11.51 |
| オク | `#3B1220` | **9.48** | 9.48 | 9.48 | 14.37 |
| すごろく | `#D9ECCC` | **7.72** | 7.72 | 7.72 | 12.79 |

**7色すべて 7:1 以上。**（着手前は 共通5.67・すごろく6.43・人狼6.87 が未達）

---

## L5 49-2：全画面×3サイズで主ボタン・主要情報が初期表示に入る

### 直したのは1行。**作りは最初から正しかったのに、効いていなかった。**

`.wiz-body` は `flex:1; overflow-y:auto`＝「余った高さのぶんだけ出して、あふれたら巻く」
つもりで書いてあり、`.wiz-foot` も `margin-top:auto` で下に貼り付くつもりだった。

効かなかった理由は `.screen` が **`min-height` しか持っていなかった**こと。
下限しか無いので中身が増えれば画面はいくらでも伸び、
伸びれば `flex:1` は「余った高さ」を計算できず、`overflow-y:auto` は一度も働かない。
**本文が伸びきって、その下の「つぎへ ▶」が折り返しの外へ押し出されていた。**

    .screen:has(> .wiz-body){max-height:100dvh;}
    .wiz-body{ …… min-height:0; }   ← 縦並びの flex では、これが無いと overflow が負ける

**余白は1pxも削っていない。押す的も縮めていない**（49-2の禁止）。

### 結果（実サーバー・実ブラウザ）

| 条件 | 測れた画面 | はみ出し | **主ボタンが折り返しの外** |
|---|---|---|---|
| 375×667 | 68 | **0** | **0** |
| 375×667 ＋ 文字サイズ「大」 | 68 | **0** | **0**（主ボタンのある45画面すべて） |
| 375×812 | 68 | **0** | **0** |
| 1280×720 | 66 | `scr-door` +28 のみ | **0** |

着手前（同じ測り方）：

| 画面 | 375×667 | 375×812 | 1280×720 | 667＋大 |
|---|---|---|---|---|
| `scr-set-wolfrole` | +170・**主ボタン外** | +25・**外** | +96・**外** | +296・**外** |
| `scr-mode` | +169・**主ボタン外** | +24 | +116・**外** | +194・**外** |
| `scr-titles` | +660 | +515 | +572 | +680 |
| `scr-catalog` | +621 | +476 | +476 | +798 |
| `scr-rt-lobby` | +111 | 0 | +16 | +170 |
| `scr-rt-room` | 0 | 0 | 0 | +2 |

長い一覧（`scr-titles`・`scr-catalog`）も0になった——
どちらも `.wiz-body` を持っていたので、同じ1行で本文の中を巻くようになった。

### 巻けることが分かる印（49-2）

`.wiz-body` の上下に、**中身が続いている側にだけ**影を出す
（`background-attachment` の `local` と `scroll` を重ねる定番の形）。
いちばん上／下まで巻くと、その側の影は自然に消える。
JSで測って class を付け替える形にはしていない——
**出す側に条件を書かせない**（良い型4）ので、あとで足した画面にも既定で効く。

### 残した1件：`scr-door` +28（1280×720）

**直していない。理由を書く。**

- 玄関の扉は `matrix3d` でY軸回転している（開く演出）。
  `getBoundingClientRect` は回った後の外接箱を返すので、
  指定高さ 532.8px に対して実測 748px になる
- `.door-scene` は `overflow:visible` なので、**本当に28pxはみ出している**
- ただし **この画面に主ボタンは無い**（どこを押しても開く）。
  はみ出しているのは開き終わった扉の板で、すぐ次の画面へ移る
- **指示49より前から出ている**（着手前の1回目の計測にも `scr-door | 748 | 28` と出ている）
- `.door-scene` に `overflow:hidden` を足せば消えるが、
  それは**アプリの第一印象の演出を切る**変更で、
  この指示（見た目と日本語）の中で黙って入れるものではないと判断した

### 測れなかった9画面（**未検証**）

`scr-play-way`・`scr-cassette` は状態が無いと棚へ戻される。
`scr-wolf-pass`・`scr-wolf-result`・`scr-wr-pass`・`scr-wr-day`・`scr-quiz-turn`・
`scr-auction-bid`・`scr-auction-question` は進行の状態が無いと例外を投げる。
**これらは測っていない。**`tools/room-bots.js` で本物の状態を作れば測れるが、この指示ではやっていない。

### 測り方そのものの検査（落とし穴28）

- **巻ける箱の中身は「はみ出し」に数えない。**数えると、本文を巻く形にした瞬間に
  「+103」のような嘘の数字が出る（実際に一度出した）
- **わざと900pxの箱を足して、0 → 322 → 0 に動くことを確かめてから測った。**
  最初 `flex:none` を付け忘れて、**箱が409pxに縮められて壊せていなかった**（落とし穴10-b）。
  「足した箱が本当に900pxか」を測る行を足して、そこから先へ進んだ

---

## L6 49-3・49-4：7色の総当たり21組

閾値は **ΔE2000 ≥ 10**。決め方は正本 1-2b に書いた
（世界の色は**記憶で比べる**——横に送ると0.3秒で溶け替わるので、並置の弁別閾では足りない。
現物で唯一「明らかに別」と言えた組が 12.5 だったので、そこに見劣りしない線）。

| 組 | 着手前 | いま |
|---|---|---|
| 共通 × あれそれ | **0.0** | **10.8** |
| 共通 × すごろく | **1.2** | **10.9** |
| 人狼 × クイズ | **3.4** | **13.9** |
| 人狼 × 爆弾 | 6.7 | 20.0 |
| 爆弾 × オク | 7.2 | 18.9 |
| 爆弾 × クイズ | 9.8 | 18.1 |
| 人狼 × オク | 9.9 | 19.3 |
| クイズ × オク | 12.5 | 27.7 |
| あれそれ × すごろく | （同じ色だった） | 21.4 |
| 明×暗の12組 | 84.5〜87.4 | 70.2〜88.6 |
| **最小** | **0.0** | **10.8** |

- 道具：`node tools/color-diff.js`（現物）・`node tools/color-palette.js`（案の採点）
- **道具の側を先に検算した**：ΔE2000 を Sharma らの公表検証データ9件と突き合わせ、
  9件とも小数4桁まで一致。sRGB→Lab も 白 L\*100／灰 L\*53.585 で確認（落とし穴19）
- 見張り：`tests/design-tokens.js`「7つの世界の色が、どの2つも見分けられる」
- 正本との一致：同じく「正本 1-2 の表と、実装の配色が一致する」

### 49-4 が掘り当てた穴（**本物の退行**）

`currentThemeName()` は「棚で選んだカセット」を先に見ていた。
`state.selectedCassetteId` の**既定値は `'aresoredorekore'`** なので、
棚を一度も通っていない人（部屋コードで直接入った人）でも
「あれそれを選んでいる」ことになっている——
**それでも動いていたのは、あれそれだけテーマを持っていなかったから。**

テーマを持たせた瞬間、部屋コードで人狼の部屋に入った人が杏色のままになった。
`tests/rt-screens.js` が3件で捕まえた。

直し方は「あれそれのテーマを外す」ではない。

**最初、順番を入れ替えて直そうとした**（部屋にいるなら部屋のゲームを先に見る）。
それは**間違いだった**——「進行役が次のゲームを選んでいる最中」は、
部屋の `game` がまだ前のゲームのままなので、
**選んだばかりのカセットの世界に変わらない**。
`tests/rt-screens.js` が別の2件（クイズ王・オークション）で捕まえた。

**順番は元のままが正しかった。**問題は「まだ何も選んでいない」を表せていなかったこと。
`state.selectedCassetteId` の**既定値を `null` にした**のが最終の直し方。
読む側は全部 `cassetteById(...)` の結果を見てから使っているので、`null` で困らない。

この回り道そのものが、落とし穴28の型だった——
**最初の直し方が通ったように見えたのは、そこを見ている検査を私が走らせていなかったから。**
部分実行では緑で、クリーンな1回で2件が出た（落とし穴32）。

---

## L7・L8 49-5：文言の全数校正

### 層ごとの件数（**ここが 49-5 の本体**）

指示は「HTMLの地の文も含めて2層」を前提にしていたが、掃き直したら**層は5つ足りなかった**。

| 層 | のべ | これまで |
|---|---|---|
| JS | 3048 | **単引用だけ**見ていた（二重引用・バッククォートは外） |
| HTML | 500 | **行ごと**に掃いていて、改行をまたぐ節点30件を落としていた |
| 属性 | 14 | 一度も見ていない（placeholder・aria-label・title） |
| CSS `content` | 3 | 一度も見ていない（`content:'枚'` は1文字なので「2文字以上」で切ると消える） |
| SRV | 81 | 一度も見ていない（`textContent = e.message` でそのまま画面に出る） |
| 文言表 | 186 | **ファイルごと除外していた**（ui-text.js。外すべきは表であってファイルではない） |
| **合計** | **3832** | 種類 3182 |

バッククォート（複数行のテンプレート）は **0件**。
ただし今度は**測って0**であって、見ていない0ではない。

### 読んだ件数

| | 件数 |
|---|---|
| のべ | 3832 |
| 種類 | 3182 |
| 対象外（お題データ `QUIZ_BANK`・本人の裁定） | 種類 171 |
| **読む単位（＝読了件数）** | **3011 / 3011** |

台帳は `docs/文言一覧.md`（`node tools/dump-ui-text.js` が作る。手で書き足さない）。
対象外は「E. 対象外」に全件が並ぶ——**読まないと決めただけで、見ていないのではない。**
再開の入口は `docs/切り出した宿題.md` の15。

### 直した件数：**20件**

| 層 | 直したもの |
|---|---|
| SRV（8） | ホストだけが譲れます→進行役だけがゆずれます／大画面には進行役を譲れません→ゆずれません／ホストだけが 操作できます・始められます・進められます・終了できます（4件）→進行役だけが…／今はその操作ができません・今は投票できません→いまは… |
| 文言表（5） | ROLE の「渡します／渡す相手をえらびます／渡す相手をえらぶ／だれに渡しますか？」→ ゆずる（4件）／ホストが部屋を閉じました→進行役が部屋を閉じました |
| HTML（1） | 押すと戻せません→押すともどせません（**改行をまたぐ節点**。この層は一度も掃かれていなかった） |
| JS（6） | 譲れませんでした×2→ゆずれませんでした／AIの説明を取得できませんでした。もう一度お試しください。→ MSG.AIが用意できない＋MSG.あとでためせる／しばらく待ってからもう一度お試しください→MSG.あとでためせる／🔄 もう一度取得×2→🔄 もう一度ためす／エラー：{e.error}→うまくいきませんでした（{e.error}） |
| 専門用語（3） | デフォルトは全部→はじめは全部えらばれています／プリセットの内容が入っています→おすすめの配分が入っています／全部リセットして最初から→全部消して最初から |

**「ゆずる」に寄せたのは、アプリ側の決まった言い方に合わせたため**
（台帳の「進行役をゆずりますか？」）。
連れて古くなる参照も直した（`tests/rt-screens.js`・台帳の SITES・index.html のコメント）。

### 見張りを、層をまたぐ形に直した

「責める言い方」と「専門用語」は **SOURCES の単引用しか見ていなかった**。
realtime.js も server.js も ui-text.js も、まるごとその外にいた——
だから `ホストだけが…` が6件、誰にも見られずに残っていた。
両方とも `校正の対象()`（層をまたぐ）に付け替えた。

**わざと戻して赤くなることを確かめた**：
`realtime.js` に ホスト を1つ戻す → `SRV realtime.js:1259「ホストだけが操作できます」→ 進行役`

### 残した件数と理由

| 残したもの | 理由 |
|---|---|
| 対象外 種類171件（お題データ） | 性質がデータであって UI の文言ではない（本人の裁定）。`docs/切り出した宿題.md` 15 |
| 下の「まとめて聞くこと」の一覧 | **勝手に直さない**と決めた分。指示 §3「49-5で迷う文言はまとめて聞く」に従う |

---

## L9 49-6：6カセットの名前が3サイズで1行

実ブラウザ（375×667）で、3つの文字サイズすべてを測った。

| 名前 | fs-small | 標準 | fs-large |
|---|---|---|---|
| 人狼ゲーム | 1行 | 1行 | 1行 |
| 爆弾解除 | 1行 | 1行 | 1行 |
| クイズ王 | 1行 | 1行 | 1行 |
| オークション | 1行 | 1行 | 1行 |
| すごろく | 1行 | 1行 | 1行 |
| ほか7つ準備中 | 1行 | 1行 | 1行 |

**ただし正直に書く**：`.cart-label .title` は `font-size:12px` の**直書き**で、
文字サイズ3段階はタイル名に**効いていない**。だから「3サイズで1行」は
そのぶん自明に成立している。札は絵の一部なので今回は触っていない
（正本 2-2 の「役割ごとの文字サイズ」からは外れている。**未処理として残す**）。

見張りは `tests/shelf.js`「棚のタイルの名前に、手で入れた改行が無い」
（jsdom にレイアウトは無いので、見るのは**改行が書かれていないこと**）。

---

## L10 49-7：実物解除が棚に無い／説明・人数・時間が追随／記録は残る

実サーバーで確認：

| 見たもの | 着手前 | いま |
|---|---|---|
| 人数・時間 | 2人〜（上限なし） | **2〜8人 / 14分〜**（クイズ解除に追随） |
| 分類 | brain ＋ move | **brain のみ**（`alsoGenre` を外した） |
| カセットの説明 | 実物解除が並ぶ | **🔧 実物解除 じゅんび中 ＋ なぜ下ろしたか** |
| ゲーム選択画面 | 2つから選ぶ | **挟まない**（games が1つ） |
| blurb | 爆発を止める・伝え合って解く | **問題を解いて、爆発を止める**（「伝え合って解く」は実物解除の説明だった・落とし穴33） |

記録・称号は**そのまま**（`defusePlays` / `defuseWins` / `defuseNoMiss` と称号3件）。
コードも消していない。

宣言の正本は `CASSETTES` の `paused:[{id,title,なぜ}]` **1か所**で、
画面も `tests/inventory.js` の `PAUSED_GAME_IDS` もそこを読む（落とし穴1）。

---

## L12 実機（色の見え方・屋外・TV）

**未検証。**

- 実機の画面で色がどう見えるかは測っていない。
  この作業場にあるのはブラウザ枠の実測だけで、**パネルの個体差・屋外の照度・TVの色域**は入っていない
- 案2の明るい3色（共通・あれそれ・すごろく）は L\* が 88.8〜92.0 と近い。
  **屋外の強い光の下では、明るい3つの見分けがいちばん先に崩れる**と予想している——
  ここは実機で見てもらうのが早い
- 暗い4色は逆に、明るい場所でのほうが見分けにくい（画面の反射で全部黒く見える）

---

## この指示で足した／直した見張り

| どこ | 何を見るか | わざと壊して赤を確認 |
|---|---|---|
| `tests/design-tokens.js` | 7つの世界の色が、どの2つも見分けられる（ΔE≥10） | あれそれを共通色に戻す → 「共通 × aresore：ΔE2000 0.0 < 10」 |
| 〃 | 世界の色と、そのカセットの地の色が同じ（落とし穴1） | 薄墨を古い値に戻す → 「auction：補 が 世界=#BFA9A2 / 配色=#E2BEB4」 |
| 〃 | 正本 1-2 の表と実装が一致する（落とし穴33のdocs版） | 正本だけ古い値に → 「wolf：地 が 正本=#161A29 / 実装=#261A55」 |
| `tests/ui-text.js` | 掃いている層が、宣言した層と一致する（両方向） | 行番号を+40ずらす→3563件／CSS層を落とす→「CSS」／未宣言の層を混ぜる→「未宣言」 |
| 〃 | 責める言い方・専門用語を**層をまたいで**掃く | realtime.js に ホスト を戻す → SRV層で名指し |
| `tests/shelf.js` | タイルの名前に手で入れた改行が無い | 自己赤チェックを同じ検査の中に置いた |
| 〃 | 下ろしたゲームは説明から黙って消えない | 理由を空にする → 「実物解除 に、遊ぶ人へ出す理由が書いてある（実際:「」）」 |
| `tests/room-paths.js` | 下ろしたゲームの宣言（両方向・理由・宿題の入口） | 理由を空にする → 「defuse に、遊ぶ人へ出す理由が書いてある」 |

**自分の新しい検査にも穴が1つあった。**
「なぜ下ろしたか が説明に出ている」を `説明.indexOf(p.なぜ) >= 0` で見ていたが、
**`indexOf('')` は必ず 0 を返す**ので、理由を空にしても緑のままだった。
空文字を先に弾く1行を足して、赤くなることを確かめた（型b）。

---

## この指示で足した道具

| 道具 | 何をするか |
|---|---|
| `tools/color-diff.js` | 世界の色の総当たり ΔE2000。表は index.html の**現物**から読む |
| `tools/color-palette.js` | 「案」を21組で採点する（現物／案1／案2／案3） |
| `tools/ui-text-scan.js` | 画面に文字が出る道を**6層**で集める。**tests/ui-text.js と同じ実装** |
| `tools/dump-ui-text.js` | `docs/文言一覧.md` を作る |
| `tools/triage-ui-text.js` | 全数校正の下ごしらえ（表記ゆれ・専門用語・誤字の候補・句点の様子） |
| `tools/measure-screens.js` | 77画面×3サイズの計測。**測り方が効くことを見る自己検査つき** |

道具は見つけたその場でリポジトリに置く（落とし穴29）。

---

## 測定そのものについて（落とし穴28）

この指示では、**測る道具が2回嘘をついた。**どちらも記録済みの型。

1. **`getComputedStyle().backgroundColor` が遷移の途中で止まっていた**（落とし穴47）。
   枠が裏に回っていると `document.timeline.currentTime` が 0 のままなので、
   人狼のテーマを当てたのに**共通のクリーム**が返り、爆弾では**途中の灰色**が返った。
   → 遷移を切ってから測り、**宣言値と computed が一致すること**を毎回確かめる形にした
2. **`window.scrollY` が当てにならなかった**（落とし穴45の親戚）。
   `innerHeight`(870) と `clientHeight`(812) が食い違い、
   枠のほうが背が高いぶん、はみ出していてもスクロールが起きない。
   375×667 でも `scrollHeight − clientHeight = 196` に対し実際に届くのは 148 だった。
   → **折り返しの権威は `clientHeight`**、はみ出しは**要素の矩形**で見る。
   実スクロールは参考値として並べるだけにした
