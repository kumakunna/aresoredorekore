# 監査：指示55の門（大画面の総点検 ＋ 問題文の正確さ・2026-09-18）

指示55の「完成の門 S1〜S13」に対する証拠。
**「未検証」は未検証と書く。**証拠の無い行に丸を付けない。

---

## 着手前に分かった、指示の前提とのズレ

指示55の前提の表は **`8b29474` の現物**と書いてあるが、
`8b29474` は**指示49 L12 のコミット**で、HEAD（`6d48f51`＝指示54）より**5つ前**。
そのあいだに指示53（新カセット False or True）が入っているので、行番号と本数がずれている。

| # | 指示の前提 | 現物（HEAD `6d48f51`） |
|---|---|---|
| ① | 大画面の描画関数は**7つ** | **8つ。** 指示53 で `renderRtBigFalsetrue`（`public/index.html:14151`）が増えた。行番号も全部ずれている（下の表が現物） |
| ② | 問題バンク `quiz-bank.js` は **85問** | **82問**（easy 25／normal 20／hard 15／nanisore 12／muri 10）。`8b29474` と HEAD で `quiz-bank.js` に差分は無いので、**最初から82問**。加えて**つぎつぎクイズの `LIST_TOPICS` が11本・答え257語**あり、こちらも同じ4観点の対象になる |
| ③ | (3) は「既存の `secrecy-gates` と突き合わせ」 | **`tests/secrecy-gates.js` は大画面を一行も見ていない。** 見ているのは手渡しの3画面（`scr-wolf-pass`・`scr-wr-pass`・`scr-topic-pass`）と、あれそれのお題が部屋へ漏れない逆向きの門だけ。大画面の秘匿を見張っているのは `tests/realtime-*.js` の「`privateFor` が大画面に `null` を返す」（wolf・bomb・defuse・falsetrue）と `tests/rt-screens.js:620` の1件 |
| ④ | マラソンの問題 | **実在。** 現物は「マラソンの距離は？」で、「フル」が無い |
| ⑤ | 協力版と競争版は `renderRtBigBomb` 1つ・`mode` 分岐 | **実在**（`public/index.html:18063` `else if(v.mode === 'coop')`） |
| ⑥ | 協力版のライフ・進行度は全員共通 | **実在。** `entryOf()`（`bomb-room.js:50-53`）が協力版では memberId を無視して `w.entries['team']` を返す |
| ⑦ | 正本に「大画面」の節は無い | **実在**（§1〜§10。§11 をこの指示で新設する） |

### 大画面の描画関数（現物・HEAD）

| # | 関数 | 行 | 受け持つゲーム |
|---|---|---|---|
| 1 | `renderRtBigFalsetrue` | `14151` | falsetrue |
| 2 | `renderRtBigSugoroku` | `14206` | sugohide／sugopair／sugograb／sugotoll／sugohand |
| 3 | `renderRtBigQuiz` | `15983` | quizrush／quizlist／quizreveal／buzzer |
| 4 | `renderRtBigAuction` | `16872` | auction |
| 5 | `renderRtBig`（振り分け＋**素通り本体**） | `17257`（本体は `17315`〜） | wolfrole |
| 6 | `renderRtBigWordwolf` | `17432` | wordwolf 系 |
| 7 | `renderRtBigBomb` | `18042` | bomb（協力版・競争版） |
| 8 | `renderRtBigDefuse` | `19029` | defuse |

---

## 本人の裁定（2026-09-18・着手前の報告に対して）

| # | 論点 | 裁定 |
|---|---|---|
| ① | 大画面の文字の最小サイズ | **承認。** 主役96px／数字48px／本文28px。前提（1280×720・50型・4m・ISO 9241-303 の20分角）もそのまま。**「添えだから小さくてよい」の逃げ道は作らない**——例外を1つ許すと、そこから崩れる |
| ② | 協力/対戦の分岐の持ち方 | **承認。** 正はサーバーの `v.coop`。`MODES.coopLayout` は宣言として置き、**両者が一致しているかを照合するためだけに使う**。`modeId` に依存すると「協力版が黙って対戦版として出る」という、まさに今回直す欠陥を再発させる |
| ③ | (4) の新規演出（クイズ王3ゲーム・すごろく2ゲーム） | **切り出す。** 規模が読めず、遊びの手ざわりの判断が要る種類の作業を「構造を直す」指示に混ぜると範囲が曖昧になる → `docs/切り出した宿題.md` 宿題18 |
| ④ | 宿題17（大画面の生存者一覧が溢れる） | **この指示に含める。** 「§11を新設するこのタイミングでしか、この宿題を綺麗に飲み込む機会はない」 |

※ 宿題19（称号 `rushNoPass`）・宿題20（死因の語彙が3か所）も、
③ と同じ理由で切り出した。

---

## 棚卸し①：全モードの大画面（指示55 1節）

4つの問い。**「否」＝直すべき**。

1. 共通の情報（全員に同じ値）を人ごとに並べていないか
2. そのモードで一番見せるべき情報が、一番大きいか
3. 秘密が映っていないか
4. 大画面だからできる演出を使っているか

| モード | (1) | (2) | (3) | (4) | 規模 |
|---|---|---|---|---|---|
| クイズ解除（協力版） | **否** | **否** | 可 | 可 | 中 |
| クイズ解除（競争版） | 可 | **否** | 可 | **否** | 中 |
| 実物解除 | 可 | **否** | 可 | 可 | 中 |
| クイズ王（4ゲーム） | **否** | **否** | 可 | **否** | 中 |
| オークションバトル | 可 | **否** | 可 | 可 | 中 |
| すごろく（5ゲーム） | **否** | **否** | 可 | **否** | 中 |
| False or True | 可 | **否** | 可 | 可 | 中 |
| ワードウルフ | 可 | **否** | 可 | 可 | 中 |
| 人狼（wolfrole） | 可 | **否** | 可 | 可 | 中 |
| **否の数** | **3** | **9** | **0** | **3** | — |

### (2) が 9/9 で「否」になった理由は、**1モードずつの不注意ではない**

3つの共通の穴が、全モードに同じ形で出ている。

#### 穴A：一番大きい枠が、すぐ上の文字を言い直している

`.big-main`（1280×720 で **115.2px**）に、`.big-phase`（25.6px）と同じ語が入る。

| モード | `bigPhase` | `bigMain` | 行 |
|---|---|---|---|
| クイズ解除（競争版） | 解除中 | **解除中** | `18073` |
| クイズラッシュ | クイズラッシュ | **クイズラッシュ** | `16006` |
| ワードウルフ | 話し合い／作戦会議／投票 ほか | 同じ語（8段階中**5段階**） | `17435`〜`17441` |
| 人狼 | 夜／作戦会議／投票 | 同じ語 | `17317`・`17318`・`17331` |

**画面でいちばん高価な場所が、情報量ゼロで使われている。**

#### 穴B：大画面に「共通の時計」が無い

- **置き場そのものが無い。** markup（`5486`〜`5528`）に時計の要素が1つも無い
- 出るのは `bigCountdownTick`（`17242`〜`17255`）の巨大カウントダウンだけで、
  **`sec` が 1〜5 の時しか描かない**（`17245`）。5分のゲームなら 4分55秒は何も出ない
- しかも **3つの進行役が `remainingMs` を送っていない**：

  | 進行役 | `remainingMs` |
  |---|---|
  | `wolf-room.js` | **0件**（`deadline` だけ） |
  | `wordwolf-room.js` | **0件** |
  | `sugoroku-room.js` | **0件**（手番90秒・売り札25秒・申告45秒があるのに） |
  | `bomb-room.js` | あり（ただし**競争版は `null`**・`313`） |
  | `defuse-room.js`／`quiz-room.js`／`auction-room.js`／`falsetrue-room.js` | あり |

- 逆に**出てはいけない所で出ている**：とくとくクイズ（quizreveal）の `deadline` は
  「次の1文字が出る時刻」（`quiz-room.js:309-325`）で 1文字あたり 870〜2222ms。
  `Math.ceil(msLeft/1000)` が常に 1 か 2 になるので、
  **420px の巨大カウントダウンが伏せ字の問題文の上に出続ける**
  （実測：6秒間に6回／別の回で3回。`position:fixed`・`z-index:95`）

#### 穴C：大画面の文字が、TVの大きさに追従しない

`clamp()` の付いていない**固定px**が、そのまま大画面に出る。

| セレクタ | 値 | どこ |
|---|---|---|
| `.bb-head`／`.bb-name` | **20px** | クイズ解除の横棒の名前 |
| `.bb-meta` | **18px** | 同・本数とライフ |
| `.blc-name` | **14px** | 人狼の盤の名前 |
| `.sugo-piece` | **13px** | すごろくの順位・駒の印 |
| `.bft-case` | 28.8px（`--fs-title`×1.2・vw を持たない） | False or True のケース番号 |
| `.blc-title` | **10px** | 二つ名 |

1920px のTVでは `.big-main` が 172.8px まで伸びるのに、名前は 14px のまま——**約12倍差**。
人狼では `.blc-x`（✕＝欠けた印）が 56px なので、
**「欠けた」という事実だけが名前の4倍**で、「誰が」が一番小さい。

### 実ブラウザで測った大画面の文字（1280×720・`node tools/dev-server.js`）

測る前に、測定そのものが健全かを確かめた（落とし穴28）：

- 使い捨ての要素に `font-size:37px` を当てて `getComputedStyle` が `37px` を返す
- **`innerWidth` は 1280、`documentElement.clientWidth` は 1265**（スクロールバー15px）。
  CSS の `vw` は `innerWidth` 側に付くので、`9vw` は 115.2px。
  **この2つを混ぜると 1.2% ずれる**——「同じ幅でも道具によって別のもの」

視角は **50型16:9（画の高さ 623mm → 1px＝0.865mm）を 4m から**見た値。

| セレクタ | 実測 | 視角 | どこの文字 |
|---|---|---|---|
| `.big-cd`（巨大カウントダウン） | **420.0px** | 312分 | 残り5秒の数字 |
| `#bigMain` | **115.2px** | 85.7分 | 一番大きい枠 |
| `.bmk-k .bk-v` | 38.4px | 28.6分 | 相場の値 |
| `.blc-x`（`.bl-card.out`） | 56.0px | 41.6分 | 人狼の「欠けた」✕ |
| `.blc-face` | 30.0px | 22.3分 | 人狼の顔 |
| `.bft-case` | 28.8px | 21.4分 | FoT のケース番号 |
| `#bigSub` | 26.9px | 20.0分 | 状況の行 |
| `#bigPhase` | 25.6px | 19.0分 | 段階名 |
| `.rules-ol` | 22.0px | 16.4分 | ルール文 |
| `.bb-head`／`.bb-name` | 20.0px | 14.9分 | 解除の名前 |
| `#bigTurn`／`.big-list`／`.bl-item` | 19.2px | 14.3分 | 名簿 |
| `.bb-meta` | 18.0px | 13.4分 | 解除の本数・ライフ |
| `.bmk-k .bk-l` | 17.9px | 13.3分 | 相場の札 |
| `.bmk-c .bc-l` | 14.7px | 10.9分 | 品名 |
| `.blc-name` | **14.0px** | 10.4分 | **人狼の名前** |
| `.bmk-c .bc-no` | 13.0px | 9.7分 | 品番 |
| `.bl-item .sugo-piece` | **13.0px** | 9.7分 | すごろくの順位・駒 |
| `.bk-hot`／`.bc-sold` | 12.8px | 9.5分 | HEATING UP／SOLD |
| `.bft-note` | 12.5px | 9.3分 | FoT の「のこり N 枚」 |
| `.bl-title` | 11.9px | 8.9分 | 二つ名 |
| `.blc-title` | **10.0px** | **7.4分** | 二つ名（札） |

参考：ISO 9241-303:2011 は **20〜22分角**を推奨・**16分角**が最小。
国交省の案内サインの目安（視距離÷250）は **13.8分角**で、これは
「視力0.5の人が視認できる下限」であって読み続ける基準ではない。

→ **21か所のうち14か所が20分角に届かず、9か所は13.8分角にも届かない。**
`.blc-title` は視認下限のおよそ半分。

### はみ出しも実ブラウザで測った（1280×720）

| 検体 | `scrollWidth` | `clientWidth` | はみ出し | 箱の高さ |
|---|---|---|---|---|
| とくとくクイズ・全部伏せ（22字） | 2351 | 1169 | **1182px** | 132px |
| 同・途中まで開いた | 1502 | 1169 | **333px** | 265px |
| 宿題17・6文字の名前×4人 | 1169 | 1169 | 0 | **397px** |

`window.scrollTo(2000,0)` で `scrollLeft` が **1162px 実際に動いた**（落とし穴45の確認）。

**なぜ伏せ字だけ折り返さないか**（仕組みまで見た）：
伏せ字は `◻`（U+25FB・Geometric Shapes）で、
Unicode の行分割クラスが **AL（英字あつかい）**。
`◻` を並べても**分割の機会が1つも生まれない**ので、折り返さずに横へ出る。
途中まで開いた方が折り返すのは、`maskedText`（`quiz-logic.js:205-215`）が
`、。？！「」（）` と空白を**伏せずに通す**からで、そこに分割の機会がある。

宿題17 の方は横には出ず、**縦に397px**になる（画面は705px）。
`bigPhase`＋`bigSub`＋`bigList`＋`big-tools` を足すと下が出る。

### (1) で「否」になった3件（すべて実測で裏を取った）

| モード | 何が起きているか | 場所 |
|---|---|---|
| **クイズ解除（協力版）** | `entryOf` が全員 `team` entry を返すので、`players[].solved/lives/pct/misses` が**全員同じ値**。それを人数ぶんの横棒に並べている。実測（3人で一番ばらけた状況を作っても）3行とも `2 / 6 ❤️❤️🖤`・`33%`。同じ「2 / 6」は `bigMain` にも出るので**画面上に同じ数字が4回** | `bomb-room.js:50-53`／`index.html:18081`・`17855-17866` |
| **すごろく「こまはひとつ」** | 進行中は `pos: null` を返すのに、**決着の `resultView` だけ共有駒の位置を全員ぶんコピーする**。大画面は「2位ならび あき 30/30 ／ 2位ならび ちか 30/30」と同じ数字を並べる | `sugoroku-room.js:1153-1158`／`index.html:14266` |
| **クイズ王「つぎつぎクイズ（協力）」** | `w.playerIds.forEach((id) => { w.scores[id] = l.said.length; })`——**チームで何個出せたかという1つの値を、全員の得点欄に書き込む**。実測で決着が「あき 1／びび 1／ちか 1」「1位 あき（1） 1位 ちか（1） 1位 びび（1）」 | `quiz-room.js:255-259`／`index.html:16015-16019` |

**逆向きも1件**：早押しとサバイバルは `w.scores` が遊んでいるあいだ一度も動かないので、
**人ごとの欄なのに中身が全員 0 のまま**。
本当に人ごとの値（`wins`）は `bigMain` の文字列に埋まっていて、対戦していない人のぶんはどこにも出ない。

### (3) はすべて「可」。ただし**守っているのは実装であって、見張りではない**

9モードとも、サーバーが大画面に秘密を送っていない（`privateFor` が `null` を返す）。
実測でも漏れは1件も出なかった。
ただし**大画面を見張っている検査は `tests/rt-screens.js:620` の1件だけ**で、
残りは「サーバーが送らない」という一方向の門。
**新しい表示を足した時に気づける形になっていない**（指示55 2-6 の恒久化が要る）。

### (4) で「否」になった3件

| モード | 何が無いか |
|---|---|
| **クイズ解除（競争版）** | `rtBigFx` の bomb 枝が `v.team`（協力版にしか無い）を見ているので、競争版は CRITICAL も縁の赤も出ない |
| **クイズ王** | 枝は13行で、中身は quizrush 専用の PERFECT と決着の紙吹雪だけ。**つぎつぎ・とくとく・早押しは、始まってから決着するまで大画面だけの演出が1つも無い** |
| **すごろく** | 枝は2つ（できごとのコールアウト・決着の紙吹雪）。前者は `events:'none'` の**どこにいる？・てふだでは一度も鳴らない**ので、この2ゲームは「終わった時の紙吹雪1回」だけ |

**False or True は「可」だが、置き場所が他と違う**——演出が `rtBigFx` ではなく
描画関数の中（`14190`〜`14200`）に直に書いてある。
`rtBigFx` から探すと falsetrue だけ見落とす（落とし穴1の型）。

### 棚卸しのついでに出た、4つの問いの外の不具合

| # | 何 | 場所 |
|---|---|---|
| a | **`.bft-case.is-held` が一度も当たらない。** `takeCase` が `heldNo` を `remaining` から `splice` するので、`v.cases` に `heldNo` は絶対に入らない。**大画面から「いまどのケースが動いているか」が分からない** | `falsetrue-room.js:319-322`／`index.html:14162` |
| b | **False or True の `bigList` を一度も書き直さない。** 待合の名簿（札4枚）が決着まで残り、脱落の印も付かない | `index.html:14151-14203` |
| c | **とくとくクイズの伏せ字が横にはみ出す。** 1280×720 の実測で `#bigMain` の `scrollWidth 2351` 対 `clientWidth 1169`＝**1182pxはみ出し**。`window.scrollTo(2000,0)` で `scrollLeft` が本当に 1134 動いた | `index.html:15992` |
| d | **称号「パスを1回も使わなかった」が原理的に解除できない。** `v.rush.passLimit` を読むが、`rushPublic` も `privateFor` も `passLimit` を送っていない | `index.html:14399`／`quiz-room.js:447-464`・`538-551` |
| e | **すごろくの「手番のない段階」が `mini` と `play` の2つしか数えていない。** `v.turn` は最初に入ったまま残るので、ready・roll・split・offer で**関係ない人の名前が200pxで出続ける**。てふだの `offer` では、200pxの `bigMain` が「あき さんの番」、22pxの帯が「手番の人以外が、売る札を出しています」と**反対のことを言う** | `index.html:14225` |
| f | **すごろくの巨大カウントダウンが構造的に死んでいる。** 点火条件が `v.remainingMs != null` なのに、`sugoroku-room.js` は `deadline` しか配らない | `index.html:17297`／`sugoroku-room.js:137-157` |
| g | **死因の語彙が3つ並走している。** `rtCauseLabel`（`15341`）・盤面の手書き分岐（`21224-21226`）・`DEAD_CAUSE_LABEL`（`22238-22243`）で「謎の死／呪殺」「後を追った／後追い」「襲われた／襲撃」。いま壊れてはいないが型1の再発源 | `index.html` |
| h | **`RT_PHASE_LABEL` に実物解除の `consent`／`roles`／`brief` が無い。** 大画面に生の段階名（英語）が出る。端末側の手書き `LABEL`（`18119`）も `brief` が抜けていて、2つの表が並走 | `index.html:14307-14318` |
| i | **`rtBig.bombLives` は書かれるだけで一度も読まれない**（第47弾で方式を変えた時の残骸） | `index.html:17050`・`17052`・`17091` |

### 宿題17 は、§11 の規則違反そのものだった

`docs/切り出した宿題.md` の宿題17（大画面の生存者一覧が溢れる）は、
**§11-1（個人のものを、共通のものの器に入れている）と §11-3（一番大きい器は短い見出しのためのもの）**
に真正面からぶつかる。
`renderRtBigFalsetrue` の決着が `el('bigMain').textContent = 生存.join('　')` で、
`.big-main` は `clamp(64px,9vw,200px)` の一枚看板。
**§11 を作る以上、これは「別の指示」ではなく §11 の適用対象になる。**

---

## §11「大画面」の草案（指示55 2-1）

**本人が承認した（2026-09-18）。** 実装の区切りで正本へ入れる。

### §11 草案：大画面（指示55）

大画面は、このアプリで**1台を全員で同時に見る唯一の画面**。
スマホの規則をそのまま持ってくると、**共通のものが人数分ならぶ**。
指示55 の棚卸しで、実際にそうなっていた（下の 11-1）。

**大画面は「みんなで囲む卓」であって、「小さい画面を大きくしたもの」ではない。**

#### 11-1. 共通のものは1つ大きく、個人のものは人ごとに

| 何 | どこに |
|---|---|
| **全員に同じ値**（協力版のライフ・共通の時計・共通の盤・残り本数・進行ゲージ） | 画面の上部に **1つだけ**、最大の大きさで |
| **人ごとに違う値**（担当・得点・生死・進捗・順位） | 名簿の行に、人数ぶん |

**見分け方は目で見るのではなく、データで決める。**
`publicView` が `players[]` に入れている項目でも、**モードによっては全員同じ値になる**——
爆弾解除の協力版は `entryOf()`（`bomb-room.js:50`）が
**チームの entry を返す**ので、`lives`・`solved`・`pct` が人を変えても同じ値になる。

> **「`players[]` に入っているから個人のもの」ではない。**
> 人を変えて値が変わるかどうかが、唯一の物差し。

同じ値を人数ぶん並べると、遊ぶ人には
「なぜ全員のライフが同時に減るのか」しか見えない——**共有していることが伝わらない。**

#### 11-2. 協力と対戦で画面を分ける

同じゲームでも、**勝ち方が違えば見せるものが違う**。

| | 主役 | 名簿の行にあるもの |
|---|---|---|
| 協力 | **みんなで1つの成績**（上部の大きな値） | 誰が何をしているか（担当・状態） |
| 対戦 | **並び**（順位・進捗の比べ合い） | その人の成績 |

描画関数を共有してよい。ただし**レイアウトの分岐は「モードの性質」で持つ**。

- `mode === 'coop'` のような**文字列比較を、レイアウトの分岐に新しく増やさない**
- **性質は、進行役（サーバー）が公開ビューに1つの真偽値で言う**（`v.coop`）
- `MODES` の項目にも属性（`coopLayout:true`）を持たせ、**両方向で照合する**

**なぜ `MODES` だけを読む形にしないか。**

> **【訂正・2026-09-18】この節の最初の草案は、理由を間違えて書いていた。**
> 「`modeId` は再戦・強制終了で消える（`tools/probe-rules-key.js` が並べている）」と書いたが、
> **その2経路はもう消えない。**第48弾 48-1 が `realtime.js` の `clearGameState`（`80-107`）に
> 「持ち越す」を入れ、`103` で `modeId`・`104` で `rulesEpoch` を明示的に残している。
> `rt-client.js:294-302` の `pickGame` も二重に載せ直す。
> **`tools/probe-rules-key.js` のコメントが1つ古かった**のを、そのまま前提にしていた——
> 落とし穴16（過去の断定を無条件に前提としない）そのもの。道具も「過去の断定」に入る。

実際に消えるのは**もっと広い**。本物のサーバーを立てて、
ゲスト端末に届いた `room.state.data` を見た実測：

| いつ | `game` | `data` の中身 | `modeId` | `mode` |
|---|---|---|---|---|
| 待合（協力版をえらんだ） | `bomb` | `["modeId"]` | `bomb-coop` | — |
| **ゲーム中** | `bomb` | `phase`／`mode`／`total`／`players`／`team`／`board`… | **無い** | `coop` |
| 「もう一度」のあと | `bomb` | `["modeId"]` | `bomb-coop` | — |

理由は `realtime.js:460`（`publicSnapshot`）：

    data: driverOf(room) ? driverOf(room).publicView(room) : room.state.data

**ゲーム中は `state.data` が publicView に丸ごと差し替わる。**
放送は `room:update` 1本だけなので、`modeId` は端末に**一度も届かない**。

つまり `if(rtPickedMode() && rtPickedMode().coopLayout)` と書くと、
**ゲーム中は必ず偽**。しかも待合では大画面の bomb 描画をそもそも通らない（`17257` の振り分けは
始まってからしか走らない）ので、**一度も真にならない。**
エラーは出ず、`v.mode` を触っている検査は緑のまま、
実機で「協力版なのに競争版の画面が出る」だけが起きる（落とし穴37：書いてあるのに効いていない）。

だから**正はサーバー**。`MODES.coopLayout` は**棚・設定の側の宣言**として持ち、
「`coopLayout:true` のモードで遊ぶと `v.coop === true` になる／その逆も」を
検査が両方向で見る（落とし穴20：A⊆B を書いたら B⊆A も要るか考える）。

**前例がある**：`wordwolf-room.js:67`・`108` が `MODES` の `wolfMulti` を
`multiTurn:` として publicView に載せている。同じ形にそろえる。

**不在で表さない**（落とし穴36）：`bomb && !bombRace` のような書き方にすると、
3つ目のモードが来た日に黙って協力版へ落ちる。`auctionOpen`／`auctionSealed` と同じく
**両方に印を付ける**。

**この根から出ている、別の不具合**（実測からの導出・画面では未確認）：
`nowPlaying()`（`24340-24343`）は部屋にいるあいだ `rtPickedMode()` を返すので、
**ゲーム中は null**。すると `liveGameSettings`（`24591`）が空になって
⚙の「このゲームの設定」の行が消え、`renderSetRules`（`24713-24722`）が
「このゲームの説明は、まだ用意できていません」を出す（大切なこと6とぶつかる）。
**実機で1度確かめる。**

理由は大切なこと9（借りたものは世界観ごと運ばれる）と落とし穴1。
文字列比較で散らすと、モードを足した日に**片方だけ直す**。
実際に第47弾で `mode === 'coop'` の門が競争版の決着演出をふさいでいた
（`public/index.html:17149` のコメントに記録が残っている）。

**いま散っている文字列比較**（レイアウト・表示の分岐に使っているもの）：
`index.html:15741`・`15758`・`15997`（`v.list.style === 'coop'`）、
`17139`（`結.mode === 'coop'`）、`18051`・`18063`（`r.mode`／`v.mode === 'coop'`）、
`17703`・`17748`・`17872`・`17993`（`v.mode === 'race'`）。

#### 11-3. 一番見せるべきものが一番大きい

モードごとに「**これが一目で分かればいい**」を**1つだけ**決め、それを最大にする。
決めたものは画面台帳（`docs/監査_画面一覧.md`）の「大画面の主役」列に書く。

**1つに決まらないなら、それはその画面が2つ分の仕事をしている。**

「一番大きい」は器の大きさで決まる。`.big-main` は
`font-size:clamp(64px,9vw,200px)` の**一枚看板**で、
部屋コードや勝者ひとりの名前のような**短い見出しの器**。
名前の列・一覧・複数行を入れる器ではない（入れると溢れる——宿題17）。

#### 11-4. 雰囲気と状況の両立

テーマ・演出（第47弾）は残す。ただし——

**演出の間も、状況（ライフ・時計・順位）は隠れきらない。**

コールアウトは画面の中央に大きく出るが、**上部の共通の値を覆わない**。
遊んでいる人は演出の最中も「あと何回ミスできるか」を知っていてよい。

いまの `.fx-callout` は `top:36%`（`public/index.html:967`）なので、
**共通の帯を上から 1/4 までに置けば、規則と実装が最初から噛み合う。**

##### 時計は「数えてよい締め切り」だけが持つ

大画面の巨大カウントダウン（`.big-cd`）は `v.remainingMs` を無条件に読んでいる。
ところが締め切りには2種類ある。

| 種類 | 例 | 数えてよいか |
|---|---|---|
| **人が待っている締め切り** | 話し合いの残り・解除の持ち時間・入札の締切 | **数える** |
| **画面が変わるだけの時刻** | とくとくクイズの「次の1文字が出る時刻」 | **数えない** |

区別しないと、**420pxの数字が伏せ字の問題文の上に1〜2秒ごとに出続ける**
（実測：6秒間に6回）。
**どちらかは進行役が言う**（`v.clockKind` のような属性）。
端末が締め切りの中身を当てにいかない——当てにいくと、
締め切りの意味を変えた日に遠くで壊れる。

#### 11-5. 大画面だからできること

| 使えるもの | どう使う |
|---|---|
| **画面の縁（全周）** | 知らせ。ライフが減った＝縁が赤く**1回**／もう後がない＝縁が**脈打つ** |
| **全員が同時に見る** | コールアウト・紙吹雪・入場・夜明け |
| **画面の大きさ** | 離れた席から読める文字（下） |

縁は**共通部品**にする（`FxKit.edge()`）。
いま `flashRoleEdge()`（人狼の陣営色）が `public/index.html` に個別実装されているが、
**演出は `fx.js` に足す**のが決まり（CLAUDE.md §4）。

##### 文字の最小サイズ（TVで離れた席から読める）

**前提の条件を1つに固定する**——決めないと「何インチの何メートル」で答えが変わる。

| 前提 | 値 | なぜその値か |
|---|---|---|
| 画面の作り | **1280×720**（CSS px） | 門 S9 の計測条件 |
| TVの大きさ | **50型 16:9**（画の高さ 623mm → **1px = 0.865mm**） | 学校・イベントで使う現実的な下限 |
| いちばん遠い席 | **4m** | 教室の後ろ |
| 必要な視角 | **20分角以上** | ISO 9241-303:2011 の推奨（20〜22分角）の下限 |

**この規則は「大画面らしい幅」でだけ効く**（`min-width:900px`）。
同じ画面を小さな窓で開いている時は、スマホの規則（§2-2）に戻る。

参考にした数字：

- **ISO 9241-303:2011** … 画面の文字は **20〜22分角**を推奨、**最小16分角**
  （[ISO 9241-303:2011 preview](https://webstore.ansi.org/preview-pages/ISO/preview_ISO+9241-303-2011.pdf)）
- **国土交通省の案内サイン** … 文字高 ≒ 視距離 ÷ 250（**13.8分角**）。
  ただしこれは**視力0.5の人が視認できる下限**で、読み続ける基準ではない
  （[移動円滑化整備ガイドライン](https://www.mlit.go.jp/common/001223022.pdf)）
- **このアプリ自身** … スマホの本文15px を 35cm から見ると **25.5分角**
  （375px幅＝およそ65mm → 1px ≒ 0.173mm）。ISO推奨より上

計算：20分角 → 4000mm × tan(20/60°) = 23.3mm → 23.3 ÷ 0.865 = **26.9px**。
切り上げて **28px**（20.8分角）。

| 役割 | 最小（1280×720） | 4m・50型での視角 |
|---|---|---|
| **主役**（11-3 で決めた1つ） | **96px** | 71分角 |
| **数字**（得点・時計・残り・ライフ） | **48px** | 36分角 |
| **本文・名前・札** | **28px** | 21分角 |

**これ以下でよい文字は、大画面に出さない。**
逃げ道（「添えだから小さくてよい」）を作らない——
作った瞬間に、どれが添えかを手で決める一覧ができる（落とし穴4）。
読めない文字を出すのは、**出していないのに出したつもりになる**という点で、
出さないことより悪い。

**いまの現物**（同じ条件で解いた `clamp()`。16か所のうち**14か所が 28px に届かない**）：

| セレクタ | 宣言 | 1280×720 | 視角 | 判定 |
|---|---|---|---|---|
| `.big-main` | `clamp(64px,9vw,200px)` | 115.2px | 85.7分 | ○（主役の96pxも満たす） |
| `.big-sub` | `clamp(20px,2.1vw,38px)` | 26.9px | 20.0分 | **×**（あと1.1px） |
| `.big-phase` | `clamp(20px,2vw,40px)` | 25.6px | 19.0分 | **×** |
| `.big-rules .rules-ol` | `22px` | 22.0px | 16.4分 | **×** |
| `.bb-head`／`.bb-name` | `20px` | 20.0px | 14.9分 | **×** |
| `.big-turn` | `clamp(16px,1.5vw,28px)` | 19.2px | 14.3分 | **×** |
| `.big-list` | `clamp(16px,1.5vw,30px)` | 19.2px | 14.3分 | **×** |
| `.bb-meta`（数字） | `18px` | 18.0px | 13.4分 | **×**（数字なので48px要る） |
| `.big-mk .bk-l` | `clamp(14px,1.4vw,22px)` | 17.9px | 13.3分 | **×** |
| `.big-mk .bc-l` | `clamp(12px,1.15vw,18px)` | 14.7px | 10.9分 | **×** |
| `.blc-name` | `14px` | 14.0px | 10.4分 | **×** |
| `.big-mk .bc-no` | `13px` | 13.0px | 9.7分 | **×** |
| `.bk-hot`／`.bc-sold` | `clamp(11px,1vw,15px)` | 12.8px | 9.5分 | **×** |
| `.bft-note` | `var(--fs-sub)` | 12.5px | 9.3分 | **×** |
| `.bl-item .bl-title` | `.62em` | 11.9px | 8.9分 | **×** |
| `.blc-title` | `10px` | 10.0px | 7.4分 | **×**（視認下限13.8分の**約半分**） |

**11か所は、国交省の「視認できる下限」（13.8分角）にすら届いていない。**

見張り：1280×720 での機械計測（門 S9）。
`.big-main` の 115.2px は、宿題17 が実ブラウザで測った値と**一致した**——
計算の側も健全（落とし穴28）。

#### 11-6. 秘密は映さない・二人称を使わない

- 大画面に出してよいのは**公開情報だけ**。見張りは `tests/secrecy-gates.js`。
  **新しい表示を足す時は、必ずここを通す**
- **「あなた」を使わない**（第48弾 48-2）。大画面には「あなた」が居ない——
  見ているのは全員で、誰のことか決まらない

#### 11-7. 順位表は共通の1部品

順位・得点を並べるものは、**アプリ全体で1つの部品**を使う（`renderRankingBig(rows)`）。

| 列 | 中身 |
|---|---|
| 順位 | 1位・2位…。**同着は並べる**（同じ順位を2人に出し、次を飛ばす） |
| 姿 | アイコン＋名前（名簿の1人分＝`memberFaceHtml` の考え方を運ぶ・§10） |
| 得点 | 数字（`DotGothic16`・11-5の最小サイズ） |

- **1位は大きい**
- 並べ替えの動きは **0.4秒**（§4 の「見せ場」0.75秒と「切り替え」0.25秒の間。
  順位が動くのは見せ場だが、行が何本も同時に動くので、跳ねると読めない）
- クラス名には**専用の前置き**を付ける（落とし穴23：後から書いたCSSが同名の先客に負ける）

#### 11-8. 安全基準は §6 のまま

点滅は1秒に3回まで／揺れは0.2秒・1回／**赤は縁のみ**／全画面の明滅は1回だけ。
11-5 の「縁が赤」は、**縁のみ**の規則にそのまま合う（面を塗らない）。


---

## 提案：「否」の扱い（指示55 2-3・この指示で直すか、切り出すか）

**本人が承認した（2026-09-18）。**

### 結論：(1)(2)(3) はこの指示で直す。(4) の一部を切り出す

(2) が 9/9 で「否」になったのを見て「9枚の画面を書き直す」と読むと、規模が読めなくなる。
実際には**3つの共通の穴（A・B・C）が9回見えている**だけで、
穴を塞ぐと (2) の大半が同時に片づく。

| 段 | 何をするか | 効く範囲 |
|---|---|---|
| 1 | **§11 を正本に置く**（規則） | 全部。新カセット6本の門にもなる |
| 2 | **穴B・Cを共通の器で塞ぐ**：大画面の上部帯（共通ライフ・共通の時計）を1つ作る／時計の種類を進行役が言う形にする／文字サイズの下限を効かせる | 9モード全部＋新カセット |
| 3 | **穴Aを塞ぐ**：モードごとに「主役」を1つ決めて `bigMain` に置き直し、台帳の列に書く | 9モード（1モードあたりは小さい） |
| 4 | **順位表を共通の1部品に**して既存3か所を寄せる（2-4） | すごろく・オークション・クイズ王 |
| 5 | **(1) の3件を直す**（協力版クイズ解除・こまはひとつ・つぎつぎクイズ協力） | サーバー側も1行ずつ |
| 6 | **(3) の見張りを足す**（大画面の秘匿を、画面の側から両方向で） | 全部 |

### 切り出しを提案するもの

| # | 何 | なぜ別か |
|---|---|---|
| A | **(4) の演出を新しく作る分**——クイズ王3ゲーム（つぎつぎ・とくとく・早押し）と、すごろく2ゲーム（どこにいる？・てふだ）に、大画面だけの演出を**新しく考える** | §11 は「使っているか」を問う規則で、**何を作るかはカセットごとの創作**。規模が読めないうえ、遊びの手ざわりを決める判断が要る（本人の領分） |
| B | **称号「パスを1回も使わなかった」が解除できない**（`v.rush.passLimit` を誰も送っていない） | 大画面と無関係。称号の話 |
| C | **死因の語彙が3か所で並走**（謎の死／呪殺 ほか） | いま壊れていない。型1の再発源だが、指示55の門に無い |
| D | **`RT_PHASE_LABEL` に実物解除の3段階が無い**（大画面に生の英語が出る） | 小さいので**この指示で拾う**方に倒してもよい。本人の判断 |

### この指示に**入れる**ことを提案するもの（指示に明記は無い）

| # | 何 | なぜ入れるか |
|---|---|---|
| ア | **宿題17（大画面の生存者一覧が溢れる）** | §11-1・§11-3 の規則違反そのもの。§11 を作る以上、これを残すと**最初から例外が1つある規則**になる |
| イ | **とくとくクイズの伏せ字が1182pxはみ出す** | §11-3（一番見せるべきものが一番大きい）の対象。**問題文が画面の外にある**ので、いまこのゲームはTVで遊べない |
| ウ | **巨大カウントダウンが伏せ字の上に1〜2秒ごとに出る** | 同上。§11-4（演出の間も状況は隠れきらない）の裏返しで、**状況でないものが主役を隠している** |
| エ | **False or True の `.bft-case.is-held` が一度も当たらない** | §11-3。大画面から「いまどのケースが動いているか」が分からない |
| オ | **すごろくの「手番のない段階」が2つしか無く、関係ない人の名前が200pxで出続ける** | §11-3。てふだでは200pxの文字と22pxの帯が**反対のことを言う** |

---

## 棚卸し②：問題バンク（指示55 1節）

対象は `public/js/quiz-bank.js` の **82問**（指示の前提は85問だったが現物は82問）。
**他のクイズ系が別の問題データを持っていないことも確かめた**——早押し・すごろくのミニゲームを
含めて全部 `quiz-bank.js` 経由（`bomb-room.js:24`・`quiz-room.js:28`・`sugoroku-room.js:32`・
`public/index.html:19601`・`22352`）。

※ 紛らわしいが、`public/index.html` の `QUIZ_BANK`（`9099`〜）は**別物**——
あれそれどれこれのお題（`name`／`yomi`／`ng_words`・206件）で、宿題15 として切り出し済み。

※ **つぎつぎクイズの `LIST_TOPICS`（11本・答え257語）は別に走らせている。**結果は次の区切りで。

### 校正の結果（82問・全問に根拠あり）

| | 数 |
|---|---|
| 読んだ問題 | **82**（easy 25／normal 20／hard 15／nanisore 12／muri 10） |
| 要修正 | **14** |
| 根拠（出典URL）が付いた問題 | **82 / 82** |
| 事実が確認できなかった | 1
| 難しすぎる印 | 1

観点ごとの内訳：

| 観点 | 否の数 |
|---|---|
| (a) 答えが一つに決まるか | 6 |
| (b) 前提の省略が無いか | 11 |
| (c) 事実として正しいか | 4 |
| (d) ダミーが区別できるか | 6 |

**「マラソン」型（総称語が複数の対象を指す）が、いちばん多い。**
実機報告の1件だけかと思っていたが、**同じ型が7件**あった。

### 要修正の一覧（before / 案）

#### 1. 〔b〕人間の骨はおよそ何本？  〈〉

- **いま**：正解は「約200本」
- **何が問題か**：(b) 前提の省略。実機報告の「マラソンの距離は？」とまったく同じ型。骨の数は年齢で変わり、成人は約206本だが新生児は約300本（出典により約270個／約305個）ある。「人間の」だけでは大人か赤ちゃんかが決まらない。選択肢に「約300本」が無いおかげで結果的に答えは1つに絞れる（＝(a) は可）が、それは選択肢の運であって問題文の力ではない。骨の数が成長で減ることを知っている子ほど迷う、という一番まずい形。
- **案**：問題文を「大人の骨はおよそ何本？」に変える（選択肢・正解位置はそのまま）。※影響：q は pickQuestions の「もう出した」印のキー（public/js/quiz-bank.js:214 `used[q.q]`）と defuse-logic.js:489 の出題済み判定に使われているので、文言を変えるとこの1問だけ印が一度リセットされ、直後の回で再出題されうる（実害は軽微）。tests/fixes45.js:761 は画面の文言からバンクを逆引きする作りなので、赤にはならない。
- **根拠**：星城大学 作業療法学ブログ「人間の骨は何個あるの？」https://www.seijoh-u.ac.jp/blog/2013/10/post-42.html ／看護roo!「全身の骨はいったい何個あるの？」https://www.kango-roo.com/learning/3753/ （成人206個）／国立国会図書館 レファレンス協同データベース「人間の骨の数が知りたい。」https://crd.ndl.go.jp/reference/entry/index.php?page=ref_view&id=1000233028 ／ラブすぽ「大人の骨は約200個！赤ちゃんは300個!?」https:…

#### 2. 〔b〕マラソンの距離は？  〈〉

- **いま**：正解は「42.195km」
- **何が問題か**：(b) 前提の省略。タスクで名指しされた実機報告の型そのもので、quiz-bank.js:80 に現物がある。「マラソン」はフルマラソンだけを指す語ではなく、ハーフマラソン（21.0975km）やウルトラマラソンも「マラソン」と呼ばれる。今の3択には21kmが無いので結果として答えは一つに決まる（＝(a) は可）が、子どもに「ハーフマラソンもマラソンでしょ」と言われたら、問題文の側に返す言葉がない。(c) は42.195kmで正しく、(d) も40km・45kmは同ジャンル・同水準で紛らわしすぎない。
- **案**：q を「フルマラソンの距離は？」に変更する（選択肢・正解位置は現状のままでよい）。**文言変更の影響は確認済みで、安全**：q の文字列をキーにしているのは (1) public/js/quiz-bank.js:213 の `!(used ／／ {})[q.q]`、(2) public/js/bomb-logic.js:183 の `seen[q.q] = true` の2か所だけで、どちらも「同じ問題を続けて出さない」ためにその場で組む一時キー。localStorage にもDBにも永続保存していない。テスト側（tests/fixes45.js:761、tests/smoke.js:2894 / 2983 / 3008）は画面に出た文字列でバンクを引く動的照合なので文言を変えても壊れない。tests/quiz-bank.js:37 の重複チェックも、バンク内で「マラソン」を含む問題は line 80 のこの1件だけ（grep …
- **根拠**：World Athletics 公式「Marathon」https://worldathletics.org/disciplines/road-running/marathon （「athletes run a distance of 26 miles and 385 yards (42.195km)」、1921年に標準化）／Wikipedia「マラソン」https://ja.wikipedia.org/wiki/%E3%83%9E%E3%83%A9%E3%82%BD%E3%83%B3 （「定められた距離以外の大会でもマラソンと呼ばれる場合がある、数キロ程度の長距離走からハーフマラソン（21.…

#### 3. 〔a・b〕パンダの体の色の組み合わせは？  〈〉

- **いま**：正解は「白と黒」
- **何が問題か**：「パンダ」だけでは種が決まらず、ダミーの「茶と白」がレッサーパンダの説明としてそのまま成立してしまう。レッサーパンダは「背面は赤褐色で…鼻面や唇、頬、耳介の外縁は白い」。実機報告の「マラソン／フルマラソン」と完全に同じ型で、レッサーパンダは日本の動物園の人気者なので、小学生ほど「茶と白」を選びうる。なお「単に『パンダ』といった場合はジャイアントパンダの方を指す」という慣用はあるので、事実(c)としては白と黒で正しい。
- **案**：問題文を「ジャイアントパンダの体の色の組み合わせは？」に変える（正解位置はそのまま0）。ダミーは「茶と白」をレッサーパンダ連想のまま残してよい（同ジャンル・同知名度で、むしろ良いダミーになる）。／id変更の影響：無害と確認済み。(1) 記録側で q をキーにしているのは public/js/bomb-logic.js:183 の seen[q.q] → public/index.html:22352 が渡す play.usedQuestions だけで、これは public/index.html:9321 の「同一マッチ内」用・9467 で毎回 {} に初期化される揮発値。永続化していない。(2) tests/fixes45.js:761 は `win.QuizBank.QUESTIONS.easy.find((q) => q.q === desc)` で、画面に出た文字列とバンクを実行時に突き合わせているだけでハードコードして…
- **根拠**：レッサーパンダ - Wikipedia（形態）「背面は赤褐色で、腹面や四肢・耳介外側は黒い。鼻面や唇、頬、耳介の外縁は白い」／（名称）「初めはレッサーパンダは単に『パンダ』と呼ばれていたが、のちにジャイアントパンダが発見されて有名になると、単に『パンダ』といった場合はジャイアントパンダの方を指すようになり」 https://ja.wikipedia.org/wiki/%E3%83%AC%E3%83%83%E3%82%B5%E3%83%BC%E3%83%91%E3%83%B3%E3%83%80 ／ 名古屋市 東山動植物園ガイド「レッサーパンダ」（全身が赤茶色の長い毛、顔に白い模様）https:/…

#### 4. 〔b・d〕「シャルル・ド・ゴール」は何の名前になっている？  〈〉

- **いま**：正解は「パリの空港」
- **何が問題か**：3つの選択肢の中では「パリの空港」だけが正しいので (a) は成立するが、(b) と (d) に問題がある。(b)：シャルル・ド・ゴールはフランスの軍人・大統領の人名で、その名前が付いているものは1つではない。パリの空港のほかに、パリのセーヌ川に架かる Pont Charles-de-Gaulle（橋）、パリの地下鉄・RER の Charles de Gaulle – Étoile（駅）、原子力空母シャルル・ド・ゴールなどが実在する。つまり「何の名前になっている？」という問い方は、実機報告の「マラソンの距離は？」と同じ型で、前提（どのカテゴリの話か）が省かれている。ダミーが「ロンドンの駅」「ベルリンの橋」と都市をずらしてあるおかげで辛うじて一意になっているだけで、「パリの駅」「パリの橋」と書かれていたら正解が3つになる。(d)：ダミーの外し方が都市名なので、シャルル・ド・ゴールがフランス人だと知っているだけで、空港のことを何も知…
- **案**：外さずに直せる。(b) は問題文を「フランスの元大統領シャルル・ド・ゴールの名前がついている、パリの玄関口は？」または「パリにある国際空港の名前になっている、フランスの元大統領は？」のようにカテゴリを明示する形へ。(d) はダミーの都市をパリに揃えず、かつ実在の「シャルル・ド・ゴール〜」とぶつからないものにする（例：「パリの空港」「パリの図書館」「パリの美術館」——図書館はミッテラン、美術館はオルセー／ポンピドゥーで、同じジャンル・同じくらいの知名度になり、国籍による消去法が効かなくなる）。影響：q と choices はどちらも docs/文言一覧.md（207〜210行）に載っているので、変更後に node tools/dump-ui-text.js の再生成が要る。q をキーにした恒久記録は無い（index 0 の proposal に根拠を記載）。
- **根拠**：パリ＝シャルル・ド・ゴール空港 - Wikipedia（日本語版） https://ja.wikipedia.org/wiki/%E3%83%91%E3%83%AA%EF%BC%9D%E3%82%B7%E3%83%A3%E3%83%AB%E3%83%AB%E3%83%BB%E3%83%89%E3%83%BB%E3%82%B4%E3%83%BC%E3%83%AB%E7%A9%BA%E6%B8%AF （「フランスの軍人・大統領のシャルル・ド・ゴールにちなんで名付けられた」、ロワシー＝アン＝フランス、1974年3月8日開港）／ Pont Charles-de-Gaulle - Wikipedia h…

#### 5. 〔a・d〕カフェオレとカフェラテの違いは？  〈〉

- **いま**：正解は「ベースがドリップかエスプレッソか」
- **何が問題か**：正解の「ベースがドリップかエスプレッソか」は事実として正しいが、ダミーの「牛乳の量」も事実として正しい違いになってしまっている。UCC 上島珈琲の公式 FAQ は、カフェ・オ・レを「コーヒーと温めたミルクを同量で合わせたもの」（＝1:1）、カフェ・ラテを「エスプレッソ1/5、スチームドミルク4/5程度の割合」と明記している。つまりミルクの割合も両者ではっきり違う。問題文が「違いは？」とだけ聞いているので、「牛乳の量」を選んだ子に「それも違いです」と言われたら反論できない——(a) が崩れ、(d) も崩れる。学校イベントで揉める型。
- **案**：外さずに直せる。q は変えず、ダミーの「牛乳の量」を事実として誤りのものに差し替えるのが最小の直し方（例：「豆の産地がちがう」「カップの大きさがちがう」。どちらも定義上の違いではないので一意になる）。問題文の側で締めるなら「カフェオレとカフェラテの、ベースになるコーヒーの違いは？」とし、選択肢を「ドリップとエスプレッソ」「浅煎りと深煎り」「水出しと湯出し」に揃える手もある。影響：choices も docs/文言一覧.md（212〜214行）に載っているので、変更後に node tools/dump-ui-text.js の再生成が要る。q を変えなければ記録側への影響は無い（index 0 の proposal に根拠を記載）。
- **根拠**：UCC上島珈琲 公式FAQ「『カフェ・オ・レ』『カフェ・ラテ』『カプチーノ』『エスプレッソ』は、どのように違うのですか？」 https://www.ucc.co.jp/customer/faq/faq013.html （カフェ・オ・レ＝「フレンチローストなど深めに焙煎した豆で抽出したコーヒーと温めたミルクを同量で合わせたもの」／カフェ・ラテ＝「カップにエスプレッソを抽出し、その上にスチームドミルクを注ぎます。エスプレッソ1/5、スチームドミルク4/5程度の割合です」）／ UCC上島珈琲「『カフェラテ』と『カフェオレ』の違いは！？」 https://www.ucc.co.jp/enjoy/tri…

#### 6. 〔b〕「いろは」で始まる文字は全部で何文字？  〈〉

- **いま**：正解は「47文字」
- **何が問題か**：47という数字そのものは正しい（静岡県教委「あすなろ学習室」小5・6古典に『「いろは歌」は仮名４７文字を１回ずつ使って作られています』、コトバンク＝ブリタニカ／日本大百科全書も「47の仮名をすべて1度ずつ用いてつくられた」）。問題は出題文が「いろは歌」と言っていないこと。「『いろは』で始まる文字」を「ひらがな全部」と読むと、ダミーの45文字も50文字も正解に見えてしまう——国立国語研究所によれば五十音図の実際の字数は45字（「ん」を入れて46字）で、50はあくまで呼び名。つまり読み違えた子にとっては、正解らしい選択肢が3つとも並んでいる状態になる。さらに、いろは順には末尾に「京」を伴って48字とする流儀が広く行われ、現代では「ん」を足すこともある（Wikipedia・ブリタニカ）ので、「いろは」という語だけでは47に絞りきれない。まさに実機報告の「マラソン／フルマラソン」と同じ型。
- **案**：問題文を「『いろは歌』は全部で何文字でできている？」に直す（「歌」の1字を足すだけで、五十音との読み違えも48字の流儀も同時に閉じられる）。q の文字列を変えても安全：q をキーにしている箇所はすべて実行時の照合で、保存された記録の鍵ではない（public/js/defuse-logic.js:489・public/index.html:18892 は1試合内の出題済み判定、tests/fixes45.js:761・tests/smoke.js:2894/2983/3008 は画面表示とバンクの突き合わせ。どちらもバンク側と表示側が同時に変わるので赤くならない）。難易度は easy のままでよい——小5・6の古典教材で「47文字」と明記して扱われている。
- **根拠**：静岡県教育委員会 あすなろ学習室 小5・6 古典「歴史的仮名遣い」 https://gakusyu.shizuoka-c.ed.jp/japanese/syou_56/koten/01/rekishiteki02.html ／ コトバンク「いろは歌」（ブリタニカ国際大百科事典・日本大百科全書） https://kotobank.jp/word/%E3%81%84%E3%82%8D%E3%81%AF%E6%AD%8C-32577 ／ Wikipedia「いろは歌」（四十七文字／「京」を加えて四十八字） https://ja.wikipedia.org/wiki/%E3%81%84%E3%82%8…

#### 7. 〔a・b・c・d〕キーボードの並びが「QWERTY」になった理由の定説は？  〈〉

- **いま**：正解は「タイプライターの絡まりを減らすため」
- **何が問題か**：そもそも「定説」が存在しない。IT用語辞典 e-Words は「なぜこのような配列になったのかは諸説ありはっきりしない」と明記し、(1)アームが干渉しないよう連続する文字を遠ざけた説、(2)手の交互打鍵の効率化説、(3)モールス符号の受信オペレータ向け説 の3つを並列に置いている。このうち(2)は選択肢1「英語で打ちやすいため」と実質同じ内容なので、正解と不正解が同じ資格の仮説になり、(a)も(d)も崩れる。さらに安岡孝一・素子『キーボード配列QWERTYの謎』は「アームの衝突を防ぐために、タイピストがなるべく打ちにくいようなキー配列をデザインした」という説を誤りだと述べており（神奈川県立の図書館「司書のでばん」2025年4月）、日本語版Wikipediaも諸説を並列に記すだけで「定説」「俗説」という評価語を使っていない。学校イベントで子どもに、専門家が否定している説を「定説」として覚えさせることになる。
- **案**：外す。理由は二重で、(a)選択肢1が実在する対立仮説なので答えが一つに決まらない、(c)「定説」という前提そのものが事実でない。言い換えでは救えない——「よく言われている理由は？」に直しても、誤りとされている説を正解として教える点は変わらない。差し替えるなら争いの無い機械的事実（例：QWERTYの最上段の文字だけで TYPEWRITER が打てる）にする。影響：q 文字列をキーにしているのは pickQuestions の used（public/js/quiz-bank.js:213）と bomb-logic.js:183 の seen だけで、どちらも実行中のメモリなので永続記録への波及は無い（tests/fixes45.js:761・tests/smoke.js:2894 も easy 層を実行時に照合しているだけで、この文言を名指ししていない）。ただし tests/quiz-bank.js:153 が「各難易度に最低10…
- **根拠**：IT用語辞典 e-Words「QWERTY配列」 https://e-words.jp/w/QWERTY%E9%85%8D%E5%88%97.html （「なぜこのような配列になったのかは諸説ありはっきりしない」、3説を並列記載）／神奈川県立の図書館「司書のでばん」2025年4月『キーボード配列QWERTYの謎』 https://www.klnet.pref.kanagawa.jp/publications/public-relations/shishonodeban/2025/04/qwerty.html （アーム衝突説は「誤りである」）／日本語版Wikipedia「QWERTY配列」 ht…

#### 8. 〔a・d〕ぞうの鼻はどこが伸びたもの？  〈〉

- **いま**：正解は「鼻と上くちびる」
- **何が問題か**：事実（鼻と上唇が一緒に伸びたもの）は動物園の公式記述で確認でき正しいが、選択肢の作りが2重に壊れている。(1)【(d)否・包含関係】正解「鼻と上くちびる」とダミー「口」は排他ではない——上くちびるは口（口唇）の一部なので、「口が伸びた」と答えた子を明確に誤りとは言い切れない。(2)【(a)否・同じ理由】2つ目が正解になりうる。(3)【バンクの決まり違反】問題文が「ぞうの“鼻”は」と名指ししているのに、3択のうち正解だけが「鼻」という語を含む。事実を1つも知らなくても、問題文と同じ言葉が入っている札を選べば当たる＝quiz-bank.js 冒頭の「内容を知らなくても消去法で当たってしまう」を避ける決まり（public/js/quiz-bank.js:16-17）に正面から反する。(4)【easy 不適】そもそも「鼻だけでなく上唇も一緒」は雑学寄りで、小学生が知っている前提にはできない。いま当たるのは選択肢の作りのせいで、知識で当た…
- **案**：作り直す。案①（事実を残す）：q を「ぞうの長い鼻は、鼻と何が一緒にのびたもの？」、choices を ['上くちびる','したくちびる','した']、correct 0。口まわりの部位でジャンルがそろい、問題文と正解の語の重複も消え、包含関係も無くなる。ただしこの形にすると事実の難しさがそのまま出るので、tier は normal（大人ならだいたい知っている）へ移すのが妥当。案②（easy に残す）：この問題は easy から外し、ぞうについては誰でも答えられる別の問題に差し替える。どちらを採っても (a)(d) は解消する。／【q の文言を変える影響】この5問について、q の文字列を直書きしている実装・テストは無い。tests/fixes45.js:761・tests/smoke.js:2894,2983,3008 はいずれも画面に出た文言で QUESTIONS.easy を実行時に逆引きしているだけなので、文言変更では壊れ…
- **根拠**：福岡市動物園 公式「アジアゾウ」＝『上唇と一緒に伸びた長い鼻は、筋肉でできており、器用に動かすことができます』 https://zoo.city.fukuoka.lg.jp/animals/detail/135 ／ 上野動物園 公式＝『実はゾウの長い鼻は、正確にいうと鼻と上唇が一緒になって伸びたものです』 https://x.com/UenoZooGardens/status/613543700891734016 ／ 愛知県総合教育センター 長鼻目の解説 https://apec.aichi-c.ed.jp/kyouka/rika/junior/2018/2nen/higasiyama/cho…

#### 9. 〔b〕「フィボナッチ数列」の最初の6つは1,1,2,3,5と、次は？  〈〉

- **いま**：正解は「8」
- **何が問題か**：問題文が壊れている。「最初の6つは1,1,2,3,5と、次は？」と書いてあるのに、並んでいる数は5つしかない。厳密に読めば「最初の6つ＝この5つ＋次の1つ」と解釈でき答えは8で確定するが、子どもには「6つと言ったのに5つしかない」としか見えず、数え間違いを疑わせる。さらに、フィボナッチ数列は OEIS A000045 の定義どおり F(0)=0 から始めるのが標準なので、その流儀では「最初の6つ」は 0,1,1,2,3,5 になり、問題文の前提自体が数学的に争える。なお副次的に、1,1,2,3,5 を見れば「前の2つの足し算」に気づけてしまうため、フィボナッチを知らなくても解ける＝hard の目安（知っている人は知っている）より易しい可能性がある。
- **案**：問題文を「フィボナッチ数列「1, 1, 2, 3, 5」の次の数は？」に書き換える。個数を数えさせる表現を消せば、0始まり／1始まりの流儀の違いにも巻き込まれず、答えは8で一意のまま。選択肢と正解位置（8 / 7 / 9・correct:0）は変更不要。【q の文字列を変える影響】記録の突き合わせが q をキーにしている箇所は tests/fixes45.js:761 の `win.QuizBank.QUESTIONS.easy.find((q) => q.q === desc)` だが、これは実行時に画面の文言と easy 帯を照合するもので、hard 帯の文言を変えても壊れない。一方 docs/文言一覧.md は quiz-bank.js から生成されており、この問題文が2か所（3651行目・6982行目）に載っているので、変更したら `node tools/dump-ui-text.js` で再生成が必要。
- **根拠**：OEIS A000045「Fibonacci numbers」（F(n)=F(n-1)+F(n-2), F(0)=0, F(1)=1。0, 1, 1, 2, 3, 5, 8, 13, 21, ...。1,1,2,3,5 の次は 8） https://oeis.org/A000045

#### 10. 〔a・b・c・d〕人体で一番大きい臓器は？  〈〉

- **いま**：正解は「皮膚」
- **何が問題か**：「臓器」の語で正解が2つになる。日本語の「臓器」は胸腹部の内臓を指す使い方が主流で、日本移植学会は「肝臓は、脳とともに人体最大の臓器です。」と明記している。つまり選択肢の「肝臓」も出典つきで正解になりうる（観点(a)否・観点(d)否）。「皮膚が最大」は英語 organ（＝器官）の定義で言われる話で、MSDマニュアルは「器官（臓器ともいいます）」と同義に扱っており、語を器官に替えても曖昧さが残る（観点(b)否）。学校イベントで子どもが「肝臓」を選んで不正解になると説明がつかない。
- **案**：（1）差し替え案：問題文を「人体で一番大きい内臓は？」、選択肢を ['肝臓','肺','胃']、正解を0（肝臓）にする。内臓と限定すれば出典（日本移植学会）と一致し、答えが一つに決まる。（2）皮膚の意外性を残したいなら「体の表面をおおう、人体で一番重い器官は？」のように限定が要るが、子ども向けには文が重い。（3）どちらも採らないなら外す。／文言変更の影響：q の文字列を鍵にしているのは public/index.html:9321 の play.usedQuestions（同一マッチ内の重複防止。9467 のマッチ開始でリセットし、22352 で pickQuestionWires に渡すだけ）と、tests/fixes45.js:761 の `QUESTIONS.easy.find(q => q.q === desc)`（画面に出ている文言をその場でバンクと突き合わせる比較。tier は easy なのでこの問題は対象外）。ど…
- **根拠**：一般社団法人 日本移植学会「臓器について｜肝臓」 https://www.asas.or.jp/jst/general/qa/liver/qa1.php （「肝臓は、脳とともに人体最大の臓器です。」）／Cleveland Clinic "Skin" https://my.clevelandclinic.org/health/body/10978-skin （skin is the body's largest organ）／MSDマニュアル家庭版「組織と器官」 https://www.msdmanuals.com/ja-jp/home/01-知っておきたい基礎知識/人体/組織と器官 （「器官（…

#### 11. 〔b〕音の速さは秒速およそ何メートル？  〈〉

- **いま**：正解は「約340m」
- **何が問題か**：(b) 媒質（＝空気中）の前提が省略されている。指示にある「マラソンの距離は？」型そのもの。音速は媒質で変わり、金沢工業大の学習支援サイトも式を「t〔℃〕の乾燥した空気中での音速」と限定し、「気体・液体・固体の順に大きくなる」と明記している。水中は約1500m/s。中学理科の教科書・NHK for School も必ず「空気中を伝わる音の速さ」と書く。なお気温の前提（約15℃）も省略されているが、「およそ」と書いてあるので気温の方は実害は小さい。選択肢に1500mが無いため答え自体は一意（a=可）だが、水中を知っている子が「約1000m」に引きずられる余地があり、ダミー「約1000m」が水中の1500m/sと桁が近いぶん、この省略は放置しない方がよい。
- **案**：問題文を「空気中を伝わる音の速さは秒速およそ何メートル？」に変える（選択肢・正解位置は変更なし）。／q 文字列変更の影響：この5問の q はリポジトリ内では public/js/quiz-bank.js:83 と、自動生成物 docs/文言一覧.md（3623行目・6955行目）にしか現れない。記録キーの懸念とされた tests/fixes45.js:761 は `win.QuizBank.QUESTIONS.easy.find((q) => q.q === desc)` で **easy tier しか引いていない**ため、normal の文言変更では赤くならない。長期保存される記録で q を鍵にしている箇所は見つからなかった（爆弾の答え合わせ review は1試合内のメモリ上のみ・public/index.html:22819 付近）。ただし docs/文言一覧.md が古くなるので `node tools/dump-ui…
- **根拠**：金沢工業大学 数学・物理 学習支援サイト「音の性質」 https://w3e.kanazawa-it.ac.jp/math/physics/high-school_index/wave/sound/henkan-tex.cgi?target=%2Fmath%2Fphysics%2Fhigh-school_index%2Fwave%2Fsound%2Fnature_of_sound.html — 原文「t 〔 ℃ 〕 の乾燥した空気中での音速 V 〔 m / s 〕 は，以下の式で表される。V = 331.5 + 0.6 t」、14℃で V = 339.9 ≒ 340 m/s の計算例あり、さら…

#### 12. 〔b〕将棋で、最初に持っている駒は何枚？  〈〉

- **いま**：正解は「20枚」
- **何が問題か**：(b) 前提の省略が2つある。①「一人が」が抜けている。日本将棋連盟の公式表記は「8種類の駒を各20枚、それを先手・後手がそれぞれ使いますので、計40枚」なので、盤全体では40枚。子どもが「盤に並んでいる駒ぜんぶ」と読むと40枚になり、選択肢に無い。②もっと悪いのは「持っている駒」という言い方で、将棋では「持ち駒」が確立した専門用語（デジタル大辞泉：相手からとって自分の手もとに持ち、いつでも使用できる駒）であり、**対局開始時の持ち駒は0枚**。将棋を知っている子ほど「最初は持ち駒ゼロでは？」と混乱する。将棋をよく知る子が不利になる問いになっており、学校イベントで出すには直しておきたい。なお選択肢に40枚も0枚も無いので答え自体は一意（a=可）。
- **案**：問題文を「将棋で、対局のはじめに一人が並べる駒は何枚？」に変える（選択肢 20枚／16枚／24枚・正解位置0はそのまま）。「持っている」を避けることで持ち駒との衝突が消え、「一人が」で40枚との取り違えも消える。／q 文字列変更の影響は #17 と同じ：quiz-bank.js:85 と自動生成の docs/文言一覧.md（3631行目・6963行目）のみ。tests/fixes45.js:761 は easy tier 限定の照合なので normal の文言変更では赤にならない。docs/文言一覧.md は `node tools/dump-ui-text.js` で再生成が必要。／ダミーについては、16枚がチェスの一人分の駒数なので「将棋の駒数のつもりで覚え違いをしている人」を拾う良いダミーとして機能しており、変更不要と判断した。
- **根拠**：日本将棋連盟 公式「将棋の基礎知識｜本将棋｜1.駒の並べ方」 https://www.shogi.or.jp/knowledge/shogi/01.php — 原文「本将棋は、8種類の駒を各20枚、それを先手・後手がそれぞれ使いますので、計40枚の駒を使うことになります。」（内訳：玉将／王将1・飛車1・角行1・金将2・銀将2・桂馬2・香車2・歩兵9＝20枚）。「持ち駒」の語義は デジタル大辞泉（Weblio辞書）https://www.weblio.jp/content/%E6%8C%81%E3%81%A1%E9%A7%92 — 原文「相手からとって自分の手もとに持ち、いつでも使用できる駒。手…

#### 13. 〔a・c・d〕ハチミツが腐らないおもな理由は？  〈〉

- **いま**：正解は「水分が非常に少ない」
- **何が問題か**：3択の3つとも本当の理由で、しかも1と3は同じ仕組みの言い換え。ハチミツの保存性は「糖が水と強く結びついて“使える水”＝水分活性が下がる（aw 0.50〜0.6、微生物生育なし）」ことで生まれるので、『砂糖だから』は『水分が非常に少ない』の原因そのもの。さらに悪いことに、水分量そのものは17〜20%あり「非常に少ない」とは言いにくい。メータージャパンの解説は「水分活性であって、多くの人が考えるような水分含量（含水率）ではない」と明言し、HACCP関連情報データベースも「水分含量と水分活性は比例関係にあるわけではなく、糖質や食塩濃度によって水分活性は変わる」「保存性では水分含量よりも水分活性が重要な指標」と書いている。つまり『水分が非常に少ない』を単独の正解、『砂糖だから』を不正解とするのは食品科学の説明と逆。加えて『酸性だから』（pH3.2〜4.5）も実在の要因で、米国NLM(PMC)の総説は non-peroxide fact…
- **案**：外す。3つとも正解になりうる以上、選択肢の入れ替えでは直らない（正解も含めて全部が同じ現象の別の面だから）。もし同じ題材を残したいなら、仕組みを1つに絞った別問題として作り直す：q「ハチミツの中で細菌が増えられないのはなぜ？」／正解「糖が水をつかまえて、菌が使える水が無いから」／ダミー「まわりより温度が低いから」「油の膜でふたがされているから」。q が変わるが、“出した問題”の集合は public/js/bomb-logic.js:183（seen[q.q]）と public/index.html:19603（sugo.usedQuiz[picked[0].q]）の2か所だけで、どちらも index.html:11487 で初期化される実行中メモリ、端末に保存はしていないことを確認済み——よって記録への影響は無い。tests/fixes45.js:761 と tests/smoke.js:2894/2983/3008 も、その場…
- **根拠**：メータージャパン「水分活性が微生物の生育を抑える仕組み」 https://www.metergroup.co.jp/expertise_library/library_220719_01.html （「水分活性であって、多くの人が考えるような水分含量（含水率）ではない」。表でハチミツ aw 0.50＝微生物生育なし）／公益社団法人日本食品衛生協会 HACCP関連情報データベース「水分活性」 https://haccp.shokusan.or.jp/glossary/suibun/ （「水分含量と水分活性は比例関係にあるわけではなく、糖質や食塩濃度によって水分活性は変わる」「保存性では水分含量よ…

#### 14. 〔b・c〕囲碁で「劫（コウ）」とは？  〈〉

- **いま**：正解は「同じ形の繰り返しを禁じる決まり」
- **何が問題か**：語そのものの定義を問いながら、正解はその語の「ルール」の説明になっている。日本囲碁規約 第六条（劫）は「交互に相手方の石一個を取り返し得る形を『劫』という。劫を取られた方は、次の着手でその劫を取り返すことはできない。」と、劫を**形**と定義していて、決まり（ルール）とは書いていない。しかもダミーの「二眼の形」が**形**なので、規約どおりに「劫＝形」と知っている人——muri の想定読者そのもの——ほど手が止まる。ただし二眼は生きの形であって劫ではないため、3択のうち選べるのは結局 0 番だけで、答えが割れることはない（a は可）。実装は public/js/quiz-bank.js:124。
- **案**：問題文を「囲碁の『コウのルール』とは？」に変える。選択肢は1つも触らずに (b)(c) が同時に解消する。（別案：問題文はそのままで正解を「交互に石を取り返し合える形」に変える。こちらは「二眼の形」が本物のダミーになり muri らしさは増すが、「端の石」が形ではないのでジャンルが揃わなくなる。前案を推す。）q を変える影響：q をキーにしているのは1試合内の重複防止だけで永続しない（public/index.html:19603 `sugo.usedQuiz[picked[0].q]=true`、9467 と 11487 で毎回リセット）。q で検体を名指ししているテスト tests/fixes45.js:761・tests/smoke.js:2894/2983/3008 は**4か所とも QUESTIONS.easy しか見ていない**ので、muri の文言変更では赤くならない。
- **根拠**：日本囲碁規約 全文 第六条（劫）https://www.nihonkiin.or.jp/match/kiyaku/zenbun.html ／ 日本囲碁規約 逐条解説（第六条は劫、第七条は死活）https://www.nihonkiin.or.jp/match/kiyaku/kiyaku07-2.html ／ 日本棋院「囲碁の基本：囲碁の打ち方（コウ）」https://www.nihonkiin.or.jp/teach/lesson/school/ko.html ／ Wikipedia「コウ」https://ja.wikipedia.org/wiki/%E3%82%B3%E3%82%A6



※ 校正の結果は次の区切りで追記する。

---

## 完成の門 S1〜S13

※ 実装後に埋める。
