# 監査：指示55の門（大画面の総点検 ＋ 問題文の正確さ・2026-09-18）

指示55の「完成の門 S1〜S13」に対する証拠。
**「未検証」は未検証と書く。**証拠の無い行に丸を付けない。

---

## 着手前に分かった、指示の前提とのズレ

指示55の前提の表は **`8b29474` の現物**と書いてあるが、
`8b29474` は**指示49 L12 のコミット**で、HEAD（`6d48f51`＝指示54）より**5つ前**。
そのあいだに指示53（新カセット False or True）が入っているので、行番号と本数がずれている。

| # | 指示の前提 | 現物（HEAD `6d48f51`） |
|---|---|---|
| ① | 大画面の描画関数は**7つ** | **8つ。** 指示53 で `renderRtBigFalsetrue`（`public/index.html:14151`）が増えた。行番号も全部ずれている（下の表が現物） |
| ② | 問題バンク `quiz-bank.js` は **85問** | **82問**（easy 25／normal 20／hard 15／nanisore 12／muri 10）。`8b29474` と HEAD で `quiz-bank.js` に差分は無いので、**最初から82問**。加えて**つぎつぎクイズの `LIST_TOPICS` が11本・答え257語**あり、こちらも同じ4観点の対象になる |
| ③ | (3) は「既存の `secrecy-gates` と突き合わせ」 | **`tests/secrecy-gates.js` は大画面を一行も見ていない。** 見ているのは手渡しの3画面（`scr-wolf-pass`・`scr-wr-pass`・`scr-topic-pass`）と、あれそれのお題が部屋へ漏れない逆向きの門だけ。大画面の秘匿を見張っているのは `tests/realtime-*.js` の「`privateFor` が大画面に `null` を返す」（wolf・bomb・defuse・falsetrue）と `tests/rt-screens.js:620` の1件 |
| ④ | マラソンの問題 | **実在。** 現物は「マラソンの距離は？」で、「フル」が無い |
| ⑤ | 協力版と競争版は `renderRtBigBomb` 1つ・`mode` 分岐 | **実在**（`public/index.html:18063` `else if(v.mode === 'coop')`） |
| ⑥ | 協力版のライフ・進行度は全員共通 | **実在。** `entryOf()`（`bomb-room.js:50-53`）が協力版では memberId を無視して `w.entries['team']` を返す |
| ⑦ | 正本に「大画面」の節は無い | **実在**（§1〜§10。§11 をこの指示で新設する） |

### 大画面の描画関数（現物・HEAD）

| # | 関数 | 行 | 受け持つゲーム |
|---|---|---|---|
| 1 | `renderRtBigFalsetrue` | `14151` | falsetrue |
| 2 | `renderRtBigSugoroku` | `14206` | sugohide／sugopair／sugograb／sugotoll／sugohand |
| 3 | `renderRtBigQuiz` | `15983` | quizrush／quizlist／quizreveal／buzzer |
| 4 | `renderRtBigAuction` | `16872` | auction |
| 5 | `renderRtBig`（振り分け＋**素通り本体**） | `17257`（本体は `17315`〜） | wolfrole |
| 6 | `renderRtBigWordwolf` | `17432` | wordwolf 系 |
| 7 | `renderRtBigBomb` | `18042` | bomb（協力版・競争版） |
| 8 | `renderRtBigDefuse` | `19029` | defuse |

---

## 本人の裁定（2026-09-18・着手前の報告に対して）

| # | 論点 | 裁定 |
|---|---|---|
| ① | 大画面の文字の最小サイズ | **承認。** 主役96px／数字48px／本文28px。前提（1280×720・50型・4m・ISO 9241-303 の20分角）もそのまま。**「添えだから小さくてよい」の逃げ道は作らない**——例外を1つ許すと、そこから崩れる |
| ② | 協力/対戦の分岐の持ち方 | **承認。** 正はサーバーの `v.coop`。`MODES.coopLayout` は宣言として置き、**両者が一致しているかを照合するためだけに使う**。`modeId` に依存すると「協力版が黙って対戦版として出る」という、まさに今回直す欠陥を再発させる |
| ③ | (4) の新規演出（クイズ王3ゲーム・すごろく2ゲーム） | **切り出す。** 規模が読めず、遊びの手ざわりの判断が要る種類の作業を「構造を直す」指示に混ぜると範囲が曖昧になる → `docs/切り出した宿題.md` 宿題18 |
| ④ | 宿題17（大画面の生存者一覧が溢れる） | **この指示に含める。** 「§11を新設するこのタイミングでしか、この宿題を綺麗に飲み込む機会はない」 |
| ⑤ | つぎつぎクイズの判定（`normalize` が漢字を吸収せず、正しい答えが弾かれて**退場**になる） | **この指示に含める。** 「82問の校正は精度の問題だが、こちらは正しく答えた子が罰を受ける、原則1そのものへの違反。今見つけた以上、次の指示に回す方が不誠実」。**器（正本＋別名の束）→判定→別名を足す、の順序を厳守**（素で足すと同じ答えが2回得点する・実測ずみ） |

※ 宿題19（称号 `rushNoPass`）・宿題20（死因の語彙が3か所）も、
③ と同じ理由で切り出した。

**指示55 の範囲は、本人の確定（2026-09-19）で
「大画面の総点検・問題文の正確さ・つぎつぎクイズの判定修正」の3つ。**

---

## 棚卸し①：全モードの大画面（指示55 1節）

4つの問い。**「否」＝直すべき**。

1. 共通の情報（全員に同じ値）を人ごとに並べていないか
2. そのモードで一番見せるべき情報が、一番大きいか
3. 秘密が映っていないか
4. 大画面だからできる演出を使っているか

| モード | (1) | (2) | (3) | (4) | 規模 |
|---|---|---|---|---|---|
| クイズ解除（協力版） | **否** | **否** | 可 | 可 | 中 |
| クイズ解除（競争版） | 可 | **否** | 可 | **否** | 中 |
| 実物解除 | 可 | **否** | 可 | 可 | 中 |
| クイズ王（4ゲーム） | **否** | **否** | 可 | **否** | 中 |
| オークションバトル | 可 | **否** | 可 | 可 | 中 |
| すごろく（5ゲーム） | **否** | **否** | 可 | **否** | 中 |
| False or True | 可 | **否** | 可 | 可 | 中 |
| ワードウルフ | 可 | **否** | 可 | 可 | 中 |
| 人狼（wolfrole） | 可 | **否** | 可 | 可 | 中 |
| **否の数** | **3** | **9** | **0** | **3** | — |

### (2) が 9/9 で「否」になった理由は、**1モードずつの不注意ではない**

3つの共通の穴が、全モードに同じ形で出ている。

#### 穴A：一番大きい枠が、すぐ上の文字を言い直している

`.big-main`（1280×720 で **115.2px**）に、`.big-phase`（25.6px）と同じ語が入る。

| モード | `bigPhase` | `bigMain` | 行 |
|---|---|---|---|
| クイズ解除（競争版） | 解除中 | **解除中** | `18073` |
| クイズラッシュ | クイズラッシュ | **クイズラッシュ** | `16006` |
| ワードウルフ | 話し合い／作戦会議／投票 ほか | 同じ語（8段階中**5段階**） | `17435`〜`17441` |
| 人狼 | 夜／作戦会議／投票 | 同じ語 | `17317`・`17318`・`17331` |

**画面でいちばん高価な場所が、情報量ゼロで使われている。**

#### 穴B：大画面に「共通の時計」が無い

- **置き場そのものが無い。** markup（`5486`〜`5528`）に時計の要素が1つも無い
- 出るのは `bigCountdownTick`（`17242`〜`17255`）の巨大カウントダウンだけで、
  **`sec` が 1〜5 の時しか描かない**（`17245`）。5分のゲームなら 4分55秒は何も出ない
- しかも **3つの進行役が `remainingMs` を送っていない**：

  | 進行役 | `remainingMs` |
  |---|---|
  | `wolf-room.js` | **0件**（`deadline` だけ） |
  | `wordwolf-room.js` | **0件** |
  | `sugoroku-room.js` | **0件**（手番90秒・売り札25秒・申告45秒があるのに） |
  | `bomb-room.js` | あり（ただし**競争版は `null`**・`313`） |
  | `defuse-room.js`／`quiz-room.js`／`auction-room.js`／`falsetrue-room.js` | あり |

- 逆に**出てはいけない所で出ている**：とくとくクイズ（quizreveal）の `deadline` は
  「次の1文字が出る時刻」（`quiz-room.js:309-325`）で 1文字あたり 870〜2222ms。
  `Math.ceil(msLeft/1000)` が常に 1 か 2 になるので、
  **420px の巨大カウントダウンが伏せ字の問題文の上に出続ける**
  （実測：6秒間に6回／別の回で3回。`position:fixed`・`z-index:95`）

#### 穴C：大画面の文字が、TVの大きさに追従しない

`clamp()` の付いていない**固定px**が、そのまま大画面に出る。

| セレクタ | 値 | どこ |
|---|---|---|
| `.bb-head`／`.bb-name` | **20px** | クイズ解除の横棒の名前 |
| `.bb-meta` | **18px** | 同・本数とライフ |
| `.blc-name` | **14px** | 人狼の盤の名前 |
| `.sugo-piece` | **13px** | すごろくの順位・駒の印 |
| `.bft-case` | 28.8px（`--fs-title`×1.2・vw を持たない） | False or True のケース番号 |
| `.blc-title` | **10px** | 二つ名 |

1920px のTVでは `.big-main` が 172.8px まで伸びるのに、名前は 14px のまま——**約12倍差**。
人狼では `.blc-x`（✕＝欠けた印）が 56px なので、
**「欠けた」という事実だけが名前の4倍**で、「誰が」が一番小さい。

### 実ブラウザで測った大画面の文字（1280×720・`node tools/dev-server.js`）

測る前に、測定そのものが健全かを確かめた（落とし穴28）：

- 使い捨ての要素に `font-size:37px` を当てて `getComputedStyle` が `37px` を返す
- **`innerWidth` は 1280、`documentElement.clientWidth` は 1265**（スクロールバー15px）。
  CSS の `vw` は `innerWidth` 側に付くので、`9vw` は 115.2px。
  **この2つを混ぜると 1.2% ずれる**——「同じ幅でも道具によって別のもの」

視角は **50型16:9（画の高さ 623mm → 1px＝0.865mm）を 4m から**見た値。

| セレクタ | 実測 | 視角 | どこの文字 |
|---|---|---|---|
| `.big-cd`（巨大カウントダウン） | **420.0px** | 312分 | 残り5秒の数字 |
| `#bigMain` | **115.2px** | 85.7分 | 一番大きい枠 |
| `.bmk-k .bk-v` | 38.4px | 28.6分 | 相場の値 |
| `.blc-x`（`.bl-card.out`） | 56.0px | 41.6分 | 人狼の「欠けた」✕ |
| `.blc-face` | 30.0px | 22.3分 | 人狼の顔 |
| `.bft-case` | 28.8px | 21.4分 | FoT のケース番号 |
| `#bigSub` | 26.9px | 20.0分 | 状況の行 |
| `#bigPhase` | 25.6px | 19.0分 | 段階名 |
| `.rules-ol` | 22.0px | 16.4分 | ルール文 |
| `.bb-head`／`.bb-name` | 20.0px | 14.9分 | 解除の名前 |
| `#bigTurn`／`.big-list`／`.bl-item` | 19.2px | 14.3分 | 名簿 |
| `.bb-meta` | 18.0px | 13.4分 | 解除の本数・ライフ |
| `.bmk-k .bk-l` | 17.9px | 13.3分 | 相場の札 |
| `.bmk-c .bc-l` | 14.7px | 10.9分 | 品名 |
| `.blc-name` | **14.0px** | 10.4分 | **人狼の名前** |
| `.bmk-c .bc-no` | 13.0px | 9.7分 | 品番 |
| `.bl-item .sugo-piece` | **13.0px** | 9.7分 | すごろくの順位・駒 |
| `.bk-hot`／`.bc-sold` | 12.8px | 9.5分 | HEATING UP／SOLD |
| `.bft-note` | 12.5px | 9.3分 | FoT の「のこり N 枚」 |
| `.bl-title` | 11.9px | 8.9分 | 二つ名 |
| `.blc-title` | **10.0px** | **7.4分** | 二つ名（札） |

参考：ISO 9241-303:2011 は **20〜22分角**を推奨・**16分角**が最小。
国交省の案内サインの目安（視距離÷250）は **13.8分角**で、これは
「視力0.5の人が視認できる下限」であって読み続ける基準ではない。

→ **21か所のうち14か所が20分角に届かず、9か所は13.8分角にも届かない。**
`.blc-title` は視認下限のおよそ半分。

### はみ出しも実ブラウザで測った（1280×720）

| 検体 | `scrollWidth` | `clientWidth` | はみ出し | 箱の高さ |
|---|---|---|---|---|
| とくとくクイズ・全部伏せ（22字） | 2351 | 1169 | **1182px** | 132px |
| 同・途中まで開いた | 1502 | 1169 | **333px** | 265px |
| 宿題17・6文字の名前×4人 | 1169 | 1169 | 0 | **397px** |

`window.scrollTo(2000,0)` で `scrollLeft` が **1162px 実際に動いた**（落とし穴45の確認）。

**なぜ伏せ字だけ折り返さないか**（仕組みまで見た）：
伏せ字は `◻`（U+25FB・Geometric Shapes）で、
Unicode の行分割クラスが **AL（英字あつかい）**。
`◻` を並べても**分割の機会が1つも生まれない**ので、折り返さずに横へ出る。
途中まで開いた方が折り返すのは、`maskedText`（`quiz-logic.js:205-215`）が
`、。？！「」（）` と空白を**伏せずに通す**からで、そこに分割の機会がある。

宿題17 の方は横には出ず、**縦に397px**になる（画面は705px）。
`bigPhase`＋`bigSub`＋`bigList`＋`big-tools` を足すと下が出る。

### (1) で「否」になった3件（すべて実測で裏を取った）

| モード | 何が起きているか | 場所 |
|---|---|---|
| **クイズ解除（協力版）** | `entryOf` が全員 `team` entry を返すので、`players[].solved/lives/pct/misses` が**全員同じ値**。それを人数ぶんの横棒に並べている。実測（3人で一番ばらけた状況を作っても）3行とも `2 / 6 ❤️❤️🖤`・`33%`。同じ「2 / 6」は `bigMain` にも出るので**画面上に同じ数字が4回** | `bomb-room.js:50-53`／`index.html:18081`・`17855-17866` |
| **すごろく「こまはひとつ」** | 進行中は `pos: null` を返すのに、**決着の `resultView` だけ共有駒の位置を全員ぶんコピーする**。大画面は「2位ならび あき 30/30 ／ 2位ならび ちか 30/30」と同じ数字を並べる | `sugoroku-room.js:1153-1158`／`index.html:14266` |
| **クイズ王「つぎつぎクイズ（協力）」** | `w.playerIds.forEach((id) => { w.scores[id] = l.said.length; })`——**チームで何個出せたかという1つの値を、全員の得点欄に書き込む**。実測で決着が「あき 1／びび 1／ちか 1」「1位 あき（1） 1位 ちか（1） 1位 びび（1）」 | `quiz-room.js:255-259`／`index.html:16015-16019` |

**逆向きも1件**：早押しとサバイバルは `w.scores` が遊んでいるあいだ一度も動かないので、
**人ごとの欄なのに中身が全員 0 のまま**。
本当に人ごとの値（`wins`）は `bigMain` の文字列に埋まっていて、対戦していない人のぶんはどこにも出ない。

### (3) はすべて「可」。ただし**守っているのは実装であって、見張りではない**

9モードとも、サーバーが大画面に秘密を送っていない（`privateFor` が `null` を返す）。
実測でも漏れは1件も出なかった。
ただし**大画面を見張っている検査は `tests/rt-screens.js:620` の1件だけ**で、
残りは「サーバーが送らない」という一方向の門。
**新しい表示を足した時に気づける形になっていない**（指示55 2-6 の恒久化が要る）。

### (4) で「否」になった3件

| モード | 何が無いか |
|---|---|
| **クイズ解除（競争版）** | `rtBigFx` の bomb 枝が `v.team`（協力版にしか無い）を見ているので、競争版は CRITICAL も縁の赤も出ない |
| **クイズ王** | 枝は13行で、中身は quizrush 専用の PERFECT と決着の紙吹雪だけ。**つぎつぎ・とくとく・早押しは、始まってから決着するまで大画面だけの演出が1つも無い** |
| **すごろく** | 枝は2つ（できごとのコールアウト・決着の紙吹雪）。前者は `events:'none'` の**どこにいる？・てふだでは一度も鳴らない**ので、この2ゲームは「終わった時の紙吹雪1回」だけ |

**False or True は「可」だが、置き場所が他と違う**——演出が `rtBigFx` ではなく
描画関数の中（`14190`〜`14200`）に直に書いてある。
`rtBigFx` から探すと falsetrue だけ見落とす（落とし穴1の型）。

### 棚卸しのついでに出た、4つの問いの外の不具合

| # | 何 | 場所 |
|---|---|---|
| a | **`.bft-case.is-held` が一度も当たらない。** `takeCase` が `heldNo` を `remaining` から `splice` するので、`v.cases` に `heldNo` は絶対に入らない。**大画面から「いまどのケースが動いているか」が分からない** | `falsetrue-room.js:319-322`／`index.html:14162` |
| b | **False or True の `bigList` を一度も書き直さない。** 待合の名簿（札4枚）が決着まで残り、脱落の印も付かない | `index.html:14151-14203` |
| c | **とくとくクイズの伏せ字が横にはみ出す。** 1280×720 の実測で `#bigMain` の `scrollWidth 2351` 対 `clientWidth 1169`＝**1182pxはみ出し**。`window.scrollTo(2000,0)` で `scrollLeft` が本当に 1134 動いた | `index.html:15992` |
| d | **称号「パスを1回も使わなかった」が原理的に解除できない。** `v.rush.passLimit` を読むが、`rushPublic` も `privateFor` も `passLimit` を送っていない | `index.html:14399`／`quiz-room.js:447-464`・`538-551` |
| e | **すごろくの「手番のない段階」が `mini` と `play` の2つしか数えていない。** `v.turn` は最初に入ったまま残るので、ready・roll・split・offer で**関係ない人の名前が200pxで出続ける**。てふだの `offer` では、200pxの `bigMain` が「あき さんの番」、22pxの帯が「手番の人以外が、売る札を出しています」と**反対のことを言う** | `index.html:14225` |
| f | **すごろくの巨大カウントダウンが構造的に死んでいる。** 点火条件が `v.remainingMs != null` なのに、`sugoroku-room.js` は `deadline` しか配らない | `index.html:17297`／`sugoroku-room.js:137-157` |
| g | **死因の語彙が3つ並走している。** `rtCauseLabel`（`15341`）・盤面の手書き分岐（`21224-21226`）・`DEAD_CAUSE_LABEL`（`22238-22243`）で「謎の死／呪殺」「後を追った／後追い」「襲われた／襲撃」。いま壊れてはいないが型1の再発源 | `index.html` |
| h | **`RT_PHASE_LABEL` に実物解除の `consent`／`roles`／`brief` が無い。** 大画面に生の段階名（英語）が出る。端末側の手書き `LABEL`（`18119`）も `brief` が抜けていて、2つの表が並走 | `index.html:14307-14318` |
| i | **`rtBig.bombLives` は書かれるだけで一度も読まれない**（第47弾で方式を変えた時の残骸） | `index.html:17050`・`17052`・`17091` |

### 宿題17 は、§11 の規則違反そのものだった

`docs/切り出した宿題.md` の宿題17（大画面の生存者一覧が溢れる）は、
**§11-1（個人のものを、共通のものの器に入れている）と §11-3（一番大きい器は短い見出しのためのもの）**
に真正面からぶつかる。
`renderRtBigFalsetrue` の決着が `el('bigMain').textContent = 生存.join('　')` で、
`.big-main` は `clamp(64px,9vw,200px)` の一枚看板。
**§11 を作る以上、これは「別の指示」ではなく §11 の適用対象になる。**

---

## §11「大画面」の草案（指示55 2-1）

**本人が承認した（2026-09-18）。正本 `docs/デザインの正本.md` §11 に入れた。**

### §11 草案：大画面（指示55）

大画面は、このアプリで**1台を全員で同時に見る唯一の画面**。
スマホの規則をそのまま持ってくると、**共通のものが人数分ならぶ**。
指示55 の棚卸しで、実際にそうなっていた（下の 11-1）。

**大画面は「みんなで囲む卓」であって、「小さい画面を大きくしたもの」ではない。**

#### 11-1. 共通のものは1つ大きく、個人のものは人ごとに

| 何 | どこに |
|---|---|
| **全員に同じ値**（協力版のライフ・共通の時計・共通の盤・残り本数・進行ゲージ） | 画面の上部に **1つだけ**、最大の大きさで |
| **人ごとに違う値**（担当・得点・生死・進捗・順位） | 名簿の行に、人数ぶん |

**見分け方は目で見るのではなく、データで決める。**
`publicView` が `players[]` に入れている項目でも、**モードによっては全員同じ値になる**——
爆弾解除の協力版は `entryOf()`（`bomb-room.js:50`）が
**チームの entry を返す**ので、`lives`・`solved`・`pct` が人を変えても同じ値になる。

> **「`players[]` に入っているから個人のもの」ではない。**
> 人を変えて値が変わるかどうかが、唯一の物差し。

同じ値を人数ぶん並べると、遊ぶ人には
「なぜ全員のライフが同時に減るのか」しか見えない——**共有していることが伝わらない。**

#### 11-2. 協力と対戦で画面を分ける

同じゲームでも、**勝ち方が違えば見せるものが違う**。

| | 主役 | 名簿の行にあるもの |
|---|---|---|
| 協力 | **みんなで1つの成績**（上部の大きな値） | 誰が何をしているか（担当・状態） |
| 対戦 | **並び**（順位・進捗の比べ合い） | その人の成績 |

描画関数を共有してよい。ただし**レイアウトの分岐は「モードの性質」で持つ**。

- `mode === 'coop'` のような**文字列比較を、レイアウトの分岐に新しく増やさない**
- **性質は、進行役（サーバー）が公開ビューに1つの真偽値で言う**（`v.coop`）
- `MODES` の項目にも属性（`coopLayout:true`）を持たせ、**両方向で照合する**

**なぜ `MODES` だけを読む形にしないか。**

> **【訂正・2026-09-18】この節の最初の草案は、理由を間違えて書いていた。**
> 「`modeId` は再戦・強制終了で消える（`tools/probe-rules-key.js` が並べている）」と書いたが、
> **その2経路はもう消えない。**第48弾 48-1 が `realtime.js` の `clearGameState`（`80-107`）に
> 「持ち越す」を入れ、`103` で `modeId`・`104` で `rulesEpoch` を明示的に残している。
> `rt-client.js:294-302` の `pickGame` も二重に載せ直す。
> **`tools/probe-rules-key.js` のコメントが1つ古かった**のを、そのまま前提にしていた——
> 落とし穴16（過去の断定を無条件に前提としない）そのもの。道具も「過去の断定」に入る。

実際に消えるのは**もっと広い**。本物のサーバーを立てて、
ゲスト端末に届いた `room.state.data` を見た実測：

| いつ | `game` | `data` の中身 | `modeId` | `mode` |
|---|---|---|---|---|
| 待合（協力版をえらんだ） | `bomb` | `["modeId"]` | `bomb-coop` | — |
| **ゲーム中** | `bomb` | `phase`／`mode`／`total`／`players`／`team`／`board`… | **無い** | `coop` |
| 「もう一度」のあと | `bomb` | `["modeId"]` | `bomb-coop` | — |

理由は `realtime.js:460`（`publicSnapshot`）：

    data: driverOf(room) ? driverOf(room).publicView(room) : room.state.data

**ゲーム中は `state.data` が publicView に丸ごと差し替わる。**
放送は `room:update` 1本だけなので、`modeId` は端末に**一度も届かない**。

つまり `if(rtPickedMode() && rtPickedMode().coopLayout)` と書くと、
**ゲーム中は必ず偽**。しかも待合では大画面の bomb 描画をそもそも通らない（`17257` の振り分けは
始まってからしか走らない）ので、**一度も真にならない。**
エラーは出ず、`v.mode` を触っている検査は緑のまま、
実機で「協力版なのに競争版の画面が出る」だけが起きる（落とし穴37：書いてあるのに効いていない）。

だから**正はサーバー**。`MODES.coopLayout` は**棚・設定の側の宣言**として持ち、
「`coopLayout:true` のモードで遊ぶと `v.coop === true` になる／その逆も」を
検査が両方向で見る（落とし穴20：A⊆B を書いたら B⊆A も要るか考える）。

**前例がある**：`wordwolf-room.js:67`・`108` が `MODES` の `wolfMulti` を
`multiTurn:` として publicView に載せている。同じ形にそろえる。

**不在で表さない**（落とし穴36）：`bomb && !bombRace` のような書き方にすると、
3つ目のモードが来た日に黙って協力版へ落ちる。`auctionOpen`／`auctionSealed` と同じく
**両方に印を付ける**。

**この根から出ている、別の不具合**（実測からの導出・画面では未確認）：
`nowPlaying()`（`24340-24343`）は部屋にいるあいだ `rtPickedMode()` を返すので、
**ゲーム中は null**。すると `liveGameSettings`（`24591`）が空になって
⚙の「このゲームの設定」の行が消え、`renderSetRules`（`24713-24722`）が
「このゲームの説明は、まだ用意できていません」を出す（大切なこと6とぶつかる）。
**実機で1度確かめる。**

理由は大切なこと9（借りたものは世界観ごと運ばれる）と落とし穴1。
文字列比較で散らすと、モードを足した日に**片方だけ直す**。
実際に第47弾で `mode === 'coop'` の門が競争版の決着演出をふさいでいた
（`public/index.html:17149` のコメントに記録が残っている）。

**いま散っている文字列比較**（レイアウト・表示の分岐に使っているもの）：
`index.html:15741`・`15758`・`15997`（`v.list.style === 'coop'`）、
`17139`（`結.mode === 'coop'`）、`18051`・`18063`（`r.mode`／`v.mode === 'coop'`）、
`17703`・`17748`・`17872`・`17993`（`v.mode === 'race'`）。

#### 11-3. 一番見せるべきものが一番大きい

モードごとに「**これが一目で分かればいい**」を**1つだけ**決め、それを最大にする。
決めたものは画面台帳（`docs/監査_画面一覧.md`）の「大画面の主役」列に書く。

**1つに決まらないなら、それはその画面が2つ分の仕事をしている。**

「一番大きい」は器の大きさで決まる。`.big-main` は
`font-size:clamp(64px,9vw,200px)` の**一枚看板**で、
部屋コードや勝者ひとりの名前のような**短い見出しの器**。
名前の列・一覧・複数行を入れる器ではない（入れると溢れる——宿題17）。

#### 11-4. 雰囲気と状況の両立

テーマ・演出（第47弾）は残す。ただし——

**演出の間も、状況（ライフ・時計・順位）は隠れきらない。**

コールアウトは画面の中央に大きく出るが、**上部の共通の値を覆わない**。
遊んでいる人は演出の最中も「あと何回ミスできるか」を知っていてよい。

いまの `.fx-callout` は `top:36%`（`public/index.html:967`）なので、
**共通の帯を上から 1/4 までに置けば、規則と実装が最初から噛み合う。**

##### 時計は「数えてよい締め切り」だけが持つ

大画面の巨大カウントダウン（`.big-cd`）は `v.remainingMs` を無条件に読んでいる。
ところが締め切りには2種類ある。

| 種類 | 例 | 数えてよいか |
|---|---|---|
| **人が待っている締め切り** | 話し合いの残り・解除の持ち時間・入札の締切 | **数える** |
| **画面が変わるだけの時刻** | とくとくクイズの「次の1文字が出る時刻」 | **数えない** |

区別しないと、**420pxの数字が伏せ字の問題文の上に1〜2秒ごとに出続ける**
（実測：6秒間に6回）。
**どちらかは進行役が言う**（`v.clockKind` のような属性）。
端末が締め切りの中身を当てにいかない——当てにいくと、
締め切りの意味を変えた日に遠くで壊れる。

#### 11-5. 大画面だからできること

| 使えるもの | どう使う |
|---|---|
| **画面の縁（全周）** | 知らせ。ライフが減った＝縁が赤く**1回**／もう後がない＝縁が**脈打つ** |
| **全員が同時に見る** | コールアウト・紙吹雪・入場・夜明け |
| **画面の大きさ** | 離れた席から読める文字（下） |

縁は**共通部品**にする（`FxKit.edge()`）。
いま `flashRoleEdge()`（人狼の陣営色）が `public/index.html` に個別実装されているが、
**演出は `fx.js` に足す**のが決まり（CLAUDE.md §4）。

##### 文字の最小サイズ（TVで離れた席から読める）

**前提の条件を1つに固定する**——決めないと「何インチの何メートル」で答えが変わる。

| 前提 | 値 | なぜその値か |
|---|---|---|
| 画面の作り | **1280×720**（CSS px） | 門 S9 の計測条件 |
| TVの大きさ | **50型 16:9**（画の高さ 623mm → **1px = 0.865mm**） | 学校・イベントで使う現実的な下限 |
| いちばん遠い席 | **4m** | 教室の後ろ |
| 必要な視角 | **20分角以上** | ISO 9241-303:2011 の推奨（20〜22分角）の下限 |

**この規則は「大画面らしい幅」でだけ効く**（`min-width:900px`）。
同じ画面を小さな窓で開いている時は、スマホの規則（§2-2）に戻る。

参考にした数字：

- **ISO 9241-303:2011** … 画面の文字は **20〜22分角**を推奨、**最小16分角**
  （[ISO 9241-303:2011 preview](https://webstore.ansi.org/preview-pages/ISO/preview_ISO+9241-303-2011.pdf)）
- **国土交通省の案内サイン** … 文字高 ≒ 視距離 ÷ 250（**13.8分角**）。
  ただしこれは**視力0.5の人が視認できる下限**で、読み続ける基準ではない
  （[移動円滑化整備ガイドライン](https://www.mlit.go.jp/common/001223022.pdf)）
- **このアプリ自身** … スマホの本文15px を 35cm から見ると **25.5分角**
  （375px幅＝およそ65mm → 1px ≒ 0.173mm）。ISO推奨より上

計算：20分角 → 4000mm × tan(20/60°) = 23.3mm → 23.3 ÷ 0.865 = **26.9px**。
切り上げて **28px**（20.8分角）。

| 役割 | 最小（1280×720） | 4m・50型での視角 |
|---|---|---|
| **主役**（11-3 で決めた1つ） | **96px** | 71分角 |
| **数字**（得点・時計・残り・ライフ） | **48px** | 36分角 |
| **本文・名前・札** | **28px** | 21分角 |

**これ以下でよい文字は、大画面に出さない。**
逃げ道（「添えだから小さくてよい」）を作らない——
作った瞬間に、どれが添えかを手で決める一覧ができる（落とし穴4）。
読めない文字を出すのは、**出していないのに出したつもりになる**という点で、
出さないことより悪い。

**いまの現物**（同じ条件で解いた `clamp()`。16か所のうち**14か所が 28px に届かない**）：

| セレクタ | 宣言 | 1280×720 | 視角 | 判定 |
|---|---|---|---|---|
| `.big-main` | `clamp(64px,9vw,200px)` | 115.2px | 85.7分 | ○（主役の96pxも満たす） |
| `.big-sub` | `clamp(20px,2.1vw,38px)` | 26.9px | 20.0分 | **×**（あと1.1px） |
| `.big-phase` | `clamp(20px,2vw,40px)` | 25.6px | 19.0分 | **×** |
| `.big-rules .rules-ol` | `22px` | 22.0px | 16.4分 | **×** |
| `.bb-head`／`.bb-name` | `20px` | 20.0px | 14.9分 | **×** |
| `.big-turn` | `clamp(16px,1.5vw,28px)` | 19.2px | 14.3分 | **×** |
| `.big-list` | `clamp(16px,1.5vw,30px)` | 19.2px | 14.3分 | **×** |
| `.bb-meta`（数字） | `18px` | 18.0px | 13.4分 | **×**（数字なので48px要る） |
| `.big-mk .bk-l` | `clamp(14px,1.4vw,22px)` | 17.9px | 13.3分 | **×** |
| `.big-mk .bc-l` | `clamp(12px,1.15vw,18px)` | 14.7px | 10.9分 | **×** |
| `.blc-name` | `14px` | 14.0px | 10.4分 | **×** |
| `.big-mk .bc-no` | `13px` | 13.0px | 9.7分 | **×** |
| `.bk-hot`／`.bc-sold` | `clamp(11px,1vw,15px)` | 12.8px | 9.5分 | **×** |
| `.bft-note` | `var(--fs-sub)` | 12.5px | 9.3分 | **×** |
| `.bl-item .bl-title` | `.62em` | 11.9px | 8.9分 | **×** |
| `.blc-title` | `10px` | 10.0px | 7.4分 | **×**（視認下限13.8分の**約半分**） |

**11か所は、国交省の「視認できる下限」（13.8分角）にすら届いていない。**

見張り：1280×720 での機械計測（門 S9）。
`.big-main` の 115.2px は、宿題17 が実ブラウザで測った値と**一致した**——
計算の側も健全（落とし穴28）。

#### 11-6. 秘密は映さない・二人称を使わない

- 大画面に出してよいのは**公開情報だけ**。見張りは `tests/secrecy-gates.js`。
  **新しい表示を足す時は、必ずここを通す**
- **「あなた」を使わない**（第48弾 48-2）。大画面には「あなた」が居ない——
  見ているのは全員で、誰のことか決まらない

#### 11-7. 順位表は共通の1部品

順位・得点を並べるものは、**アプリ全体で1つの部品**を使う（`renderRanking(rows, opts)`）。

> **【訂正・2026-09-18】指示は「既存3か所」と書いているが、掃き出したら8か所あった。**
> 4つのゲーム（すごろく・オークション・クイズ王・クイズ解除の競争版）が、
> **それぞれスマホ用と大画面用を別々に持っている**。

| # | どこ | 行 | 形 |
|---|---|---|---|
| 1 | すごろく・スマホ | `13768` | 4列（位／名前／数／内訳）・`.mk-rank .arow` |
| 2 | すごろく・大画面 | `14263` | `.bl-item` の行・**アイコンが付くのはここだけ** |
| 3 | オークション・スマホ | `16711` | 4列・`.mk-rank .arow`（**1と同じ規則**） |
| 4 | オークション・大画面 | `16883` | **文字列を join して `.big-sub` に1本** |
| 5 | クイズ王・スマホ | `15930` | 2列・`.quiz-board .qb-row`（**別系統**） |
| 6 | クイズ王・大画面 | `15989` | 文字列 join |
| 7 | クイズ解除（競争版）・スマホ | `17905` | チップ状・`.vote-counts`（**人狼の投票集計から借りた名前**） |
| 8 | クイズ解除（競争版）・大画面 | `18055` | 文字列 join |

**これに加えて、得点を並べるだけの一覧が15か所ある**（手渡しのスコア画面・ラウンド結果・
ワードウルフの結果・チップオークション・クイズの途中経過・みんなの状況・記録…）。
共通部品にするのは**順位（`rank`）を持つ8か所だけ**——
落とし穴22：「順位」という言葉が同じ意味になるのは、そこまで。

#### 先に片づける前提が4つある（部品を書く前）

1. **CSSの系統が2本並走している。**
   (A) `.mk-rank .arow/.ark/.arn/.ars/.arb`（`2331-2338`・オークション発祥、すごろくが借用）と
   (B) `.quiz-board .qb-row/.qb-name/.qb-score`（`3349-3355`）。
   **テーマの上書きは (B) にしかない**（`.app.theme-quiz`／`theme-auction`／`theme-falsetrue` の3件）。
   (A) に寄せると**その3件が宙に浮く**（落とし穴30・31）。
   → 新しい部品は `:root` のトークンだけで組み、**テーマ側に書き足さない**（落とし穴27の「両方の親に置く」と同じ判断）。
2. **`.qb-row` は消せない。**着ている4か所のうち**3か所は順位表ではない**
   （途中経過 `15592`・ラウンド間 `15674`・早押しの合間 `15840`）。
   順位表だけ移し、`.qb-row` の規則は**残す**。
3. **同着の扱いが4通り。**`tied` を返すのは `SugorokuLogic.rankPlayers` だけ。
   `AuctionLogic.rank` は**第2キーが無く同点の並びが入力順に依存**し、
   `BombLogic.rankPlayers` は**`rank` に `null` がありうる**（ライフ0＝記録なし）。
   → **`tied` を4か所に足さない。部品が rows を1周数えて導く**（落とし穴1の作り先を増やさない）。
4. **前置きに `rank` は使えない。**`.rank`／`.rank.gold` を手渡しのスコア画面が既に取っている（`586-587`）。
   掃いて空いていることを確かめた **`rk-`** を使う（`.rk-` `.rank-` `.lb-` `.rnk-` `.board-` は現物に1件も無い）。

#### 部品の形

    renderRanking(rows, opts)

    rows: [{ id, rank, tied, name, main, note, out }]
      rank … number | null（null は「記録なし」）
      main … 主たる数字の**表示用文字列**（'12点' / 'あがり' / '3本'）
      note … 内訳。null なら欄ごと出さない
      out  … 記録なし・脱落
    opts: { big, showFace, highlight }

**部品は点の意味を知らない。**単位（点／マス／本／枚）はゲーム側が済ませてから渡す——
部品の中に対応表を持つと、ゲームを足すたびに書き足すことになる（落とし穴4。
`RT_STATUS_LABELS`（`16958`）が既にその形で腐りかけている）。

| 列 | クラス | 中身 |
|---|---|---|
| 順位 | `.rk-place` | 1位・2位…。**同着は「N位ならび」**（いま言葉にしているのはすごろくだけ） |
| 姿 | `.rk-face` ＋ `.rk-name` | アイコン＋名前（§10 の名簿の1人分の考え方） |
| 数 | `.rk-main` | `DotGothic16`・11-5 の最小サイズ |
| 内訳 | `.rk-note` | 無い行は**要素ごと出さない**（空の欄を残すと理由が分からない） |

- **1位は大きい**（`.rk-row.is-top`。**同着なら全員**）
- 並べ替えの動きは **0.4秒**（§4 の「見せ場」0.75秒と「切り替え」0.25秒の間。
  順位が動くのは見せ場だが、行が何本も同時に動くので、跳ねると読めない）
- **いま部屋版の順位表には演出が1つも無い**（8か所とも `stagger` も `countUp` も持たない）。
  動いているのは手渡しの `scoreList`（`24163`）だけ——
  **いちばん褒めるべき画面が、いちばん静か**になっている（大切なこと7）
- 検査が `.arow` を名指ししている3行（`tests/fixes52.js:674`・`726`、`tests/rt-screens.js:3517`）を
  同時に直す（落とし穴32）
- 部品カタログ（`scr-catalog`）に1枚足す。**いまカタログに順位表が1つも無く、
  6テーマ×3サイズを一度も通っていない**

#### 11-8. 安全基準は §6 のまま

点滅は1秒に3回まで／揺れは0.2秒・1回／**赤は縁のみ**／全画面の明滅は1回だけ。
11-5 の「縁が赤」は、**縁のみ**の規則にそのまま合う（面を塗らない）。


---

## 提案：「否」の扱い（指示55 2-3・この指示で直すか、切り出すか）

**本人が承認した（2026-09-18）。**

### 結論：(1)(2)(3) はこの指示で直す。(4) の一部を切り出す

(2) が 9/9 で「否」になったのを見て「9枚の画面を書き直す」と読むと、規模が読めなくなる。
実際には**3つの共通の穴（A・B・C）が9回見えている**だけで、
穴を塞ぐと (2) の大半が同時に片づく。

| 段 | 何をするか | 効く範囲 |
|---|---|---|
| 1 | **§11 を正本に置く**（規則） | 全部。新カセット6本の門にもなる |
| 2 | **穴B・Cを共通の器で塞ぐ**：大画面の上部帯（共通ライフ・共通の時計）を1つ作る／時計の種類を進行役が言う形にする／文字サイズの下限を効かせる | 9モード全部＋新カセット |
| 3 | **穴Aを塞ぐ**：モードごとに「主役」を1つ決めて `bigMain` に置き直し、台帳の列に書く | 9モード（1モードあたりは小さい） |
| 4 | **順位表を共通の1部品に**して既存3か所を寄せる（2-4） | すごろく・オークション・クイズ王 |
| 5 | **(1) の3件を直す**（協力版クイズ解除・こまはひとつ・つぎつぎクイズ協力） | サーバー側も1行ずつ |
| 6 | **(3) の見張りを足す**（大画面の秘匿を、画面の側から両方向で） | 全部 |

### 切り出しを提案するもの

| # | 何 | なぜ別か |
|---|---|---|
| A | **(4) の演出を新しく作る分**——クイズ王3ゲーム（つぎつぎ・とくとく・早押し）と、すごろく2ゲーム（どこにいる？・てふだ）に、大画面だけの演出を**新しく考える** | §11 は「使っているか」を問う規則で、**何を作るかはカセットごとの創作**。規模が読めないうえ、遊びの手ざわりを決める判断が要る（本人の領分） |
| B | **称号「パスを1回も使わなかった」が解除できない**（`v.rush.passLimit` を誰も送っていない） | 大画面と無関係。称号の話 |
| C | **死因の語彙が3か所で並走**（謎の死／呪殺 ほか） | いま壊れていない。型1の再発源だが、指示55の門に無い |
| D | **`RT_PHASE_LABEL` に実物解除の3段階が無い**（大画面に生の英語が出る） | 小さいので**この指示で拾う**方に倒してもよい。本人の判断 |

### この指示に**入れる**ことを提案するもの（指示に明記は無い）

| # | 何 | なぜ入れるか |
|---|---|---|
| ア | **宿題17（大画面の生存者一覧が溢れる）** | §11-1・§11-3 の規則違反そのもの。§11 を作る以上、これを残すと**最初から例外が1つある規則**になる |
| イ | **とくとくクイズの伏せ字が1182pxはみ出す** | §11-3（一番見せるべきものが一番大きい）の対象。**問題文が画面の外にある**ので、いまこのゲームはTVで遊べない |
| ウ | **巨大カウントダウンが伏せ字の上に1〜2秒ごとに出る** | 同上。§11-4（演出の間も状況は隠れきらない）の裏返しで、**状況でないものが主役を隠している** |
| エ | **False or True の `.bft-case.is-held` が一度も当たらない** | §11-3。大画面から「いまどのケースが動いているか」が分からない |
| オ | **すごろくの「手番のない段階」が2つしか無く、関係ない人の名前が200pxで出続ける** | §11-3。てふだでは200pxの文字と22pxの帯が**反対のことを言う** |

---

## 棚卸し②：問題バンク（指示55 1節）

対象は `public/js/quiz-bank.js` の **82問**（指示の前提は85問だったが現物は82問）。
**他のクイズ系が別の問題データを持っていないことも確かめた**——早押し・すごろくのミニゲームを
含めて全部 `quiz-bank.js` 経由（`bomb-room.js:24`・`quiz-room.js:28`・`sugoroku-room.js:32`・
`public/index.html:19601`・`22352`）。

※ 紛らわしいが、`public/index.html` の `QUIZ_BANK`（`9099`〜）は**別物**——
あれそれどれこれのお題（`name`／`yomi`／`ng_words`・206件）で、宿題15 として切り出し済み。

※ つぎつぎクイズの `LIST_TOPICS`（11本・答え257語）は、この節の後半に別立てで書く。

### 校正の結果（82問・2段構え）

**1段目（校正）と2段目（反証）を別の担当が持ち、2段目は「同意するためではなく、
間違いを見つけるために読む」形にした。**結果は下のとおりで、**2段目が効いた。**

| | 数 |
|---|---|
| 読んだ問題 | **82**（easy 25／normal 20／hard 15／nanisore 12／muri 10） |
| 1段目が出した要修正 | 14 |
| **反証が足した要修正** | **+10** |
| **突き合わせ後の要修正** | **24** |
| **出典の差し替えが要る問題** | **28** |
| 1段目の案が不適切だったもの | 15 |
| 反証の異議（一意） | **69**（見落とし17／根拠が弱い33／案が不適切17／過剰な指摘2） |

#### いちばん大きい学び：**URLを貼っただけでは「根拠」にならない**

異議69件のうち**33件が「根拠が弱い」**。
反証の担当が出典のURLを**実際に開き直したら、主張がそこに書かれていなかった**。

| 問題 | 何が起きていたか |
|---|---|
| 関ヶ原の戦い | `japanknowledge` のURLが 301 で販促ページに飛び、**勝者は一行も書かれていない** |
| 「あいうえお」の次の行 | 文化庁「現代仮名遣い」は目次ページで、五十音図の並びが無い |
| ピアノの鍵盤 | ダミーの根拠にした公式ページが **403 で読めていない**のに「確認済み」と書かれていた |
| 雪が積もる季節 | 出典に無い日付が混じっていた（4月17日→実際は18日） |
| オーケストラの配置 | 挙げた2つのURLが、その主張を一行も含んでいない |
| モナ・リザ | 日経の記事では裏が取れない（美術館の公式に差し替え） |
| 世界で一番小さい国 | 出典を開けていない |

**指示55 2-5 の「記憶だけで正しいとしない」は、URLを添えれば満たせるものではなかった。**
恒久の見張り（`source` 欄）を作る時は、**URLが生きていること**まで見ないと同じ形が残る。

#### 反証が新しく見つけた要修正（10件）

| 問題 | 1段目 | 反証が見つけたこと |
|---|---|---|
| 世界一長い川は？ | 可 | 現物は public/js/quiz-bank.js:102（choices は ナイル川／アマゾン川／長江、correct:0）。前段の写しは正確で、node で実引きして一致を確認した。争うのは判定の方。前段は a_unique を「可」とし、アマゾン説を issue 欄の注記にとどめて「現状維持」とした。だが index 11 との決定的な違いを見落としている——11 では争点の「品川〜横浜」が選択肢に無いので遊びは壊れないが、14 は争点そのもの（アマゾン川）が3択の1つとして出ている。バンク自身の決まり（… |
| 虹はふつう何色といわれる？ | 可 | 前段は issue 欄に「どこで」ふつうなのかが省略されている、ダミーの5色は“別の国では正解”という型、と自分で書きながら b_premise を「可」にし、修正を「厳密に閉じたいなら」の付帯条件に落とした。これは指示が探せと言っている『マラソン』型そのもの。(1) 実際にレファ協の原文を開いて確認したところ、前段の要約はさらに甘い——原文は 7色＝日本・オーストラリア・フランス／6色（藍なし）＝アメリカ・イギリス／5色（藍・橙なし）＝ドイツ・ロシア／5色（藍・紫なし）＝ベルギー。つまり「7色＝日本だけの通念」で… |
| サッカーは1チーム何人でプレーする？ | 可 | 前段は b_premise を「可」にしたが、自分の issue 欄で『JFA全日本U-12は2011年から8人制で、サッカーをやっている子ほど「8人」と答えたくなる』と書いている。書いてある内容と判定が食い違っている。しかもこの判定は、このプロジェクト自身が指示55で確立した基準と正面からぶつかる。docs/監査_指示55の門.md:581（人間の骨）は『選択肢に「約300本」が無いおかげで結果的に答えは1つに絞れる（＝(a)は可）が、それは選択肢の運であって問題文の力ではない』として (b) を否にしている。同… |
| 「うるう年」は何年に一度くる？ | 可 | 前段は100年・400年の例外だけを見て「(a)(b)とも可、このままでも出せる」としたが、もっと効く見落としがある。「うるう年」は新暦（グレゴリオ暦）だけの語ではなく、**太陰太陽暦（旧暦）の「閏月がある年」も「うるう年」**である。精選版 日本国語大辞典は閏年の①を『陰暦で、閏月のある年。一三か月ある年。』、②を『現行の太陽暦で…四年に一度』と、2つ並べて定義している。そして旧暦の閏年の周期について、改訂新版 世界大百科事典は『2年か3年に一度，ある月を2ヵ月おいて1年を13ヵ月にして』、ブリタニカ国際大百科事… |
| 雪が積もる季節は？ | 可 | (b) 前提の省略。問題文に地域の限定が無く、しかも選択肢に「夏」が入っている。南半球（オーストラリア・ニュージーランド等）では6〜8月＝日本の夏が冬で、雪が積もるのはまさにその時期なので、地域を言わないかぎり「夏」も真になる。これはタスクが名指しした型（「日本で」「世界で」が抜けている）そのもので、前段はこの可能性を一度も検討せず、北海道の春（＝「春」の側）だけを見て終わっている。しかもバンク自身の書き方が反証になっている：答えが日本限定になる問いには必ず「日本で／日本の」が付いている（public/js/qui… |
| 日本で最初の鉄道が開通した区間は？ | 可 | 【判定と記述が矛盾している】前段は issue に「厳密には観点(b)の型に触れている」と自分で書きながら、b_premise を「可」にした。私が国立公文書館の本文を自分で取得して確認したところ、指摘の中身は正しい——「品川・横浜（現在の桜木町）間に我が国はじめての鉄道が仮開業され」（明治5年5月7日）、「新橋－横浜間の鉄道が本開業」（9月12日）。つまり最初に鉄道が開通した区間は品川〜横浜であり、q の「最初の鉄道が開通した区間」は、仮開業を含む読み方では品川〜横浜が正解になる。これは実機報告の「マラソン」型そ… |
| 南極と北極、平均気温が低いのは？ | 可 | 現物は public/js/quiz-bank.js:112。(c) が確実なことは同意する（環境省 南極キッズを実際に開いて確認：「北半球、シベリアのオイミヤコンの最低気温は-71度ですが、南極（ボストーク）の最低気温は-89.2度です」「北極の氷の厚さは最大で10ｍぐらい。しかし、南極の平均の高さは約2,500ｍもあります」）。だが前段の「差は30度規模なのでダミー『ほぼ同じ』も明確に外れ、(d) も安全」という論理は**逆**。明らかに外れているダミーは、良いダミーではなく悪いダミーである——quiz-ban… |
| 「バウムクーヘン」はドイツ語で何という意味？ | 可 | 前段は (d) の弱さを本文で長々と説明しておきながら d_distractor を「可」にしている（自分の記述と判定が矛盾）。実際に出典を当てると、弱いどころか「否」にすべき水準。①語源由来辞典の本文は Kuchen を『菓子』と訳しており（「"baum"は「木」、"kuchen"は「菓子」を意味し」）、バンクの正解が使う『ケーキ』ではなくダミー側の語と一致する。②Wikipedia のバウムクーヘンの作り方は「薄い層を10〜20層程度重ねて作る」——つまりダミー「重ねた菓子」は実物の正しい説明そのもの。よって「… |
| 「アボガドロ定数」はおよそ？ | 可 | 前段は (d) を「可」とし、「ダミーの桁が全く違うので運にはならない」と書いた。運にはならないが、それは問題が成立している理由ではなく、問題が壊れている理由。quiz-bank.js:15-18 の決まりは「不正解の選択肢は、正解と同じジャンル・同じくらいの知名度のものにする。ジャンルがばらけていると、内容を知らなくても消去法で当たってしまう」で、悪い例として「1つだけ食べ物で浮いている」を挙げている。ここは向きが逆になっただけで同じ欠陥——6.02×10^23 は誰もがどこかで見たことのある実在の文字列、3.1… |
| ピタゴラスが発見したとされる音楽と数の関係は？ | 可 | 現物は public/js/quiz-bank.js:109（node で取り出した内容は前段の写しと一致）。前段は d_distractor を「可」としたが、2つあるダミーのうち**「音は空気の重さで決まる」を一度も検討していない**。問題文は「音楽と**数**の関係は？」と聞いているのに、この選択肢には比も数も出てこない——知識ゼロの子でも、問題文の言葉づかいだけでその場で消せる。残る2つ「弦の長さの比が和音を作る」「音階は12個」はどちらも数の話で、しかも前段自身が示したとおり12はピタゴラス絡み（ja.w… |

**とくに「世界一長い川は？」は、私も独立に疑って同じ結論に達した**——
争点の**アマゾン川が3択の1つとして出ている**ので、(a) が崩れる。
反証の案は「問題文は変えず、ダミーをミシシッピ川に差し替える」。

#### 突き合わせ後の要修正 24件

| # | 問題 | 観点 | 直し方 |
|---|---|---|---|
| 1 | 人間の骨はおよそ何本？ | b | 直すべき。修正案そのもの（「大人の骨はおよそ何本？」に変える・選択肢と正解位置はそのまま）は妥当なので採用してよい。ただし格付けは (b)否 に加えて **(a)否** も立てること。理由を「前提の省略」だけにすると、次に同型の問題を見た時に『選択肢に無いから大丈夫』という誤った免罪符が残る。あわせて文言を変えたら `node tools/dump-ui-text.js` を回して docs/文言… |
| 2 | マラソンの距離は？ | b | q を「フルマラソンの距離は？」に変更する（選択肢・正解位置は現状のままでよい）。**文言変更の影響は確認済みで、安全**：q の文字列をキーにしているのは (1) public/js/quiz-bank.js:213 の `!(used ／／ {})[q.q]`、(2) public/js/bomb-logic.js:183 の `seen[q.q] = true` の2か所だけで、どちらも「同… |
| 3 | パンダの体の色の組み合わせは？ | a・b | 問題文を「ジャイアントパンダの体の色の組み合わせは？」に変える（正解位置はそのまま0）。ダミーは「茶と白」をレッサーパンダ連想のまま残してよい（同ジャンル・同知名度で、むしろ良いダミーになる）。／id変更の影響：無害と確認済み。(1) 記録側で q をキーにしているのは public/js/bomb-logic.js:183 の seen[q.q] → public/index.html:2235… |
| 4 | 「シャルル・ド・ゴール」は何の名前になっている？ | b・d | 外さずに直せる。(b) は問題文を「フランスの元大統領シャルル・ド・ゴールの名前がついている、パリの玄関口は？」または「パリにある国際空港の名前になっている、フランスの元大統領は？」のようにカテゴリを明示する形へ。(d) はダミーの都市をパリに揃えず、かつ実在の「シャルル・ド・ゴール〜」とぶつからないものにする（例：「パリの空港」「パリの図書館」「パリの美術館」——図書館はミッテラン、美術館はオル… |
| 5 | カフェオレとカフェラテの違いは？ | a・d | 外さずに直せる。q は変えず、ダミーの「牛乳の量」を事実として誤りのものに差し替えるのが最小の直し方（例：「豆の産地がちがう」「カップの大きさがちがう」。どちらも定義上の違いではないので一意になる）。問題文の側で締めるなら「カフェオレとカフェラテの、ベースになるコーヒーの違いは？」とし、選択肢を「ドリップとエスプレッソ」「浅煎りと深煎り」「水出しと湯出し」に揃える手もある。影響：choices も… |
| 6 | 「いろは」で始まる文字は全部で何文字？ | b | 問題文を「『いろは歌』は全部で何文字でできている？」に直す（「歌」の1字を足すだけで、五十音との読み違えも48字の流儀も同時に閉じられる）。q の文字列を変えても安全：q をキーにしている箇所はすべて実行時の照合で、保存された記録の鍵ではない（public/js/defuse-logic.js:489・public/index.html:18892 は1試合内の出題済み判定、tests/fixes… |
| 7 | キーボードの並びが「QWERTY」になった理由の定説は？ | a・b・c・d | 外す。理由は二重で、(a)選択肢1が実在する対立仮説なので答えが一つに決まらない、(c)「定説」という前提そのものが事実でない。言い換えでは救えない——「よく言われている理由は？」に直しても、誤りとされている説を正解として教える点は変わらない。差し替えるなら争いの無い機械的事実（例：QWERTYの最上段の文字だけで TYPEWRITER が打てる）にする。影響：q 文字列をキーにしているのは pi… |
| 8 | ぞうの鼻はどこが伸びたもの？ | a・d | 作り直す。案①（事実を残す）：q を「ぞうの長い鼻は、鼻と何が一緒にのびたもの？」、choices を ['上くちびる','したくちびる','した']、correct 0。口まわりの部位でジャンルがそろい、問題文と正解の語の重複も消え、包含関係も無くなる。ただしこの形にすると事実の難しさがそのまま出るので、tier は normal（大人ならだいたい知っている）へ移すのが妥当。案②（easy に残… |
| 9 | 「フィボナッチ数列」の最初の6つは1,1,2,3,5と、次は？ | b | 直すべき。ただし直すのは proposal の影響欄であって、問題文の案そのもの（「フィボナッチ数列「1, 1, 2, 3, 5」の次の数は？」）は妥当で変更不要。影響欄に public/js/quiz-bank.js:213 と public/js/bomb-logic.js:183 を名指しし、「どちらも1マッチ内の揮発マップなので永続的な害は無いが、出題済み印が一度だけリセットされる」まで書… |
| 10 | 人体で一番大きい臓器は？ | a・b・d | （1）差し替え案：問題文を「人体で一番大きい内臓は？」、選択肢を ['肝臓','肺','胃']、正解を0（肝臓）にする。内臓と限定すれば出典（日本移植学会）と一致し、答えが一つに決まる。（2）皮膚の意外性を残したいなら「体の表面をおおう、人体で一番重い器官は？」のように限定が要るが、子ども向けには文が重い。（3）どちらも採らないなら外す。／文言変更の影響：q の文字列を鍵にしているのは publi… |
| 11 | 音の速さは秒速およそ何メートル？ | b | 問題文を「空気中を伝わる音の速さは秒速およそ何メートル？」に変える（選択肢・正解位置は変更なし）。／q 文字列変更の影響：この5問の q はリポジトリ内では public/js/quiz-bank.js:83 と、自動生成物 docs/文言一覧.md（3623行目・6955行目）にしか現れない。記録キーの懸念とされた tests/fixes45.js:761 は `win.QuizBank.QUE… |
| 12 | 将棋で、最初に持っている駒は何枚？ | b | 直すこと自体は賛成（そのままにしない）。ただし proposal の文言はそのまま採らない。「将棋で、一人が使う駒は全部で何枚？」のように盤の初形に依存しない言い方にするか、16枚のダミーを入れ替える。 |
| 13 | ハチミツが腐らないおもな理由は？ | a・c・d | 外す。3つとも正解になりうる以上、選択肢の入れ替えでは直らない（正解も含めて全部が同じ現象の別の面だから）。もし同じ題材を残したいなら、仕組みを1つに絞った別問題として作り直す：q「ハチミツの中で細菌が増えられないのはなぜ？」／正解「糖が水をつかまえて、菌が使える水が無いから」／ダミー「まわりより温度が低いから」「油の膜でふたがされているから」。q が変わるが、“出した問題”の集合は public… |
| 14 | 囲碁で「劫（コウ）」とは？ | b・c | 問題文を「囲碁の『コウのルール』とは？」に変える。選択肢は1つも触らずに (b)(c) が同時に解消する。（別案：問題文はそのままで正解を「交互に石を取り返し合える形」に変える。こちらは「二眼の形」が本物のダミーになり muri らしさは増すが、「端の石」が形ではないのでジャンルが揃わなくなる。前案を推す。）q を変える影響：q をキーにしているのは1試合内の重複防止だけで永続しない（public… |
| 15 | 世界一長い川は？ | 反証 | 直すべき。ただし問題文（q）は変えないのが最善。ダミーの「アマゾン川」を、世界一の座を争っていない大河に差し替える（推奨：ミシシッピ川。外務省キッズ外務省でも ja.wikipedia の一覧でも世界4位前後の大河として載り、『世界の代表的な大河』というジャンル条件を満たしたまま、正解になりうる余地が無い）。q を変えないので、前段が調べた q-as-key（quiz-bank.js:213 の … |
| 16 | アメンボが水に浮くのに使っている力は？ | 反証 | 直すべき。ただし直す場所は前段が言う問題文ではなく tier。この問題は easy か normal へ移す（表面張力という語を知っているかで割れるので normal が妥当）。そうすれば問題文も選択肢も1文字も変えなくてよく、docs/文言一覧.md の再生成も不要になる（tier は文言ではないため）。移動先の tier でも tests/fixes45.js:761 と tests/smok… |
| 17 | 雪が積もる季節は？ | 反証 | 直す。最小の直しは「日本で雪が積もる季節は？」（「日本で」を足すだけ）。前段が注記した北海道の春（札幌の終雪平年4月21日＝気象庁の区分では春）まで潰すなら「日本で雪がいちばん積もる季節は？」。どちらも小学生の語彙の内で、選択肢（冬／夏／春）は変更不要。文言を変える影響は実質ゼロで、これは実物で裏を取った：q を鍵にしているのは quiz-room.js:93 の used、sugoroku-ro… |
| 18 | サッカーは1チーム何人でプレーする？ | 反証 | 直すべき。b_premise を「否」に改め、問題文を具体化する。docs/監査_指示55の門.md:583 が「人間の骨→大人の骨」でやったのと同じ最小の直し方に揃えるなら『ワールドカップのサッカーは、1チーム何人でプレーする？』が良い（W杯は11人制しかないので8人制もフットサルも同時に閉じられ、小学生にも読める具体名）。選択肢・正解位置（correct:0）は現状のままでよい。public/… |
| 19 | 「うるう年」は何年に一度くる？ | 反証 | 直すべき。b_premise を「否」に改める（パンダ precedent に従うなら a_unique も「否」寄り）。問題文を『いまのカレンダーで、うるう年はふつう何年に一度くる？』に直す——「いまのカレンダー」で旧暦を閉じ、「ふつう」で100年・400年の例外も同時に閉じられ、どちらも小学生が読める言葉。選択肢・正解位置（correct:0）はそのままでよく、public/js/quiz-b… |
| 20 | 「バウムクーヘン」はドイツ語で何という意味？ | 反証 | 直すべき。(d) は「可」ではなく「否」。「重ねた菓子」を、実物の説明にもなっておらず部分知識とも衝突しない別のドイツ語菓子の直訳に差し替える（例「王様のケーキ」「白いパン」）。choices のみの変更で q は据え置けるので、q をめぐる懸念は不要（下の index 4 の争点を参照）。 |
| 21 | 「アボガドロ定数」はおよそ？ | 反証 | 直すべき。(d) は「可」ではなく「否」。ダミーを、実在してそれなりに知られた別の物理定数の値に差し替える（例：光の速さ 2.998×10の8乗、気体定数 8.31、重力加速度 9.8）。表記を日本語読みに直すという前段の提案自体には同意する（「^」がそのまま出るのは事実。下の争点で検証済み）が、表記だけ直してもこの一意の「本物っぽさ」は残るので、両方やる必要がある。 |
| 22 | 日本で最初の鉄道が開通した区間は？ | 反証 | 直すべき。(b) は「可」ではなく「否」。文言を「日本で最初の鉄道が正式に開業した区間は？」に変える（選択肢は現状のままでよい）。変えた場合は docs/文言一覧.md の作り直しが要る。変えない判断を採るなら、前段の「将来ダミーに品川〜横浜を入れてはいけない」という申し送りは必須でバンクのコメントに残すこと。 |
| 23 | ピタゴラスが発見したとされる音楽と数の関係は？ | 反証 | 直すべき。前段の「ダミー1つを差し替える」では足りない——ダミーは2つとも差し替えが要る。正解文を「弦の長さが簡単な整数比のときよく響く」に直す点だけは同意する（concords の訳として正しくなる）。 |
| 24 | 虹はふつう何色といわれる？ | 反証 | 直すべき（軽微だが必須側に倒す）。public/js/quiz-bank.js:41 を『虹は日本でふつう何色といわれる？』に変える。これで (b) が完全に閉じ、同ファイルの他9問と書き方もそろう。小学生の読みやすさも落ちない（「日本で」は同じバンクの easy 5問で既に使っている語）。影響は quiz-bank.js:41 の1行と、docs/文言一覧.md:3497・6831 の作り直し（… |

#### 外す提案（3件）

| 問題 | なぜ言い換えでは救えないか |
|---|---|
| キーボードの並びが「QWERTY」になった理由の定説は？ | **「定説」が存在しない。**e-Words は「諸説ありはっきりしない」と書き、3説を並列に置く。うち1説はダミー「英語で打ちやすいため」の言い換えなので (a) が崩れ、「定説」という前提自体が (b)(c) で偽。※反証：**差し替え案「最上段だけで TYPEWRITER が打てる」は採ってはいけない**（同じ一意性の崩れを再生産する）。`nanisore` は12問で下限10なので、差し替えなしで外せる |
| ハチミツが腐らないおもな理由は？ | **3択の3つとも本当の理由**で、しかも1と3は同じ仕組みの言い換え（「砂糖だから」は「水分活性が下がる」の原因そのもの）。さらに水分は17〜20%あり「非常に少ない」とは言いにくい |
| 人体で一番大きい臓器は？ | **日本移植学会が「肝臓は、脳とともに人体最大の臓器です」と明記**しているので、ダミーの肝臓が出典つきで正解になる。※反証：外す理由は「事実が確認できない」ではなく「語の定義で正解が2つになる」——差し替え案（「人体で一番大きい内臓は？」／肝臓・肺・胃）なら残せる |


---

### つぎつぎクイズのお題 11本（`LIST_TOPICS`・答え257語）

**11本すべてが `fair`＝否だった。**「正しいのに一覧に無くて弾かれる」が全本で起きている。

| お題 | 語数 | 欠け | 誤り | 古びる | **弾かれる** |
|---|---|---|---|---|---|
| 日本の都道府県 | 47 | 可 | 可 | 可 | **否** |
| 47都道府県の県庁所在地 | 47 | 可 | 可 | 可 | **否** |
| 十二支 | 12 | 可 | 可 | 可 | **否** |
| 野球のポジション | 9 | 可 | 可 | 可 | **否** |
| 太陽系の惑星 | 8 | 可 | 可 | 可 | **否** |
| 赤い食べ物 | 15 | **否** | 可 | 可 | **否** |
| 緑の野菜 | 15 | **否** | 可 | 可 | **否** |
| 楽器 | 20 | **否** | 可 | 可 | **否** |
| 元素記号1文字の元素 | 14 | 可 | 可 | 可 | **否** |
| 歴代の内閣総理大臣 | 52 | **否** | 可 | **否** | **否** |
| 寿司ネタ | 18 | **否** | 可 | 可 | **否** |

#### 原因は1つ。`normalize()` が漢字を吸収しない

判定は `judgeListAnswer`（`public/js/quiz-bank.js:179-186`）が
`normalize`（`164-177`）を通した**完全一致**だけを見る。
`normalize` がするのは カタカナ→ひらがな／小書き→大書き／記号・長音・空白の除去／小文字化 **だけ**で、
**漢字も接尾辞も触らない。**

| お題 | 実測で弾かれたもの |
|---|---|
| 野球のポジション | **正式名称9つが全部**（投手・捕手・一塁手…） |
| 日本の都道府県 | **接尾辞を落とした47件が全部**（「東京」「大阪」→ ちがいます） |
| 47都道府県の県庁所在地 | **「市」を落とした47件が全部**（「札幌」「名古屋」「横浜」） |
| 十二支 | **漢字12字が全部**（子・丑・寅…）＋読み4つ（ね・う・み・い） |
| 楽器 | 88語を通して**83語が wrong**。リコーダー・鍵盤ハーモニカ・ハーモニカ・カスタネット・タンバリン・トライアングル・木琴・鉄琴・シンバル・オルガン——**小学校で習う楽器が1つも入っていない** |
| 緑の野菜 | 一覧の5件が漢字なので、**かなで打つと弾かれる**（「ほうれんそう」「えだまめ」「こまつな」）。通称も（アスパラ・きくな・セルリー） |
| 赤い食べ物 | 世間の「赤い食べ物」上位18語のうち**9語**（えび・キムチ・梅干し・紅しょうが・唐辛子…）。一覧の「赤ピーマン」だけ漢字なので**かなで打つと弾かれる** |
| 寿司ネタ | 全国調査の上位10のうち**5つ**（中トロ・赤身・ぶり…）。「つぶ貝」だけ漢字なので「つぶがい」が弾かれる |
| 元素記号1文字の元素 | **読み7つが全部**（すいそ・たんそ・ちっそ・さんそ・いおう・ようそ・ふっそ） |
| 太陽系の惑星 | 読み8つが全部（すいせい・きんせい…） |
| 歴代の内閣総理大臣 | 常用漢字の表記が全部（山県有朋・東条英機・宮沢喜一） |

#### 罰が「減点」ではなく「退場」

**脱落形式では、判定が `correct` 以外だとその場でゲームから外れる**
（`quiz-room.js:752-755` の `l.alive[memberId] = false`）。

> つまり、**「リコーダー」と正しく答えた子が、その一言で退場させられる。**

入力は自由記述のテキスト欄（`public/index.html:15766`）なので、
スマホで「えだまめ」とかなで確定させるのがむしろ自然な打ち方になる。
大切なこと1（プレイヤーファースト）・10（一つの点からすべてを喜ばせる）に正面から反する。

#### 「歴代の内閣総理大臣」だけは、中身そのものも壊れている

- **14人欠けている**（52件／実際は66人）。山本権兵衛・寺内正毅・加藤友三郎・清浦奎吾・林銑十郎・平沼騏一郎・阿部信行・米内光政・小磯国昭・鈴木貫太郎・東久邇宮稔彦王・幣原喜重郎 ほか
- **2024年10月以降の総理が1人もいない**。石破茂（第102代）も高市早苗（第104代・現職）も弾かれる
- **古びたのではなく、書いた日にすでに足りていなかった。**`git log -L` で追うと、この一覧が書かれた 2026-08-09（`6b7c798`）の時点で石破茂の在任は既に終わり、高市早苗が現職だった

#### 直し方には順番がある（全11本の担当が独立に同じ結論に達した）

**別名を `answers` に素で足してはいけない。**足すと同じ答えが2回得点する
（「札幌市」と「札幌」が別語として通る。実測で確認ずみ）。

1. **先に器を変える**——`answers` の1件を「正本＋別名の束」にできる形へ広げる
   （`{ name:'いちご', alias:['苺','ストロベリー'] }`）
2. `judgeListAnswer` の重複判定を、**打った文字列ではなく当たった項目**で見る
   （`quiz-room.js:747` の `l.said.push(text)` も正本側に差し替える）
3. **そのあとで**別名を足す

順番を逆にすると、足した別名がそのまま新しい穴になる。


---

## 大画面の主役（正本 §11-3・実装の記録）

§11-3 は「モードごとに『これが一目で分かればいい』を**1つだけ**決め、
決めたものは台帳に書く」と言っている。その表。

**1つに決まらないなら、それはその画面が2つ分の仕事をしている。**

| モード | 決めた主役 | どこに出すか | 状態 |
|---|---|---|---|
| クイズ解除（協力版） | **共通のライフ**（0で全員まとめて負ける唯一の値）→ 次に**共通の残り時間** | 帯（`#bigStatus`）。進み具合はゲージ1本、担当は少し離して人ごと | **済**（指示55 2-2） |
| クイズ解除（競争版） | **誰がどこまで進んでいるか**（＝いまの順位） | 横棒（`#bigBoard`）。`bigMain` は空ける | **済** |
| 実物解除 | **共通の残り時間**（既定300秒）→ 次に**共通ののこりミス** | 帯（`#bigStatus`） | **済** |
| False or True | 進行中＝**いま動いているケース**／決着＝**何人のこったか** | ケースの列（`#bigFtCases`）／`bigMain` に人数 | **済**（宿題17と合わせて） |
| クイズ王・クイズラッシュ | **誰が何点で、誰が高い難易度に粘っているか** | 順位表（`#bigRank`）。難易度は内訳の欄 | **済** |
| クイズ王・つぎつぎクイズ | **いまだれの番か**（お題は1回ぶんずっと同じで、一番大きい枠を占め続ける値ではない） | `bigMain` | **済** |
| クイズ王・とくとくクイズ | **伏せ字の問題文** | `bigMain` | **済**（はみ出しと、巨大カウントダウンの誤爆を直した） |
| クイズ王・早押しトーナメント | **問題文**（読み上げの時は出さない） | `bigSub`（対戦の組は `bigMain`） | 現状維持。**この1件だけ置き直していない** |
| オークションバトル | 競りの最中＝**いまの最高額** | `bigMain`。残り時間は帯 | **済** |
| すごろく（5ゲーム） | ゲームごとに違う（こまはひとつ＝共有駒のあと何マス／つうこうりょう＝順位／ふたりでひとつ＝出た目と分け方／てふだ＝売りに出ている札） | — | **手番の誤表示と決着の順位表は済。ゲームごとの主役は未**（宿題22へ） |
| ワードウルフ | **話し合いの残り時間** | 帯（`#bigStatus`） | **済** |
| 人狼 | **盤面**（いま誰が生きていて、この回に誰が欠けたか） | 名簿（`#bigList`。札の文字を 14px → 38.4px に上げた） | **済** |

### 穴A は、1か所の規則で塞いだ

「一番大きい枠に、すぐ上の文字を言い直させない」は**モードごとに直していない**。
描画が終わったあとに `bigMain` と `bigPhase` を見比べ、同じ語なら空にする（`言い直しを消す`）。
ゲームを足した日にも自動で効く。

空いた大きさは、盤や名簿が受け取る——
**「主役が決まっていないなら、大きい枠は空ける」**のが §11-3 の素直な読み方。

---

## 完成の門 S1〜S13

※ 実装後に埋める。
