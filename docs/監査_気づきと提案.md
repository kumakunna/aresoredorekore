# 監査：気づきと提案（第35弾・随時追記）

監査中に見つけた「プレイヤーのために追加した方がいいこと」「弊害になりそうなこと」の集約ファイル（総則ルール5）。
**バグ修正はここには書かない**（各フェーズの完了報告に書く）。フェーズEで最終整理する。

書式：`- [フェーズ] [重要度] 概要`

## 気づき・提案

- [A] [高] **本番には開始の3-2-1が存在しないことを直接確認済み（2026-08-11）。** `origin/main` は 059536f（341d003の1つ前）で、VPSはGitHubから2分おきに自動pull。さらに本番URLが実際に配信している `fx.js`（17,494バイト・本物）に `countdown` が0件であることをcurlで確認した。報告された「カウントダウンが出るゲームと出ないゲームがある」の正体はこれ（手渡しモードには昔からある・部屋モードは未pushの機能）。レビュー→pushで解消する
- [A] [中] 「演出の速さ＝スキップ」設定の人には、開始の3-2-1が一瞬で消えて見えない。共有の合図なので、スキップ設定でも「はじまるよ」の静的な一言を一拍出す方が親切（原則「伝えることは必ず文字でも出す」と同じ考え）
- [A] [中] 「部屋」ボタンの在室確認はサーバーに聞いてから遷移する（第35弾Aの修正）。返事が遅い回線では一拍待つが、いまは無言。押した瞬間に「確認中…」の小さな表示があるとより安心
- [A] [中] **「出す」（kick）は退場であって出入り禁止ではない。** 出された人は部屋コードを知っているので、手で入り直せば戻れてしまうし、切断中に出された人は room:kicked が届かず、復帰時の自動入り直しで（本人も気づかないまま）自動で戻ってしまう。学校のオフラインイベントで「出したのに戻ってくる」は気まずい事故になり得る。部屋側に「出した人の短時間の出入り禁止リスト」を持つ等の設計判断が要る（勝手に実装せず、レビューで方針を決めたい）
- [A] [中] **memberId の信頼モデルの正確な整理（レビュー後に訂正）**：room:leave のフォールバックと room:peek の在室確認（you）は「code＋memberId を知っている＝本人」というモデル。ただし memberId は「本人しか知らない秘密」ではなく、**部屋の全員に配られる部屋内共有ID**（公開スナップショットの members[].id。kick・進行役譲渡のUIに必要）。境界は「部屋の外には出ない」こと：URLに載らない・console.log に出ない・サーバーログにも出ないことを grep で確認済み（2026-08-11）。部屋の中の人同士なら、**以前から** room:join の枠引き継ぎで他人の枠に成り代われる（＝役職などの秘密も受け取れてしまう）ので、今回の追加2つ（退室・在室確認）はそれより弱い能力しか与えず、攻撃面は増えていない。ただし枠の成り代わり自体は、見知った仲間内という前提（原則4）に寄りかかった作り。根本対策は「本人しか知らない入室トークンを memberId と分ける」ことで、設計判断が要る（勝手に実装せず、レビュー待ち）

- [B] [低] **hidden旧モード4つ（秒でこれ・手渡し旧クイズ王決定戦・手渡し旧早押し・手渡し旧オークションバトル）の扱い（技術的負債の判断材料）**：`hidden:true`は「復活させた時に壊れていないか確かめたい」意図で残されており、テストが`showHiddenModes`で生存確認している（＝腐ってはいない）。ただしクイズ王・オークションは第30/31弾で部屋版に作り直されており、旧手渡し版と部屋版でロジック・用語・記録の書き方が並走している状態は落とし穴1・5（片方だけ直す／古いIDの取り残し）の温床。**復活の予定が無ければ指示36以降で削除を推奨**（記録の後方互換は残す）。復活させるなら検証マトリクスに行を足してから

- [B] [中] 「もう一度」の意味が手渡し（通算成績で続ける）と部屋（全員待合に戻ってリセット）で違う。どちらも設計意図はあるが、手渡しのスコア画面に「きょうの通算」等のラベルが無いため、**初対面の人たちは「スコアがリセットされていない」ことに気づかず混乱し得る**（学校イベントが最初の本番である点を踏まえ、レビューで中に格上げ）。ラベル1つで意図が伝わる

- [B] [低] サバイバルの決着後、「もう一度（新しい勝負として同じ設定で）」が無い。「つぎのラウンドへ」との紛らわしさを避けて意図的に消してある（コード内コメントあり）が、新しい勝負をしたい時は「モードを変える」→サバイバルを選び直す2タップになる。ラベルを「新しい勝負をはじめる」にすれば紛れなく1タップにできる

- [B] [中] AIが使えない時のエラー文言が運用者向けのまま：「サーバーにGEMINI_API_KEYが設定されていません（.envに追加してpm2 restartしてください）」がプレイヤー全員の画面に出る。本番でキー切れ・クォータ切れが起きた時、遊んでいる人には意味が分からない。プレイヤーには「AIがいまつかえません。『人が説明』で遊べます」だけを出し、運用者向けの詳細はサーバーログに置くのが良い（`AI_FAIL_LIMIT`の切替案内自体は実機で動作確認済み）

## フェーズDでの検証予定（レビューで追加指示・2026-08-11）

- [D予定] スマホのスリープ→復帰でブラウザが**タブごと破棄**した場合、socket.ioの送信バッファは失われる（新インスタンスになる）。この状態で「部屋を出る」を押した時に退室が成立するか、実機（スマホ）で明示的に確認する（フェーズAの整理②が本当に成り立つかの検証）
- [D予定] memberId＝実質的な認証情報としての露出チェックをチェックリスト化する：URLクエリ・サーバーのアクセスログ/エラーログ・クライアントのconsole.log。フェーズAの予備grepでは3か所とも露出なしを確認済みだが、Dで異常系ログ（エラー時のダンプ等）も含めて再確認する
- [A] [低] 退室の頼みがサーバーに届く前にタブごと閉じられた場合、その人は名簿に「切断中」で残る（再送はしない）。これは退室ボタンを押さずに閉じた場合と同じ、以前からの仕様（入り直しの枠を残すための設計）で、名簿には「切断中」と正直に表示される。長時間切断のままの枠を名簿から自動で畳むかは、入り直し猶予とのトレードオフなので設計判断が要る
